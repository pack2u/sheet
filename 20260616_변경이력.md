# 이력

## 2026-07-07 23:15 — 로엔그린(LG) B열 이슈 추가 + 지니팩(GP) 신규 등록
- **대상**: `_partnerExclusivePush.gs`
- **LG 헤더 배열 추가**: AP 기반 20열 구성, B열에 "이슈" 삽입, 기존 열 +1 밀림
- **LG direct map 수정**: 모든 `targetCol`을 +1, `totalCols` 19→20, `phoneTargetCols` 조정
- **GP(지니팩) 신규 등록**: prefix=GP, OC(부엉이커피) 구조 동일 12열
  - 헤더 배열, 라벨("지니팩"), direct map 추가
- **주석 정리**: B=적요 → B=이슈로 관련 주석 일괄 수정

## 2026-07-07 22:55 — 전용양식 B열 "이슈" 시스템 + 15일 마감 금지
- **대상**: `_partnerExclusivePush.gs`, `_partnerOrders.gs`, `_partnerExclusiveArchive.gs`
- **B열 헤더 변경**: 전용양식 B열(index 1) "적요" → "이슈"로 변경 (12개 업체 헤더 + 생성 코드)
- **임시기록 Z열 추가**: `_PEP_NON_PARTNER_TEMP_HEADERS_`에 Z열(25) "이슈" 헤더 추가
- **송장 수집 시 이슈 연동**: 전용양식 B열(송장번호 없는 행만) → `_issueByUid` 맵 → 허브 M열(적요) + 임시기록 Z열에 기록
- **송장 배포 시**: 허브 M열 적요가 발주탭에 이미 배포되므로 이슈도 자동 반영
- **마감 금지 15일**: 임시기록 초기화 7일→15일, 전용양식 마감 이동에서 송장미입력 건 15일간 잔류 (15일 초과 시 강제 이동)

## 2026-07-07 18:00 — 카카오 송장매칭 대폭 개선
- **대상**: `_partnerExclusivePush.gs`, `_partnerWebApp.gs`, `invoiceMatchSidebar.html`
- **파싱 3단계**: ① TSV 헤더 파서(최우선) → ② Gemini 3.5 Flash → ③ 정규식 폴백
- **TSV 헤더 파서 신규**: 엑셀/TSV 헤더에서 `받으시는 분`/`운송장번호`/`품목명` 열 자동 감지, 주소 열 제외 로직
- **품목별 행 매칭**: `nameItemRowMap`으로 같은(이름+품목) 송장번호는 같은 행에 append
- **수량 기반 배분**: 품목명 없을 때 `수량` 열 자동 감지, 수량만큼 송장 배정 후 다음 행 이동
- **Gemini 환각 방지**: 프롬프트에서 예시 이름 제거, `=== 데이터 시작 ===` 구분선 추가
- **모델**: 모든 Gemini 호출을 `gemini-3.5-flash`로 통일
- **UI**: `📊 TSV` / `🤖 AI` / `📝 정규식` 파싱 방식 표시

## 2026-07-06 09:02 — 판매현황 갱신 P열 완료 문구 변경
- **대상**: `_partnerOrders.gs`
- **내용**: 판매현황 갱신 시 허브 P열에 기록되는 문구를 "이카운트 업 완료" → "판매갱신 업 완료"로 변경
- **헤더**: "이카운트 업 완료 여부" → "판매갱신 업 완료 여부"
- **하위 호환**: 기존 "이카운트 업 완료", "판매현황 업 완료" 값도 완료로 인식하여 중복 수집 방지


## 2026-06-23 22:00 탭 조회 성능 최적화 완료
- **대상**: `_partnerOrders.gs`, `_partnerExclusivePush.gs`
- **내용**: 발주 수집, 송장 배포, 대리공급 Push 시 getSheetByName 다이렉트 호출 및 폴백 구조 적용 (스킵 방지 검증 완료)

## 2026-06-23 18:45 InvoiceMatchWebApp 구글 푸시 완료
- **대상**: `InvoiceMatchWebApp` 내 2개 파일 (`Code.gs`, `appsscript.json`)
- **내용**: 로컬 수정 버전 구글 서버 업로드 완료 (CS_WebApp은 변경 없어 스킵됨)

## 2026-06-23 18:38 메뉴 개편 오류 수정 및 배포 완료

### 상세 내용
- **`_partnerMenu.gs`**:
  - 124행~172행 사이의 깨지고 중복된 구문(Conflict 찌꺼기)을 완전히 제거하여 Syntax Error 해결
  - `⚙️ 자동화 설정` 메뉴에 통합 트리거 설치와 함께 개별 레거시 설정을 서브메뉴로 포함시켜 복구
  - `⏰ 통합 일일마감 (20시)` 최상위 서브메뉴 추가 완료
  - 로컬 구문 오류 검증(Node.js) 통과 및 `clasp push`를 통한 성공적 배포

# 변경이력

## 2026-06-23 17:50 — 판매현황_단가맵 제거 → 세트분리 시트 판매현황 직접 읽기

### 변경 내용
- **`_partnerExclusivePush.gs`**:
  - `_pep_archiveUnifiedDaily_`: 단가맵 로드 → 세트분리 시트 판매현황 직접 읽기로 전환
  - 사방넷번호/고유ID 기반 0순위 매칭 키(`ID:`) 추가
  - `_clearSalesPriceMap_()` 호출 제거
  - Push 함수에서 `_pep_schedulePriceMapCollection_()` 호출 제거
- **`_partnerWebApp.gs`**:
  - `partnerCollectPriceMapManual`: 안내 메시지로 대체
  - `_trigger_collectPriceMap_`: 빈 함수로 대체
  - `_ALL_SCHEDULED_TRIGGERS_`: 15:10 항목 제거 → **트리거 1개 절약**
- **`_partnerMenu.gs`**:
  - "💰 판매현황_단가맵 수동 수집" 메뉴 항목 제거
- 판매현황_단가맵 탭 자체는 유지 (안정 확인 후 수동 삭제 권장)

## 2026-06-23 17:35 — 판매현황_단가맵에 로젠 송장번호 자동 매칭 기능 추가

### 변경 내용
- `_PEP_PRICE_MAP_HEADERS_` 9열→10열 확장: `운송장번호` 열 추가 (J열)
- `_pep_appendSalesPriceMap_`: 새 행 push 시 10번째 열 빈값 추가
- `_pep_matchInvoiceToPriceMap_()` 신규 함수 추가:
  - **1차 매칭**: 단가맵 D열(수취인 "홍길동/사방넷번호")에서 "/" 뒤 ID 추출 → 로젠 E열(주문번호) 매칭
  - **1차 보조**: 단가맵 A열(주문번호)로도 로젠 E열 매칭 시도
  - **2차 매칭**: 이름(D열 "/" 앞) + 전화앞7자리(E열) + 품목명(G열) → 로젠 수취인+전화+품목명 매칭
  - 배치 처리(getValues→매칭→setValues)로 API 호출 최소화
- `_pep_archiveUnifiedDaily_`: 일일마감 실행 시 단가맵 로드 전에 송장매칭 자동 호출

## 2026-06-23 17:18 — 복구시스템 다이얼로그 설명 디테일 강화

### 변경 내용
- `repairDialog.html`의 4개 복구 카테고리(수식·단가 / 헤더·양식 / 스크립트 / 탭 재생성) 설명을 실제 코드 로직 기반으로 디테일하게 수정.
- 각 카테고리에 "이럴 때 사용" + "🔧 수행 작업" 두 섹션으로 분리하여, 사용자가 증상과 실제 복구 내용을 모두 파악 가능하도록 개선.
- `.detail-section` / `.detail-label` CSS 스타일 추가 (점선 경계 + 녹색 라벨).

## 2026-06-23 16:58 — 뷰어(단가조회) 탭 수식 종합 진단/복구 기능 추가 및 백필 버그 수정해결

### 문제 상황
- **모든 업체** 시트의 단가조회 탭 3행이 전부 `#ERROR!` — C열에 코드 입력해도 D(품목명), G(최종단가) 등 전혀 연동 안 됨
- **근본 원인**: `_pt_applyRow3Formulas` 함수의 A3, B3, D3, E3, F3, G3, I3 수식에서 **XLOOKUP을 닫는 `)` 1개가 누락**되어 ARRAYFORMULA가 닫히지 않는 구문 오류
- 이전 수정(16:50)에서 괄호 방향만 조정했으나 **개수 자체가 여전히 1개 부족**했음

### 원인 분석
```
[잘못된 수식] X:X"), "-")))   ← 닫기 5개 / 열기 6개 (1개 부족!)
[올바른 수식] X:X")), "-")))  ← 닫기 6개 / 열기 6개 ✅
```
- `XLOOKUP()`을 먼저 닫아야(`))`) IFNA의 2번째 인수(`"-"`)로 넘어감
- J3만 `')),"- ")` 패턴으로 이미 올바르게 되어있어 정상 작동

### 수정 내역
- `_partnerDeploy.gs` `_pt_applyRow3Formulas` 함수: A3, B3, D3, E3, F3, G3(non-consumer), I3 수식에 `)` 1개씩 추가

### 배포 후 실행 필요
- `💼 협력업체 관리 > 📊 상태 확인/진단 > 🔧 뷰어 3행 수식 일괄 복구` 실행



### 문제 상황
- 모든 업체 시트에서 단가조회 탭 C열에 이카운트 코드를 입력해도 D(품목명), G(최종단가) 등 나머지 열이 비어있는 현상 발생.
- **원인 1**: 3행의 ARRAYFORMULA 수식이 깨지거나, 스필 영역이 차단되어 수식 확장 불가.
- **원인 2**: `_partnerMenu.gs` 복구 배치의 D/L 백필 로직이 뷰어 데이터를 4행부터 읽어 3행 데이터 누락.

### 수정 내역

#### `_partnerMaintenance.gs` — 신규 함수 2개 추가
- `partnerDiagnoseViewerRow3Formulas()`: 모든 업체의 뷰어 탭 3행 ARRAYFORMULA 수식 종합 진단
  - 수식 존재 여부, #REF!/Loading 에러, 스필 차단 여부, 코드O/품목X 행 카운트
- `partnerRepairAllViewerRow3Formulas()`: 뷰어 3행 수식 일괄 재적용 (C열 보존, 스필 영역 정리)

#### `_partnerMenu.gs` — 메뉴 항목 추가 + 백필 버그 수정
- 📊 상태 확인/진단 메뉴에 `🔍 뷰어 3행 수식 종합 진단` / `🔧 뷰어 3행 수식 일괄 복구` 2개 항목 추가
- `_repairBatch_formula_` 함수의 D/L 백필 로직: `vTab.getRange(4, 1, ...)` → `vTab.getRange(3, 1, ...)` 수정 (뷰어 데이터는 3행부터 시작)

## 2026-06-23 16:50 — 단가조회(뷰어) 탭 ARRAYFORMULA 수식 괄호 짝 오류 해결 및 복구

### 문제 상황
- 협력업체 시트의 `단가조회` 탭 C열에 새로운 이카운트 코드를 입력해도 상태, 품목명, 최종단가 등 다른 열에 데이터가 정상 연동되지 않는 현상 발생.
- **원인**: `_partnerDeploy.gs`의 `_pt_applyRow3Formulas` 함수 내 연동 수식들에서 닫는 괄호 `))`가 일찍 닫히면서 `IFNA`의 실패시값 `"-"`이 `IF` 함수의 4번째 인수로 꼬여 구문 에러를 일으킴.

### 수정 내역
- 괄호 구조를 `A:A"), "-")))'` 형태로 안전하게 수정하여 구문 오류 원천 차단.
- 대상 파일: `_partnerDeploy.gs` (`_pt_applyRow3Formulas` 함수)

## 2026-06-23 16:38 — 송장 매칭 정확도(정밀도) 대폭 개선 및 오매칭 방지 로직 보완

### 개선 사유
- 과거 Drive OCR의 오타율 때문에 이름 매칭 범위가 너무 넓게(3글자 중 2글자 오타도 허용 등) 설정되어 있어, 비스무리한 이름이나 2글자 이름이 오매칭되는 문제가 있었습니다.
- Gemini Vision OCR 전환으로 텍스트 추출 정확도가 극대화되었으므로, 매칭 정밀도를 대폭 상향했습니다.

### 수정 내역
- **유사도 매칭(Levenshtein) 임계치 세분화**:
  - 이름 길이 2자 이하: 오타 허용 0개 (완전 일치만 매칭)
  - 이름 길이 3~5자: 오타 허용 최대 1개
  - 이름 길이 6자 이상: 오타 허용 최대 2개
- **부분 일치(indexOf) 조건 강화**:
  - 양쪽 이름 모두 최소 3자 이상이고, 글자 수 차이가 1자 이하일 때만 부분 매칭 인정.
- **대상 파일**:
  - `_partnerWebApp.gs` (`_imParseAndMatch_` 함수)
  - `InvoiceMatch_협력업체용.gs` (`parseAndMatchInvoiceTextLocal` 함수)

## 2026-06-23 16:35 — [최종 완료] 중앙 송장매칭 사이드바 fetch 호출 제거 및 google.script.run 프록시 롤백 적용

### 문제 상황
- 이전 변경이력(16:30)에 프록시 아키텍처를 도입했음에도 불구하고, 실제 `invoiceMatchSidebar.html` 내부에 `callWebApp()` (fetch 방식) 코드가 잔존하고 있어 여전히 브라우저 CORS 차단으로 인한 무한 로딩이 풀리지 않는 현상이 지속됨.

### 해결 조치
- `invoiceMatchSidebar.html` 파일 내부에서 `callWebApp` (직접 fetch) 함수와 hidden input을 완전히 제거.
- `loadVendorList()`, `analyze()`, `applyAll()` 함수를 Apps Script 서버 함수(`getPartnerFileListForSidebar`, `parseAndMatchInvoiceText`, `applyInvoiceMatches`)로 전달하는 `google.script.run` 호출 방식으로 원복 완료.
- `openInvoiceMatchSidebar` 실행 시 Properties에 저장된 `INVOICE_MATCH_WEB_APP_URL`을 강제로 최신 활성 웹앱 URL로 동기화하도록 보완.

## 2026-06-23 16:30 — 중앙 송장매칭 사이드바 Server-to-Server 프록시 아키텍처 적용으로 CORS 무한 로딩 및 타 사용자 권한 오류 최종결

### 문제 상황
- 클라이언트 HTML에서 웹앱 URL을 fetch할 때 브라우저 수준의 CORS 차단으로 무한 로딩이 풀리지 않는 현상.

### 해결 조치 (기획서 작성)
- [20260623_ServerToServer프록시해결플랜.md](file:///C:/Users/x299/.gemini/antigravity-ide/brain/2f92a10b-fce0-4820-9281-7118f3240af8/20260623_ServerToServer%ED%94%84%EB%A1%9D%EC%8B%9C%ED%95%B4%EA%B2%B0%ED%94%8C%EB%9E%9C.md) 기획서 작성.
- 브라우저 클라이언트(`invoiceMatchSidebar.html`)는 `google.script.run` 방식으로 완전 롤백하여 CORS 차단을 원천 차단.
- 서버사이드 GAS(`_partnerExclusivePush.gs`)의 `getPartnerFileListForSidebar`, `parseAndMatchInvoiceText`, `applyInvoiceMatches` 함수를 **웹앱 URL로 `UrlFetchApp.fetch`를 수행해 통신을 중계하는 프록시(Proxy) 함수**로 전면 개정.

## 2026-06-23 16:25 — 중앙 송장매칭 사이드바 웹앱 API 호출 CORS 무한 로딩 오류 해결 방안 수립

### 문제 상황
- 송장 매칭 통신을 웹앱으로 프록시화한 후, 사이드바를 열었을 때 업체 목록이 로딩 상태로 무한 정체되는 현상.
- **원인**:
  1. `fetch` 호출 시 Content-Type을 지정하지 않거나 JSON으로 명시하여 브라우저가 Preflight OPTIONS 사전 조회를 날림. 구글 Apps Script 서버는 OPTIONS 응답을 처리하지 못해 CORS 에러 발생.
  2. 구글 웹앱 고유의 302 리다이렉트 응답을 따라가는 `redirect: "follow"` 옵션이 누락됨.

### 해결 조치 (기획서 작성)
- [20260623_CORS무한로딩해결플랜.md](file:///C:/Users/x299/.gemini/antigravity-ide/brain/2f92a10b-fce0-4820-9281-7118f3240af8/20260623_CORS%EB%AC%B4%ED%95%9C%EB%A1%9C%EB%94%A9%ED%95%B4%EA%B2%B0%ED%94%8C%EB%9E%9C.md) 기획서 작성.
- `invoiceMatchSidebar.html`의 `callWebApp()` 함수 내부 `fetch` 옵션에 `headers: { "Content-Type": "text/plain;charset=utf-8" }`를 설정하여 Simple Request로 Preflight를 회피하고, `redirect: "follow"` 설정을 강제 부여.

## 2026-06-23 16:10 — 냅킨코리아 Gmail 송장수집 트리거 및 알림 오류 원인 규명 및 해결 방안 수립

### 문제 상황
- 냅킨코리아 Gmail 송장 수집 기능이 트리거 스케줄대로 작동하지 않고 Chat 알림도 울리지 않음.
- **원인**:
  1. 트리거 실행 시간(15:20)과 함수 내부의 필터링 시간 윈도우(15:30 ~ 16:30) 불일치로 인해 트리거가 실행되었으나 즉시 스킵(return)됨.
  2. 다른 트리거와 달리 `_chat_sendCard_` 알림을 쏘는 Silent 래퍼가 없었음.

### 해결 조치 (기획서 작성)
- [20260623_냅킨코리아지메일수집트리거수정플랜.md](file:///C:/Users/x299/.gemini/antigravity-ide/brain/2f92a10b-fce0-4820-9281-7118f3240af8/20260623_%EB%83%85%ED%82%A8%EC%BD%94%EB%A6%AC%EC%95%84%EC%A7%80%EB%A9%94%EC%9D%BC%EC%88%98%EC%A7%91%ED%8A%B8%EB%A6%AC%EA%B1%B0%EC%88%98%EC%A0%95%ED%94%8C%EB%9E%9C.md) 기획서 작성.
- `_partnerWebApp.gs`에 `_trigger_NK_Gmail_()` Silent 래퍼 추가하여 작업 완료/에러 시 Chat 알림이 정상 전송되도록 조치.
- 트리거 설정 시간을 시간 윈도우 영역 안인 오후 3시 40분(`15:40`)으로 조정 및 핸들러 등록 변경.

## 2026-06-23 15:50 — 중앙 송장매칭 권한 및 이중 로그인 세션 충돌 해결 방안 수립

### 문제 상황
- 허브 시트의 `📬 송장 매칭` 사이드바를 일반 직원 계정이나 이중 로그인 세션 환경에서 실행 시 `google.script.run` 호출 권한 문제(401/403) 및 외부 업체 시트에 대한 openById 권한 부족 에러 발생.

### 해결 조치 (기획서 작성)
- [20260623_중앙송장매칭권한해결플랜.md](file:///C:/Users/x299/.gemini/antigravity-ide/brain/2f92a10b-fce0-4820-9281-7118f3240af8/20260623_%EC%A4%91%EC%95%99%EC%86%A1%EC%9E%A5%EB%A7%A4%EC%B9%AD%EA%B6%8C%ED%95%9C%ED%95%B4%EA%B2%B0%ED%94%8C%EB%9E%9C.md) 기획서 작성.
- 허브 시트의 사이드바 통신을 `google.script.run`에서 **웹앱 API (소유자 권한 실행)** 호출 방식으로 전환 설계.
- `doPost(e)`에 `action: "list"`를 신설하여 업체 목록 로딩도 권한 에러 없이 소유자 권한으로 한 번에 수집되도록 최적화.

## 2026-06-23 14:40 — 업체 시트 송장매칭 프로젝트 바인딩 오류 원인 진단 및 해결 방안 수립

### 문제 상황
- "스크립트 재설치"를 수십 번 반복하고 에디터에서 `InvoiceMatch.gs`와 `InvoiceMatchSidebar.html`을 삭제해도 재설치 결과가 여전히 똑같고, 사이드바 분석 버튼이 작동하지 않는 현상.
- **근본 원인**: 구글 스프레드시트 바운드 프로젝트의 꼬임 현상. API가 업데이트하는 백그라운드 스크립트 프로젝트 ID와 실제 사용자가 스프레드시트에서 열어 실행하는 프로젝트 ID가 달라 주입된 코드가 정상 반영되지 않고 있었음.

### 해결 조치 (기획서 작성)
- [20260623_송장매칭오류해결방안.md](file:///C:/Users/x299/.gemini/antigravity-ide/brain/2f92a10b-fce0-4820-9281-7118f3240af8/20260623_%EC%86%A1%EC%9E%A5%EB%A7%A4%EC%B9%AD%EC%98%A4%EB%A5%98%ED%95%B4%EA%B2%B0%EB%B0%A9%EC%95%88.md) 및 [20260623_중복스크립트자동정리플랜.md](file:///C:/Users/x299/.gemini/antigravity-ide/brain/2f92a10b-fce0-4820-9281-7118f3240af8/20260623_%EC%A4%91%EB%B3%B5%EC%8A%A4%ED%81%AC%EB%A6%BD%ED%8A%B8%EC%9E%90%EB%8F%99%EC%A0%95%EB%A6%AC%ED%94%8C%EB%9E%9C.md) 기획서 작성.
- [1단계] 허브 시트 Properties의 해당 업체 스크립트 ID 캐시를 초기화(`adminClearSingleVendorScriptId` 실행).
- [2단계] 업체 시트의 Apps Script 에디터에 수동 생성되어 충돌을 일으키는 프로젝트("제목 없는 프로젝트" 등) 삭제.
- [3단계] "스크립트 재설치"를 실행하여 완전히 깨끗한 새 스크립트 컨테이너 생성 및 코드 주입 유도.
- **추가 최적화**: `priceManager.gs`에 `cleanDuplicateViewerScripts_` 함수를 추가하여, 스크립트 재설치 시 동일한 스프레드시트에 연결된 옛날 구버전 프로젝트(Pack2U 공지팝업/송장매칭)들을 드라이브 API 수준에서 자동으로 휴지통으로 청소하고 가장 최근 것 하나만 복구하도록 개선. (이제 사용자가 일일이 수동으로 프로젝트를 지우지 않아도 자동 정리됩니다.)

## 2026-06-23 14:28 — 업체 시트 송장매칭 v10 (서버 사이드 전환)

### 문제
- v9(.toString() 인라인 JS)로 사이드바는 열리지만 분석 버튼 클릭 시 무반응
- 원인: _getInvoiceMatchHtml_()의 200줄 인라인 JS가 .toString() 직렬화 시 이스케이프 깨짐
- 특히 정규식 패턴(\\s, \\d 등)과 HTML 태그 혼합이 직렬화 불안정

### 수정 (v10)
- **접근 방식 완전 변경**: 클라이언트 JS → 서버 사이드 분석
- `openInvoiceMatchSidebarLocal`: 단순화 (HTML만 생성)
- `_getInvoiceMatchHtmlSimple_`: 새 HTML 생성기 (JS 최소화)
  - 분석: `google.script.run.parseAndMatchInvoiceTextLocal(text)` 
  - 반영: `google.script.run.applyInvoiceMatchesLocal(json)`
  - 복잡한 파서/매처 JS 완전 제거 (서버에서 실행)
- `getPartnerInvoiceMatchCode_` v10: 6개 함수 주입
  - openInvoiceMatchSidebarLocal, _getInvoiceMatchHtmlSimple_
  - parseAndMatchInvoiceTextLocal, applyInvoiceMatchesLocal
  - _parseInvoicePairs_, _levenshteinLocal_
- `createViewerNoticeScript_`: InvoiceMatchSidebar.html 주입 제거

### 적용 방법
- 허브에서 `🛠 협력업체 스크립트 재설치` 실행

### 기타 변경
- 트리거 중복 정리 (SpillGuard 10→1개, 총 29→20개)
- 전체 Silent 래퍼에 Chat 알림 추가 (성공/에러)

## 2026-06-23 13:27 — 업체 시트 송장매칭 분석 버튼 수정 (v9)

### 문제
- 업체 시트에서 📬 송장 매칭 사이드바의 🔍 분석 버튼 클릭 시 아무 반응 없음
- 원인: 주입 코드가 `createHtmlOutputFromFile("InvoiceMatchSidebar")`로 허브 전용 HTML(업체 선택 드롭다운 + 서버 호출 방식)을 참조
- 업체 시트에는 `parseAndMatchInvoiceText`, `getPartnerFileListForSidebar` 서버 함수가 없어 호출 실패

### 수정
- `getPartnerInvoiceMatchCode_()` v8 → v9 변경
- 문자열 수동 생성 방식 → `.toString()` 자동 주입 방식
  - `openInvoiceMatchSidebarLocal.toString()` — 인라인 HTML 방식 (createHtmlOutputFromFile 불필요)
  - `_getInvoiceMatchHtml_.toString()` — 인라인 HTML 생성기
  - `_getOcrApiKey_.toString()` — OCR API 키 헬퍼
  - `applyInvoiceMatchesLocal.toString()` — 반영 서버 함수
- 분석: 100% 브라우저 로컬 파싱 (서버 호출 없음)
- 반영: `google.script.run.applyInvoiceMatchesLocal()` 최소 서버 호출

### 적용 방법
- 허브에서 `💼 협력업체 관리 > 🔧 관리/AS 도구 > 스크립트 재설치` 실행 필요

## 2026-06-23 11:55 — 독립배포 시스템 레거시 코드 정리

### 삭제된 문서 (22개)
- SYSTEM_SUMMARY_2026-04-20.md (.docx 포함)
- 독립배포_월마감_분석메모_20260420.md (.docx 포함)
- 배포시트_자동완성코드_복사용.txt (.docx 포함)
- WEB_MIGRATION_01~05.md (.docx 포함, 10파일)
- system_architecture_diagram.md + system_architecture_memo.md (.docx 포함)
- ECOUNT_WORKLOG_2026-04-20.md (.docx 포함)

### debug.gs 코드 정리
- 삭제: `forceSetCorrectHubId`, `forceReplaceAllImportRangeIds`, `showCurrentHubId`, `diagnoseOrderCollection`, `diagnoseMappingSheet`
- 유지: `emergencyRepairAllDeploySheets`, `fixK2Only` (단가조회 복구용), `diagnosePtListFiles`, 스크립트 관리 함수들

### menu.gs 정리
- 독립배포 관련 주석/에러메시지 제거
- 기능 변경 없음

### 백업
- `backup_20260623_pre_cleanup/` 폴더에 작업 전 전체 백업 (95파일)

## 2026-06-23 09:41 — 05시 트리거 수정: 통합 일일마감으로 변경

- 05:00 트리거: `_trigger_dailyArchiveAll_` → `_pep_unifiedDailyArchiveScheduled_` (통합 일일마감)
- 20:00 통합 일일마감 트리거 제거 (05시로 통합)
- 총 트리거 수: 19개 → 18개

## 2026-06-23 09:30 — 통합 자동 트리거 설정 시스템 추가

### 변경 파일
- `_partnerWebApp.gs`: 통합 트리거 설정/제거/상태확인 함수 + Silent 래퍼 함수 10개 추가
- `_partnerMenu.gs`: 자동화 설정 메뉴 구조 변경 (통합 트리거 메뉴 최상단 배치)

### 상세 내용

#### 새 함수 (Silent 래퍼 — 트리거에서 UI 없이 자동 실행)
- `_trigger_dailyArchiveAll_()` — 일일 전체마감 (05:00)
- `_trigger_monthlySettle_()` — 대리판매 마감 (07:00)
- `_trigger_exclusiveArchive_()` — 대리발주 마감 (07:20)
- `_trigger_islandShipping_()` — 도서산간 추가배송비 (09:15 / 14:20)
- `_trigger_fillZipAndShipping_()` — 우편번호/택배비 채우기 (09:25 / 14:40)
- `_trigger_collectPriceMap_()` — 판매현황_단가맵 수집 (15:10)
- `_trigger_fetchInvoices_()` — 송장 수집 (15:50)
- `_trigger_pushInvoices_()` — 송장 배포 (16:30)

#### 통합 관리 함수
- `setupAllScheduledTriggers()` — 19개 트리거 일괄 설치 (기존 시간 트리거 전체 교체)
- `removeAllScheduledTriggers()` — 시간 기반 트리거 전체 제거
- `showAllScheduledTriggerStatus()` — 설치 상태 대시보드

#### 스케줄 (19개 트리거)
| 시간 | 작업 | 핸들러 |
|---|---|---|
| 05:00 | 일일 전체마감 | `_trigger_dailyArchiveAll_` |
| 06:00 | 이카운트 전체동기화 1 | `runDailyEcountBatch` |
| 07:00 | 대리판매 마감 | `_trigger_monthlySettle_` |
| 07:20 | 대리발주 마감 | `_trigger_exclusiveArchive_` |
| 09:00 | 발주 수집 (1회전) | `partnerCollectOrdersSilent_` |
| 09:15 | 도서산간 (1회전) | `_trigger_islandShipping_` |
| 09:20 | 대리공급 Push (1회전) | `partnerPushOrdersToExclusiveFormsSilent_` |
| 09:25 | 우편번호 채우기 (1회전) | `_trigger_fillZipAndShipping_` |
| 12:00 | 이카운트 전체동기화 2 | `runDailyEcountBatch` |
| 12:50 | 통합허브 상태/재고 | `runDailyHubBatch` |
| 14:05 | 발주 수집 (2회전) | `partnerCollectOrdersSilent_` |
| 14:20 | 도서산간 (2회전) | `_trigger_islandShipping_` |
| 14:30 | 대리공급 Push (2회전) | `partnerPushOrdersToExclusiveFormsSilent_` |
| 14:40 | 우편번호 채우기 (2회전) | `_trigger_fillZipAndShipping_` |
| 15:10 | 판매현황 전체 임시기록 | `_trigger_collectPriceMap_` |
| 15:20 | 냅킨코리아 Gmail 송장 | `_gmi_triggerFetchNKInvoice_` |
| 15:50 | 송장 수집 | `_trigger_fetchInvoices_` |
| 16:30 | 송장 배포 | `_trigger_pushInvoices_` |
| 20:00 | 통합 일일마감 | `_pep_unifiedDailyArchiveScheduled_` |

#### 메뉴 구조 변경
```
⚙️ 자동화 설정
├── ⏰ 통합 자동 트리거 설치 (전체)    ← NEW (메인)
├── ⏸ 통합 자동 트리거 제거 (전체)    ← NEW
├── 📋 자동 트리거 상태 확인           ← NEW
└── 📂 개별 설정 (레거시)             ← 기존 개별 메뉴 이동
    ├── 발주 수집 자동 (5분)
    ├── 대리발주 자동 Push
    ├── 냅킨코리아 Gmail 트리거
    ├── 대시보드 자동갱신
    └── 폐기송장 자동조회
```

#### 주요 설계 결정
- GAS 트리거 20개 제한: 19개 시간 트리거 + onEdit 이벤트 = 20개 이내
- `_removeAllTimeTriggers_()`: CLOCK 타입만 삭제 (ON_EDIT, ON_OPEN 보존)
- 기존 5분 간격 수집 트리거는 통합 설치 시 제거됨 → 시간표 기반으로 대체
- Silent 래퍼: 에러 시 Logger + Chat 알림, UI alert 없음
- `partnerDailyArchiveAll`은 내부에 UI 확인 팝업 있으나, 트리거에서는 `ui=null`로 자동 스킵됨
