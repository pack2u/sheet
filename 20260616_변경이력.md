# 변경이력

## 2026-06-22 20:10 — 송장매칭: 로컬 하이브리드(클라이언트 사이드 연산) 전환

- **목적**: 구글 샌드박스의 CORS 차단 및 다중 계정 로그인 충돌 이슈를 원천 차단하기 위해, 송장 파싱 및 매칭 연산을 100% 브라우저(클라이언트) 자바스크립트에서 실시간 처리하는 하이브리드 방식으로 개편
- **변경 사항**:
  - `InvoiceMatch_협력업체용.gs` (`openInvoiceMatchSidebarLocal`): 사이드바 로드 시 시트의 수취인 데이터(`nameToRows`)를 추출하여 HTML 템플릿에 `__NAME_TO_ROWS_JSON__`으로 동적 삽입
  - `InvoiceMatch_협력업체용.gs` (`_getInvoiceMatchHtml_`): 
    - `_callWebApp` 서버 통신 제거
    - 클라이언트 사이드 매칭용 `_levenshteinLocal_`, `_parseInvoicePairs_`, `analyze()` 로컬 매칭 자바스크립트 이식
    - "전용양식에 반영" 클릭 시, 웹앱 대신 로컬 바운드 스크립트 함수인 `applyInvoiceMatchesLocal`를 직접 호출
  - `priceManager.gs` (`createViewerNoticeScript_`): 불필요해진 `__WEB_APP_URL__` 치환 및 조회 코드 제거
- **후속 조치**: `clasp push` 완료 후 협력업체 시트에 코드 업데이트 일괄 배포 필요 (`updateAllVendorScripts()` 실행)

## 2026-06-22 18:24 — 송장매칭: Web App fetch 전환 오류 수정 및 CORS Preflight 방지

- **문제**: 카카오 송장 매칭 실행 시 "분석" 버튼이 반응하지 않고 비동기 응답 채널 닫힘 에러가 발생함
- **원인**: 
  - `_getInvoiceMatchHtml_()`의 `_callWebApp` 함수 내부에 예전 `google.script.run` 코드가 남아 있었음
  - `priceManager.gs`의 `createViewerNoticeScript_()` 함수 내에서 `__WEB_APP_URL__` 치환 대상을 빈 문자열(`''`)로 설정하여 배포 중이었음 (웹앱 URL 누락)
- **수정**:
  - `InvoiceMatch_협력업체용.gs` (`_getInvoiceMatchHtml_`): `_callWebApp` 함수를 Web App fetch 방식으로 전면 전환하고, Preflight OPTIONS 요청 방지를 위해 `Content-Type: text/plain` 헤더 사용
  - `priceManager.gs` (`createViewerNoticeScript_`): `__WEB_APP_URL__` 치환 대상을 실제 `_getWebAppUrl_()` 결과로 변경하여 웹앱 URL이 누락되지 않도록 함
- **후속 조치**: clasp push 완료. 이후 협력업체 시트에 코드 업데이트 일괄 배포 필요

## 2026-06-22 15:10 — 송장매칭: Gemini AI 매칭 도입

- **목적**: 규칙/순서 없는 자유형식 카카오 메시지도 정확히 파싱+매칭
- **변경**: 기존 정규식+5단계 알고리즘 → Gemini API가 텍스트 이해 + 수취인 목록과 매칭
- **구조**: 사이드바 → Web App → Gemini API → 시트 데이터 매칭
- **폴백**: Gemini 실패 시 기존 규칙 기반으로 자동 전환
- **수정 파일**: `InvoiceMatchWebApp/Code.gs`

## 2026-06-22 14:40 — 송장매칭: 허브 Web App 전환 (google.script.run 제거)

- **목적**: 권한 승인 불필요 + 다중 접속 안전 + 스크립트 재설치 최소화
- **구조 변경**:
  - `사이드바 → google.script.run → 업체 바운드 스크립트` (기존)
  - `사이드바 → fetch(POST) → 허브 doPost Web App` (변경)
- **수정 파일**:
  - `_partnerWebApp.gs`: `doPost()` + `_imParseAndMatch_()` + `_imApplyMatches_()` + 보안 검증 추가
  - `InvoiceMatch_협력업체용.gs`: analyze/applyAll을 `fetch()` 방식으로 전환, 주입 코드 7개→4개 함수로 간소화
- **후속 조치**: GAS 에디터에서 웹앱 배포 → URL 저장 → 스크립트 재설치

## 2026-06-22 13:44 — 송장매칭: 분석 버튼 무반응 근본 원인 수정

- **문제**: 업체 시트에서 분석 버튼 클릭 시 아무 반응 없음
- **원인**: `_getInvoiceMatchHtml_()` 내 백틱 3개(```)가 `.toString()` 직렬화 → 업체 스크립트 주입 시 **템플릿 리터럴로 오해석** → 인라인 `<script>` 전체 파싱 실패 → `analyze()` 함수 미정의
- **수정** (`InvoiceMatch_협력업체용.gs` L633-634):
  - `/\`\`\`[a-z]*/gi` 정규식을 `String.fromCharCode(96,96,96)` + `new RegExp()` 방식으로 대체
  - 백틱을 소스 코드에 직접 쓰지 않아 직렬화 안전
- **후속 조치**: `clasp push` + 업체별 스크립트 재설치 필요

## 2026-06-22 13:14 — 복구시스템: 업체 목록 빈 캐시 버그 수정

- **문제**: 복구시스템에서 "업체 없음" (0/0개 선택) 표시
- **원인**: `_pt_listFiles()` DriveApp 폴더 접근 실패 시 빈 배열이 CacheService에 5분 캐시됨
- **수정** (`_partnerDeploy.gs`):
  - 빈 배열은 CacheService에 캐시하지 않음
  - CacheService에서 빈 배열 캐시 읽어도 무시 후 DriveApp 재시도
  - DriveApp 접근 실패 시 에러 로깅 추가 (무음 삼킴 방지)
  - 이전 빈 캐시 자동 제거

## 2026-06-22 13:10 — 송장매칭 사이드바: 분석 버튼 에러핸들링 강화

- **문제**: 협력업체 시트에서 송장매칭 사이드바가 열리지만 분석 버튼이 작동하지 않음
- **원인**: `google.script.run` 호출 시 try/catch 미적용 → 서버 연결 실패 시 버튼 disabled 상태 고정
- **수정** (`InvoiceMatch_협력업체용.gs`):
  - `analyze()`, `applyAll()` 함수에 try/catch 추가
  - 페이지 로드 시 `google.script.run` 연결 가능 여부 자동 진단
  - 서버 연결 불가 시 "⚠️ 권한 승인 필요" 안내 표시
- **조치**: clasp push 후 **전체 스크립트 재설치** 필요


- **문제**: 소비자가(상품정보 탭)와 비교하여 모든 업체 주문이 단가 불일치로 표시됨
- **수정**: 전체 그룹 단가표의 모든 그룹별 가격과 비교하도록 변경
  - `_oa_loadGroupPrices_()` 신규 함수: 전체 그룹 단가표에서 G열부터 5열 간격의 그룹 가격 + F열(소비자가) 수집
  - `_oa_checkPriceMismatch_()`: 허브 정산금액이 어떤 그룹 가격에도 매칭되지 않을 때만 경고 (5% 이상 차이)
- **파일**: `_partnerOrderAudit.gs`

## 2026-06-22 12:30 — ★ 주문 검증 AI 시스템 신규 구축

### 개요
- 주문/발주 데이터를 자동 교차 검증하여 오류 조기 발견
- 규칙 기반 자동 검증 + Gemini AI 분석 챗봇 하이브리드

### 신규 파일
- `_partnerOrderAudit.gs` — 검증 엔진 (10가지 핵심 검증 항목)
- `orderAuditSidebar.html` — AI 사이드바 UI (다크 모드, 대시보드, 챗봇)

### 수정 파일
- `_partnerMenu.gs` — 🤖 주문 검증 AI 메뉴 추가
- `appsscript.json` — cloud-platform 스코프 추가 (이카운트 프록시용)

### 검증 항목 (10가지)
1. 송장 미배정 (🔴긴급)
2. 송장 중복 (🔴긴급)
3. 주문번호 중복 (🟡주의)
4. 단가 불일치 (🟡주의)
5. 수량 이상 (🟡주의)
6. 존재하지 않는 품목코드 (🟡주의)
7. 취소/반품 미처리 (🔴긴급)
8. 마감 누락 (🟡주의)
9. 상태 이상 — 코드오류/품절 장기 방치 (🟠경고)
10. 도서산간 배송비 누락 (🟠경고)

## 2026-06-22 11:26 — ★ 이카운트 OAPI 고정 IP 프록시 구축 (최종 완료)

### 문제
- GAS 서버 IP가 매번 변동 → 이카운트 IP 화이트리스트 차단
- 이카운트 정책상 IP 제한 해제 불가

### 해결: Google Cloud Function 프록시
- **구조**: GAS → Cloud Function(고정 IP `34.64.87.69`) → 이카운트 OAPI
- **GCP 프로젝트**: `pack2u-proxy` (서울 리전 asia-northeast3)
- **인프라**: 고정 IP + Cloud Router + Cloud NAT + VPC Connector + Cloud Function Gen2

### 수정: `ecount.gs`
- `_ECOUNT_PROXY_URL_`, `_ECOUNT_PROXY_KEY_` 상수 추가
- `_ecountFetchViaProxy_()` 헬퍼 함수 추가 (프록시 경유 + OAuth 인증)
- `verifyZoneAPI()` → 프록시 경유로 변경
- `login()` → 프록시 경유로 변경
- SESSION_ID 기반 후속 API 호출은 직접 호출 유지 (IP 제한 없는 경우)

### 수정: `InvoiceMatch_협력업체용.gs`
- `_getOcrApiKey_()`: PropertiesService 제거 → 업체 시트 권한 문제 해결

### 수정: `partnerOnEditSource.gs`, `배포시트_자동완성코드_복사용.txt`
- `_protCols`: D(4)/L(12) 보호 추가, N(14) 보호 해제

---

## 2026-06-22 09:03 — D/L열 보호 추가 + N열 보호 해제

### 문제
- D열(품목명)과 L열(단가)을 수동 편집해도 차단/복구가 작동하지 않음
- N열(상태)은 즉시 차단되어 수동 편집 불가

### 수정: `partnerOnEditSource.gs`, `배포시트_자동완성코드_복사용.txt`
- `_protCols` 변경: `[1, 13, 14]` → `[1, 4, 12, 13]`
  - **추가**: D(4), L(12) — 수동 입력 시 즉시 clearContent + C열 기반 뷰어탭 값 자동 복구
  - **제거**: N(14) — 수동 편집 허용 (필수필드 완성도 자동갱신은 C/E~H열 편집 시 유지)

### 적용 방법
```
clasp push → 관리/AS 도구 → 📝 스크립트 재설치 → 전체 업체
```

---

## 2026-06-22 08:25 — ★ 전용양식 마감 이동 시 송장 미입력분 소실 방지 안전장치

### 문제
- 전용양식 → 전용발주 마감탭 이동 시 송장번호 미입력분이 사라지는 경우 발생
- 원인: `clearContent()` 후 `setValues()` 사이에 GAS 실행 중단(타임아웃/에러) 시 모든 데이터 소실

### 수정: `_partnerExclusiveArchive.gs` — 3단계 안전장치 적용

#### 영향 함수 2개 (동일 패턴 적용)
- `_pea_core_()` — 수동 메뉴 실행
- `_pea_processOneFile_()` — 일괄 마감 통합 루프에서 호출

#### 안전장치 상세
1. **마감탭 기록 검증**: `setValues` + `flush()` 후 `getLastRow()`로 실제 기록 행수 확인 → 불일치 시 원본 보존
2. **원본 삭제 try/catch 래핑**: `clearContent` + `setValues` 과정 전체를 감싸 에러 시 메모리의 원본 `data[]` 배열로 전체 복구
3. **잔류 행 복원 검증**: 남길 행을 다시 쓴 뒤 `getLastRow()`로 복원 행수 확인 → 부족 시 원본 전체 복구
4. **keepRowsData 선구성**: `clearContent` 전에 잔류 데이터를 미리 JavaScript 배열로 구성 (기존: clearContent 후 구성)

### 영향 없음
- 마감 탭 기록 로직 변경 없음
- 이동 판단 로직(날짜/송장번호 필터) 변경 없음
- 미리보기 `_pea_preview_()` 변경 없음

---

## 2026-06-21 19:40 — 이카운트 로그인 실패 에러 메시지 상세화

### 문제
- 이카운트 동기화 1단계에서 "로그인 실패 (키 만료 또는 IP 권한)" 에러만 표시
- 실제 API가 반환한 에러 원인(만료/권한/잘못된 키 등)을 알 수 없어 원인 파악 불가

### 수정: `ecount.gs`
1. **ecountStep1() 에러 메시지 개선** (128~140행)
   - API Status, Error.Message, 응답 구조 키 목록을 에러 메시지에 포함
   - "이카운트 ERP > OAPI설정 > API인증키 재발급" 안내 추가
2. **login() 함수 진단 강화** (444~480행)
   - HTTP 응답코드 추적 추가
   - 파싱 오류 시 서버 응답 원문 일부 포함 (에러 페이지 즉시 확인 가능)
   - HTTP 비정상 응답 시 Logger에 경고 로그 기록

### 영향 없음
- 로그인 성공 시 기존 동작 동일
- 다른 동기화 단계(2~4) 변경 없음

---

## 2026-06-20 10:40~10:55 — ★ 판매현황_단가맵 헤더 감지 수정 + 수동 수집 메뉴

### 문제 원인
- 판매현황 탭 구조: **1행=제목(회사명), 2행=헤더, 3행~=데이터**
- 코드는 1행을 헤더로 읽어서 모든 열 감지 실패
- 열 이름 불일치: `합계`(실제) ≠ `판매단가/판매가`(감지 키워드)
- `일자-No.`(실제) ≠ `주문번호`(감지 키워드)
- `주문자명(사방넷)`(실제) ≠ `수취인`(감지 키워드)

### 수정 내용
1. **헤더 행 자동 감지**: 1행/2행 비빈셀 개수 비교 → 2행이 더 많으면 헤더로 사용
2. **감지 키워드 확장**: `합계`, `금액`, `일자-No.`, `주문자명` 추가
3. **단가맵 헤더 확장**: 6열→9열 (수량, 품목명, 주소 추가)
4. **탭 생성 순서 변경**: 헤더 감지/데이터 처리 전에 탭 먼저 생성
5. **수동 수집 메뉴 추가**: `💰 판매현황_단가맵 수동 수집` (마감탭 정리 하위)
6. **진단 다이얼로그**: 0건 수집 시 실제 헤더+데이터 샘플 표시

### 수정 파일
| 파일 | 변경 |
|---|---|
| `_partnerExclusivePush.gs` | 헤더 행 자동 감지 + 키워드 확장 + 탭 생성 순서 + 9열 확장 |
| `_partnerWebApp.gs` | `partnerCollectPriceMapManual()` 추가 (진단 포함) |
| `_partnerMenu.gs` | `💰 판매현황_단가맵 수동 수집` 메뉴 추가 |

---

## 2026-06-20 08:50~09:25 — ★ 통합 일일마감 단가/정산금액 + 대리판매 소스 + 파일생성 수정

### 변경 내용
- **헤더 확장 (15열 → 19열)**: P(발주업체), Q(주문유형), R(단가), S(정산금액) 추가
- **대리판매(허브) 소스 추가**: `협력업체_발주허브` 탭에서 송장번호 있는 행 수집 (③번 소스)
- **로젠 단가 매핑**: `_buildSalesPriceMap_()` 호출 → 주문번호|품목코드 / 수취인|전화|품목코드 키로 단가 조회
- **대리공급 단가**: 임시기록 L열(합계) ÷ G열(수량) = 단가, L열 = 정산금액
- **대리판매 단가**: 허브 L열(정산금액) ÷ G열(수량) = 단가
- **마감 후 초기화**: `_clearSalesPriceMap_()` 호출
- **파일 생성 수정**: `addFile()+removeFile()` → `moveTo()` 전환 (파일 휴지통 이동 방지)
- **휴지통 파일 감지**: 캐시된 ID가 휴지통 파일이면 무시 + 새로 생성
- **품목명 인덱스 수정**: `row[22]`(W열=우편번호) → `row[10]`(K열=물품명)
- **로젠 0건 디버그**: 시트 열기/탭 찾기/데이터 행수/스킵 사유별 로그 추가

### 수정 파일
| 파일 | 변경 |
|---|---|
| `_partnerExclusivePush.gs` | `_UNIFIED_HEADERS_` 19열 확장 + 단가맵 호출/매핑 + 초기화 + 디버그 로그 |

### 영향 없음
- 기존 `_buildSalesPriceMap_()`, `_clearSalesPriceMap_()` 로직 변경 없음
- 일괄마감, 월별정산, 전용마감 영향 없음

---

## 2026-06-19 23:25 — ★ 통합 일일마감 독립화 (20시 자동 트리거 + 단가맵)

### 변경 내용
- **일괄마감에서 통합 일일마감 분리**: 기존 4단계 → 3단계 (월별정산 + 전용마감 + 초기화)
- **20시 자동 트리거**: `_pep_unifiedDailyArchiveScheduled_` — 매일 20시 자동 실행
- **판매현황 단가맵**: Push 완료 10분 후 자동 수집 (Push 성능 영향 없음)
  - `_pep_schedulePriceMapCollection_`: 10분 후 원샷 트리거 예약
  - `_pep_collectPriceMapDelayed_`: 지연 실행 핸들러 (세트분리 시트 → 단가맵)
  - `_pep_appendSalesPriceMap_`: 단가맵 누적 (중복 방지)
  - `_buildSalesPriceMap_`: 단가맵 → { key: 단가 } 맵 구축
  - `_clearSalesPriceMap_`: 마감 후 초기화
- **메뉴 추가**: `⏰ 통합 일일마감 (20시)` 하위메뉴 (트리거 설치/제거)
- **수동 실행**: 기존 `📋 통합 일일마감 (수동)` 메뉴 유지

### 수정 파일
| 파일 | 변경 |
|---|---|
| `_partnerExclusivePush.gs` | 단가맵 함수 3개 추가 + Push에 단가맵 호출 |
| `_partnerWebApp.gs` | 3단계 제거 + 트리거 함수 3개 추가 |
| `_partnerMenu.gs` | 자동화 메뉴에 일일마감 트리거 추가 |

### 영향 없음
- `_partnerMonthlySettle.gs` / `_partnerExclusiveArchive.gs` 수정 없음
- 기존 월별정산/전용마감/취소반품 로직 변경 없음

## 2026-06-19 21:46 — ★ 업체 시트 카카오 송장매칭 인증 오류 수정 (OCR 클라이언트 전환)

### 문제
- 업체 시트에서 카카오 송장 매칭 사이드바의 "분석" 버튼 클릭 시 인증 팝업 발생 → 진행 불가
- 회사 계정(Google Workspace)에서 권한 승인이 차단됨

### 원인
- `InvoiceMatch_협력업체용.gs`의 `ocrImageToTextLocal()` 함수가 `UrlFetchApp.fetch()` 사용
- GAS 정적 분석으로 코드에 `UrlFetchApp`이 존재하면 **모든 함수**에 `script.external_request` 스코프 필요
- 분석(텍스트 매칭)만 사용해도 인증 요구됨

### 수정: `InvoiceMatch_협력업체용.gs` — 클라이언트 측 OCR 전환
1. **서버 측 OCR 제거**: `ocrImageToTextLocal()` (UrlFetchApp 사용) → 완전 제거
2. **클라이언트 측 OCR 신규**: 사이드바 HTML 내 `fetch()` API로 Gemini 직접 호출
   - 서버 코드에 `UrlFetchApp` 없음 → **인증 불필요**
   - OCR 기능 100% 동일하게 동작
3. **API 키 주입**: `openInvoiceMatchSidebarLocal` → `__OCR_API_KEY__` 플레이스홀더를 실제 키로 교체
4. **업체 배포**: `getPartnerInvoiceMatchCode_`에서 `GEMINI_API_KEY` 변수 선언 자동 추가
5. **`_getOcrApiKey_()` 함수 신규**: 허브/업체 환경별 API 키 반환

### 적용 방법
```
clasp push → 관리/AS 도구 → 📝 스크립트 재설치 → 전체 업체
```


---



### 문제
- 고객 A가 세트 상품(UID-X, 2품목) + 다른 업체 단품(UID-Y) 주문
- 송장 수집 후 `사방넷_송장매칭` 탭에서 UID-X와 UID-Y의 송장번호가 뒤바뀜

### 원인
1. `_po_buildHubInvoiceKeyMap_`에서 같은 UID에 여러 송장 → **마지막 값만 유지**(덮어쓰기)
2. `_po_resolveTempTabInvoice_`에서 이름+전화 폴백 시 **다른 품목 송장 배정**
3. E열 보정 `invToUid` 맵에서도 같은 송장의 **UID 덮어쓰기** 발생

### 수정: `_partnerOrders.gs`

#### 1. `_po_buildHubInvoiceKeyMap_` — UID 키 다중 송장 병합
- 같은 UID에 여러 송장(세트 몸통/뚜껑) → 줄바꿈 구분으로 병합 (기존: 덮어쓰기)
- 복합키(코드|이름|전화) → 첫 매핑만 유지 (기존: 마지막 값으로 덮어쓰기)

#### 2. `_po_resolveTempTabInvoice_` — 소비형 매칭 추가
- `usedInvSet` 파라미터 추가 (옵셔널, 기존 호출 호환)
- 한 번 배정된 송장은 다음 행에서 건너뛰어 다른 송장 배정
- `_po_pickUnusedInvoice_()` 헬퍼 함수 신규 추가

#### 3. `_po_checkNonPartnerTempTabMatches_` — usedInvSet 전달
- 기존 송장도 `usedInvSet`에 등록하여 중복 배정 방지

#### 4. E열 보정 `invToUid` — 덮어쓰기 방지
- `if (!invToUid[bInv])` 조건 추가 → 첫 번째 매핑만 유지

---

## 2026-06-18 19:57 — 챗봇 사이드바 폭 1.5배 확대

| 챗봇 | 변경 전 | 변경 후 |
|---|---|---|
| 📊 단가 분석 챗봇 | 400px | **600px** |
| 🤖 CS 챗봇 | 300px | **450px** |

---

## 2026-06-18 19:00 — Gemini 모델 계층화 전략 (3.5-flash + 3.1-flash-lite)

정확도 중요 작업은 최신 `gemini-3.5-flash`, 단순 Q&A는 비용 효율 `3.1-flash-lite` 유지:

| # | 파일 | 용도 | 모델 | 이유 |
|---|---|---|---|---|
| 1 | `_partnerExclusivePush.gs` | 허브 OCR | **3.5-flash** | 이미지 인식 정확도 |
| 2 | `InvoiceMatch_협력업체용.gs` | 업체 OCR | **3.5-flash** | 이미지 인식 정확도 |
| 3 | `priceAnalysisChatbot.gs` | 단가 분석 | **3.5-flash** | 복잡 추론 |
| 4 | `CS_WebApp/Code.gs` | CS 웹앱 OCR | **3.5-flash** | 이미지 인식 정확도 |
| 5 | `카카오챗봇_설치파일/3_복사용.gs` | 복사용 | **3.5-flash** | 동기화 |
| 6 | `geminiChat.gs` | CS 챗봇 | 3.1-flash-lite | 비용↓ 유지 |

---

## 2026-06-18 18:43 — Gemini 모델 일괄 업그레이드 (3.1-flash-lite)

전체 운영 코드의 Gemini 모델을 `gemini-3.1-flash-lite`로 통일:

| # | 파일 | 변경 전 | 변경 후 |
|---|---|---|---|
| 1 | `_partnerExclusivePush.gs` | 2.5-flash | **3.1-flash-lite** |
| 2 | `InvoiceMatch_협력업체용.gs` | 2.5-flash | **3.1-flash-lite** |
| 3 | `geminiChat.gs` | 2.5-flash-lite | **3.1-flash-lite** |
| 4 | `priceAnalysisChatbot.gs` | 2.5-flash-lite | **3.1-flash-lite** |
| 5 | `카카오챗봇_설치파일/3_복사용.gs` | 2.5-flash-lite | **3.1-flash-lite** |
| 6 | `CS_WebApp/Code.gs` | 2.5-flash-lite | **3.1-flash-lite** |

---

## 2026-06-18 18:38 — 업체용 송장매칭 v2 (허브 동기화)

### 전면 수정: `InvoiceMatch_협력업체용.gs`
중앙 허브 `_partnerExclusivePush.gs`와 동일한 수준으로 전면 업그레이드:
1. **파서**: 인라인 파싱, 영문 이름, COURIER_PREFIX, 노이즈 필터, `_isValidName()`, 사업자번호/전화번호 필터
2. **매칭**: 5단계 (완전→공백→부분→공백+부분→유사도/Levenshtein)
3. **NFC 정규화**: 한글 인코딩 통일
4. **OCR**: `gemini-2.5-flash` 모델 + 확장 프롬프트
5. **디버그**: `_debug_sheetNames` 반환 → 미매칭 시 시트 이름 표시
6. **자동 주입 유지**: `getPartnerInvoiceMatchCode_()` → `_levenshteinLocal_` 추가

---

## 2026-06-18 18:15 — 송장 매칭 자동 주입 + 수취인 인식 개선

### 수정: `InvoiceMatch_협력업체용.gs`
- `getPartnerInvoiceMatchCode_()` 함수 추가: 함수 직렬화 기반으로 코드를 문자열 반환
- "스크립트 재설치" 시 업체 시트에 자동 배포 가능

### 수정: `priceManager.gs` (createViewerNoticeScript_)
- onOpen에 **📬 송장 매칭** 메뉴 자동 등록
- `fileList`에 `InvoiceMatch` 파일 추가 → 스크립트 재설치 시 자동 주입
- `getPartnerInvoiceMatchCode_()` 호출하여 코드 직렬화

### 수정: `_partnerExclusivePush.gs`
1. **OCR 모델 업그레이드**: `gemini-2.5-flash-lite` → `gemini-2.5-flash` (정확도 향상)
2. **NOISE_PLACES 필터 완전 제거**: 글자=이름, 숫자=송장번호로 단순 인식
3. **택배사 프리픽스 안전 분리**: `COURIER_PREFIX` 패턴 추가 ("롯데 /" → 제거, "롯데약국" → 보호)
4. **_cleanName에 "/" 제거** 추가
5. **2차 COURIER_PREFIX_CLEAN** 방어: 파싱 후 매칭 전에 잔여 프리픽스 추가 정리
6. **디버그 로깅**: `[PARSE_RESULT]`, `[SHEET_NAMES]` 로그 추가
7. **OCR 프롬프트 장소명 인식 완화**: 커뮤니티센터/관리사무소도 수취인으로 인식

---

## 2026-06-18 17:19 — 송장 매칭 거래명세서 + 영문 이름 지원

### 수정: `_partnerExclusivePush.gs`

**1. OCR 프롬프트 확장** (`ocrImageToText`)
- 택배 송장 목록 테이블 외에 **거래명세서 운송방법** 섹션도 인식하도록 프롬프트 개선
- 영문 이름 추출 지원 추가

**2. 파서 확장** (`_pep_parseInvoiceNamePairs_`)
- **인라인 파싱** 추가: `이름1 번호1 이름2 번호2...` 형태 (한 줄에 2쌍 이상) 자동 분리
- **영문 이름 인식**: `YINCHENGGUO`, `John` 등 영문 이름 지원
- **이름 유효성 통합 함수** `_isValidName_()`: 한글(2~15자), 영문(2~30자), 혼합(2~20자) 지원
- **노이즈 필터**: 커뮤니티센터, 관리사무소 등 장소명 자동 제외

---

## 2026-06-18 16:31 — 송장 수집 적요(M열) 스킵 해제

### 수정: `_partnerOrders.gs`
- 기존: 적요(M열=12)에 내용이 있으면 송장 수집 패스 → 적요가 미리 입력된 행은 송장 누락
- 변경: 적요 유무와 관계없이 송장 수집 진행
- 수정 위치: UID 매칭 패스(1317행), 이름+전화 매칭 패스(1468행) 2곳

---

## 2026-06-18 15:47 — ★ 카카오 송장 매칭 유사도 매칭 추가

### 문제
- "등촌청구아파트 관리사무소" vs "등촌청아파트관리사무소" → 공백/글자 차이로 매칭 실패
- "번계라" vs "번개라" → 오타 1자 차이로 매칭 실패

### 수정: `_partnerExclusivePush.gs`
- 매칭 5단계로 확장: ①완전일치 → ②공백제거 → ③부분포함 → ④공백제거+부분포함 → ⑤편집거리(Levenshtein)
- `_pep_levenshtein_()` 유사도 함수 신규 추가
- 오차 허용 기준: 전체 길이의 30% 이하 (최소 1자)

---

## 2026-06-18 14:27 — ★ 발주 수집 중복 방지 + 단가조회 3행 인식 수정

### 문제 1: 발주 수집 시간초과 후 재수집 시 중복 수집
- 첫 수집 중 시간초과 → 업체 시트에 고유ID 기록됨, 허브에는 일부만 기록
- 재수집 시 허브에 없는 건은 중복으로 안 걸려 다시 수집됨

### 수정 1: 허브 기록 직전 최종 중복 제거
- `_partnerOrders.gs`: 허브에 쓰기 직전, 현재 허브의 고유ID를 다시 읽어 중복 건 제거
- newOrders 내부 중복도 `dedupSeen`으로 제거
- 시간초과 후 재수집해도 안전하게 중복 없음

### 문제 2: 이카운트 코드 입력 시 단가조회 3행 인식 못함
- onEdit 코드가 4행부터 읽어 3행 데이터 누락 → 코드오류 표시

### 수정 2: `partnerOnEditSource.gs`
- `getRange(4, 1, ...)` → `getRange(3, 1, ...)` (3행 포함)
- ⚠️ 스크립트 재설치 필요

---

## 2026-06-18 10:38 — ★ 출고가능 동기화 installable trigger 전환

### 문제
- 허브 O열에 "출고가능" 입력해도 업체시트에 즉시 반영 안됨

### 원인
- `menu.gs`의 `onEdit()`는 **simple trigger** → `SpreadsheetApp.openById()` 권한 없음
- 외부 업체 시트에 접근 자체가 차단됨 (try/catch로 조용히 실패)

### 수정
- `_po_onEditHubShipApproval_`를 **installable trigger**로 전환
- `_partnerOrders.gs`: 트리거 설치/제거 함수 추가
- `_partnerMenu.gs`: 트리거 설치/해제 메뉴 추가
- `menu.gs`: simple onEdit에서 출고가능 호출 제거
- ⚠️ **1회 설치 필요**: 트리거 설정 메뉴에서 "✅ 출고가능 동기화 트리거 설치" 실행

---

## 2026-06-18 10:15 — ★ onEdit 배치 처리 최적화 + 취소/반품 수식 자동갱신 복원

### 문제
- 발주 탭에서 코드 7~8개 이상 붙여넣으면 자동완성이 느리고 멈추는 현상
- 외부 업체에서 느려서 오류로 인식
- 취소/반품 수식이 마감 후 갱신되지 않는 이슈 (6/18 최적화 과정에서 실수로 분리)

### 원인
- onEdit 내에서 **행마다 개별 getValue/setValue** → 8행 = 50+회 API 호출 → 30초 초과로 중단
- 취소/반품 수식 자동갱신이 일괄 마감에서 빠져있었음

### 변경 내용

#### `partnerOnEditSource.gs` — 배포 시트 onEdit 배치 처리
- **D/L열 자동완성**: 개별 `getValue/setValue` → `getValues/setValues` 배치 처리
  - 기존: 행마다 6~8회 API 호출 (8행 = 50+회, ~20초)
  - 개선: 전체 2~3회 API 호출 (100행도 ~1초)
- **J열(적요) 경고**: 개별 `getValue/setValue` → 배치 처리
- **N열(상태) 갱신**: 개별 `getValue/setValue` → 배치 처리 (기존 batchData 재사용)
- ⚠️ **배포 시트에 onEdit 코드 재설치 필요** (스크립트 재설치 메뉴)

#### `_partnerWebApp.gs` — 취소/반품 수식 자동갱신 복원
- 통합 루프 안에서 파일마다 `_cr_applyFormulas_()` 자동 호출 (추가 openById 없음)

#### `_partnerMonthlySettle.gs` — 개별 월별정산에서도 수식 자동갱신 복원

---

## 2026-06-18 09:10 — ★ 일괄 마감 성능 최적화 (v4.0) + 발주 수집 속도 개선

### 문제
- 다른 계정(무료 Gmail 6분 제한)에서 일괄 마감 시 시간초과 2~3건 발생
- 발주 수집 속도가 전체적으로 느림

### 원인
- 일괄 마감이 4단계에서 각각 모든 업체 파일을 openById로 열어 총 4N회 호출
- 발주 수집 시 heal/cleanup 등 무거운 보수 작업이 업체마다 무조건 실행

### 변경 내용

#### `_partnerWebApp.gs` — 일괄 마감 통합 루프
- `partnerDailyArchiveAll()` v4.0: 1+2단계를 **단일 파일 루프**로 통합
- openById 호출 **4N → 1N** (75% 감소)
- **경과 시간 안전 체크** (5분 제한) 추가 — 6분 GAS 한도 대비 1분 여유
- UI 확인 대화상자 우회 (일괄 실행 전용)

#### `_partnerMonthlySettle.gs` — 월별 정산 리팩토링
- `_pms_processOneFile_(ss, todayNum, archivedUids)` 단일 파일 처리 함수 분리
- `_pms_cleanupHub_()` 허브 정리 로직 별도 함수로 분리
- **취소/반품 수식 갱신**: 일괄 마감 자동 호출에서 제거 → 별도 메뉴로 분리

#### `_partnerExclusiveArchive.gs` — 전용마감 단일 처리
- `_pea_processOneFile_(ss, tabName)` 단일 파일 처리 함수 추가
- 기존 `_pea_core_()`는 수동 실행용으로 그대로 유지

#### `_partnerOrders.gs` — 발주 수집 경량화
- `_pt_cleanupStrayValidations_()` 호출 제거 (D/L열 값 기반 전환 완료로 불필요)
- `_pt_healOrderSpillFormulas()` 호출 제거 (복구시스템 AS도구에서만 실행)
- 업체당 **2~4초 절약** (10개 기준 20~40초)

#### `_partnerMenu.gs` — 메뉴 추가
- 📋 마감탭 정리 > **🔄 취소/반품 수식 갱신** 메뉴 추가
- 기존 개별 마감 메뉴(월별정산/전용마감)는 팝업UI 포함 그대로 유지

### 예상 효과 (업체 10개 기준)
| 항목 | 기존 | 개선 후 |
|------|:---:|:---:|
| openById 호출 | 40회 (~2분) | 10회 (~30초) |
| heal/cleanup | ~40초 | 0초 |
| 전용마감 미리보기 | ~40초 | 0초 |
| 합계 | ~4분+ | ~1분 내 |

---

## 2026-06-17 09:48 — D/L열 직접 편집 시 자동 복구 (단가 변조 방지)

- **배포 onEdit**: D(4) 또는 L(12)열 직접 편집 감지 → C열 코드 기준으로 뷰어 탭 재조회 → 원래 값으로 자동 복구
- **toast 안내**: "단가/품목명은 자동입력 열입니다. 원래 데이터로 복구되었습니다."
- **변경 파일**: `priceManager.gs`, `배포시트_자동완성코드_복사용.txt`

---

## 2026-06-17 09:35 — 복구 도구 체계화: 7개→4개 통합

### 삭제
- `🔄 전체 수식 갱신` → "수식·단가 복구" 전체 선택과 동일하므로 제거
- `🔧 A1 수식 #REF 수정` → heal 함수에 이미 포함 (하위 집합)
- `🗃️ 데이터 보정` → B6 서식만 남은 상태, "헤더·양식 복구"에 통합

### 통합
- "📋 헤더·양식 복구"에 B6 거래처코드 서식 보정 흡수
- repairDialog의 `data` 카테고리 → `header`로 리다이렉트 (하위 호환)

### 수정
- SpillGuard 진단: D/L열 ARRAYFORMULA 검사 제거 → 값 기반 체크로 변경
- L열 헤더 모든 곳에서 `"단가"`로 통일
- D열 헤더 `"품목명(자동)"`으로 통일

---

## 2026-06-17 09:10 — ★ D/L열 근본 전환: 수식 폐기 → 값 기반 아키텍처

### 개요
발주 및 송장조회 탭의 D열(품목명)과 L열(단가)에서 **수식을 완전 제거**하고, C열 입력 시 **onEdit가 뷰어 탭에서 직접 조회하여 값을 기록**하는 방식으로 전환.

### 변경 이유
- ARRAYFORMULA → 개별 수식으로 전환했으나, 사용자 삭제 시 수식 소실 반복
- 수식 복구 로직이 새로운 버그의 원인이 되는 악순환
- 수식 자체를 없애면 보호/복구 문제가 원천 제거됨

### 변경 파일

1. **`priceManager.gs`** (createViewerNoticeScript_)
   - D(4), L(12)열을 보호 대상에서 제거
   - D/L 수식 복구 블록 전체 삭제
   - C열 입력 시 뷰어 탭 조회 → D열(품목명), L열(단가) **값 직접 기록**
   - C열 삭제 시 D/L 자동 클리어

2. **`_partnerHelpers.gs`**
   - `_pt_injectOrderSpillFormulas`: D/L 수식 주입 → **헤더만 설정 + 기존 수식→값 변환**
   - `_pt_healOrderSpillFormulas`: D/L 수식 복구 → **기존 수식→값 변환 + 에러값 제거**
   - `_pt_onEditSpillGuard_`: D/L열 감시 제거 → **A열만 감시**

3. **`배포시트_자동완성코드_복사용.txt`**: 값 기반으로 전면 재작성

### 적용 방법
```
clasp push → 관리/AS 도구 → 📊 수식·단가 복구 → 전체 업체
           → 관리/AS 도구 → 📝 스크립트 재설치 → 전체 업체
```

---

## 2026-06-17 08:45 — D/L열 수식 삭제 방지: clearContent → 개별 수식 복구 전환

### 파일: `priceManager.gs` (createViewerNoticeScript_)

**증상**: 사용자가 발주 행을 삭제하거나 D/L열 셀을 지우면 개별 VLOOKUP 수식까지 삭제되어, 이후 이카운트코드를 입력해도 품목명/단가가 안 나옴

**원인**:  
- 배포 onEdit 스크립트의 보호 열 로직이 D(4), L(12)열을 `clearContent()`로 처리
- ARRAYFORMULA 시절에는 D1/L1에 수식이 있어서 하위 셀 클리어 후 자동 복원 되었으나
- 개별 수식 체계 전환 후 `clearContent()`가 **수식 자체를 삭제**하는 문제 발생

**수정 내용**:  
- 보호 열을 2그룹으로 분리: `clearOnly=[1,13,14]`, `formulaRestore=[4,12]`
- D/L열 편집 감지 시 `clearContent()` 대신 **VLOOKUP 수식을 동적으로 재생성**하여 복구
- 뷰어 탭 이름 동적 탐색 + `String.fromCharCode()`로 따옴표 이스케이프 안전 처리

**적용 방법**: clasp push → 관리/AS 도구 → 📝 스크립트 재설치 → 전체 업체

---

## 2026-06-17 08:30 — 배포 onEdit 스크립트 뷰어 탭 탐색 강화 (J열 경고 오작동 수정)

### 파일: `priceManager.gs` (createViewerNoticeScript_)

**증상**: 뉴파츠 등 뷰어 탭 이름이 "단가조회"가 아닌 업체에서 이카운트코드 입력 시 J열(적요) 경고 처리가 오작동

**원인**:  
- `getSheetByName('단가조회') || getSheets()[0]` → "단가조회" 탭이 없으면 무조건 첫 번째 탭을 사용
- 첫 번째 탭이 뷰어 탭이 아닌 경우 잘못된 탭 데이터로 코드오류 판정

**수정 내용**:  
- `_pt_findViewerSheet`와 동일한 수준의 확장 탐색 적용
- "단가조회", "뷰어", "팩투유", "단가" 키워드로 탐색
- 마감/발주/설정/검색 탭 제외 필터 추가
- 모든 탐색 실패 시에만 `getSheets()[0]` 폴백 사용

**적용 방법**: clasp push → 관리/AS 도구 → 📝 스크립트 재설치 → 전체 업체

---

## 2026-06-17 00:42 — 탭별 데이터 참조 구조 코드 문서화

### 파일: `_partnerHelpers.gs`

**배경**: "발주 및 송장조회" 탭과 "전용양식" 탭의 D/L열 참조 소스가 다른데, 이를 혼동하는 문제 방지

**수정 내용**:
- 수식 빌더 섹션에 **탭별 참조 구조 대형 주석 블록** 추가
  - `발주 및 송장조회` → 단가조회 탭 (모든 업체 공통)
  - `전용양식` → 뉴파츠공급가 또는 통합 허브 누적품목매핑 (업체별 상이)
- `_pt_buildUnitPriceCellFormula`, `_pt_buildItemNameCellFormula`, `_pt_applyIndividualFormulas` 각 함수에 JSDoc 추가 (대상 탭, 전용양식 사용 금지 명시)
- `_pt_injectOrderSpillFormulas` 전용양식 스킵 주석 강화

---

## 2026-06-17 00:00 — D/L열 ARRAYFORMULA → 개별 셀 수식 전환 (spill 충돌 근본 해결)

### 파일: `_partnerHelpers.gs`

**증상**: C열 이카운트코드 입력 → 1~2초 뒤 D열 품목명 전체 #REF 에러

**근본 원인**: ARRAYFORMULA는 열 전체를 하나의 spill 범위로 관리 → 다른 트리거(onEdit/SpillGuard)가 **어떤 셀이든 setValue**하면 ARRAYFORMULA가 재계산을 시작하고, 그 사이에 또 다른 setValue가 발생하면 spill 범위가 깨져서 #REF 발생

**수정 내용** (ARRAYFORMULA 폐기 → 개별 셀 수식):
- `_pt_injectOrderSpillFormulas` — D열/L열: D1에 ARRAYFORMULA 대신 "품목명(자동)" 텍스트 헤더 + D2~D260에 각 행별 독립 VLOOKUP 수식
- `_pt_healOrderSpillFormulas` — D열/L열: heal 시에도 개별 수식으로 복구 (기존 ARRAYFORMULA 자동 마이그레이션)
- `_pt_onEditSpillGuard_` — D/L열 수동입력 시 해당 행에 개별 수식 복구 (전체 heal 불필요)

**장점**:
- 각 행이 독립적이므로 spill 충돌 자체가 불가능
- 한 행의 수식이 깨져도 다른 행에 영향 없음
- onEdit 트리거와의 타이밍 충돌 완전 해소

**적용 방법**:
1. clasp push
2. 관리/AS 도구 → 📊 수식·단가 복구 → 전체 업체 (ARRAYFORMULA → 개별 수식 자동 마이그레이션)

---

## 2026-06-16 23:07 — heal 함수에 D/L열 잔류 수동입력 사전 스캔 추가

### 파일: `_partnerHelpers.gs`

**증상**: D1 ARRAYFORMULA는 정상이지만 D열 중간(예: D12)에 수동입력된 값이 있으면 spill 차단으로 #REF 발생

**수정 내용**:
- `_pt_healOrderSpillFormulas()` — D열/L열 heal 시 **잔류 수동입력 데이터 사전 스캔** 추가
- D2:D250 범위에서 수식이 아닌 수동입력값이 있으면 → 파괴 판정 → 전체 클리어 후 ARRAYFORMULA 재설치
- L열에도 동일한 로직 적용
- 이로써 `수식·단가 복구` 실행 시 잔류 데이터도 자동 정리됨

**적용 방법**: clasp push → 관리/AS 도구 → 📊 수식·단가 복구

---

## 2026-06-16 22:35 — 발주탭 D열 품목명 #REF 오류 근본 수정 (A1 수식 순환 의존성 + spill 충돌)

### 파일: `_partnerHelpers.gs`, `배포시트_자동완성코드_복사용.txt`, `_partnerMaintenance.gs`

**증상**: C열 이카운트코드 입력 → D열 품목명 자동생성 → **0.5초 뒤 상단 D열(품목명)들이 #REF 에러로 변경**

**원인**:  
1. **A1 수식의 D2:D 참조**: `LEN(C2:C)+LEN(D2:D)=0` → D열 ARRAYFORMULA와 순환 의존성 발생
2. **2개의 onEdit 트리거 충돌**: SpillGuard + 배포시트 onEdit가 ARRAYFORMULA 재계산 중에 setValue 호출 → spill 충돌

**수정 내용**:
- **`_partnerHelpers.gs`** — A1 수식 빌더: `LEN(C2:C)+LEN(D2:D)=0` → `LEN(C2:C)=0` (D열 참조 제거)
- **`_partnerHelpers.gs`** — SpillGuard C열 핸들러: N열 setValue 전 `SpreadsheetApp.flush()` 추가
- **`_partnerHelpers.gs`** — heal 함수: D2:D 참조 잔존 시 파괴 판정 추가
- **`배포시트_자동완성코드_복사용.txt`** — B열/J열 setValue 전 `SpreadsheetApp.flush()` 추가
- **`_partnerMaintenance.gs`** — `partnerFixAllOrderA1Formulas()` 일괄 수정 함수 추가

**적용 방법**: 
1. clasp push
2. 관리/AS 도구 → 🔧 A1 수식 D2:D 참조 제거 (일괄 적용)
3. 관리/AS 도구 → 📝 스크립트 재설치 (배포시트 onEdit flush 적용)

---

## 2026-06-16 19:55 — 뷰어 탭 탐색 범위 확장 (D열 #REF! 근본 해결)

### 파일: `_partnerHelpers.gs`

**증상**: 수식·단가 복구 실행 후에도 D1이 여전히 `#REF!` (그린우드 등 다수 업체)

**원인**:  
- `_pt_findViewerSheet`가 "단가조회", "뷰어" 키워드만 탐색
- "팩투유", "단가" 등 다른 이름의 뷰어 탭을 찾지 못함
- 탐색 실패 시 폴백 "단가조회" 사용 → 해당 탭 없으면 `#REF!`

**수정 내용**:  
- **1차 탐색**: "단가조회", "뷰어", "팩투유", "단가" 키워드 추가
- **2차 탐색**: K2에 그룹 열번호(7~200)가 있는 탭 자동 감지 (K2 기반)
- 비뷰어 탭(발주/설정/검색/마감) 제외 필터 강화
- `_pt_resolveViewerTabNameForOrderSpill`도 동일 로직으로 통합

**적용**: clasp push 후 → 관리/AS 도구 → 📊 수식·단가 복구 → 전체 선택

---

## 2026-06-16 17:58 — 자동입력 열 수동 입력 차단

### 파일: `priceManager.gs` (createViewerNoticeScript_)

**기능**: 발주 및 송장조회 탭의 자동입력 열에 수동 입력 시 즉시 되돌리고 안내 표시

**보호 대상 열**:  
- **A열** (거래처명 - ARRAYFORMULA)
- **D열** (품목명 - ARRAYFORMULA)
- **L열** (정산금액 - ARRAYFORMULA)
- **M열** (고유ID - 스크립트 전용)
- **N열** (상태 - 스크립트 전용)

**동작**:  
- 보호 열에 수동 입력 → 즉시 `clearContent()` → ARRAYFORMULA 자동 재충전
- "⚠️ 입력 차단: 이 열은 자동입력 열입니다" toast 안내 3초 표시
- 붙여넣기로 보호열 포함 범위 입력 시 → 보호열만 클리어, 나머지 정상 처리

**적용 방법**: clasp push 후 → 관리/AS 도구 → 📝 스크립트 재설치

---

## 2026-06-16 17:38 — 필수필드 완성도 감지 + 입력미완 경고 자동 제거

### 파일: `priceManager.gs` (createViewerNoticeScript_)

**증상**: 수량/수취인/전화번호/주소를 모두 입력해도 N열(상태)의 "⚠️입력미완" 경고가 사라지지 않음

**수정 내용**:  
- onEdit 트리거 범위를 **E~H열 편집**까지 확장
- E(수량)/F(수취인)/G(전화번호)/H(주소) 편집 시 필수필드 완성도 자동 감지
- 모든 필드 입력 완료 → N열 "입력미완" 경고 **자동 제거**
- 일부 누락 → N열에 "⚠️입력미완(누락항목)" **실시간 표시**
- 발주수집은 기존대로 실제 필드 값 검증 후 미완 시 스킵 (변경 없음)

**적용 방법**: clasp push 후 → 관리/AS 도구 → 📝 스크립트 재설치 → 전체 업체 선택

---

## 2026-06-16 17:33 — onEdit 스크립트 ARRAYFORMULA spill 충돌 수정

### 파일: `priceManager.gs` (createViewerNoticeScript_)

**증상**: 발주 및 송장조회 탭에서 이카운트코드 입력 시 해당 행 위쪽의 품목명이 모두 사라지고 D열·L열에 `#REF!` 표시 (전체 시트 공통)

**원인**:  
- 각 협력업체 시트에 설치되는 **onEdit 스크립트**가 C열 코드 입력 시 `C~M열 전체(11열)`를 `setValues`로 일괄 기록
- D열(품목명)·L열(단가)에 직접 값이 써지면서 **D1·L1의 ARRAYFORMULA spill 영역과 충돌** → `#REF!` 발생

**수정 내용**:  
- D열(품목명)·L열(단가) 기록 제거 → ARRAYFORMULA가 자동으로 처리
- `setValues(C~M 전체)` 방식 제거 → J열(적요)만 개별 `setValue`로 경고 기록
- J열에 품절/단종/코드오류 경고 + 정상 복구 시 경고 자동 제거 기능 유지

**적용 방법**: clasp push 후 → 관리/AS 도구 → 📝 스크립트 재설치 → 전체 업체 선택

---

## 2026-06-16 13:55 — 뉴파츠 발주탭 #REF!/코드오류 수정

### 파일: `_partnerDeploy.gs`

**증상**: 뉴파츠 발주 및 송장조회 탭에서 이카운트코드 입력 시 D열(품목명), L열(단가) 헤더에 `#REF!` 에러, J열(적요)에 코드오류 표시

**원인**:  
- 뉴파츠의 뷰어 탭 이름이 **"팩투유 (단가조회)"**인데, D1/L1의 ARRAYFORMULA 수식이 **"단가조회"** 탭을 참조하여 `#REF!` 발생
- `partnerForceUpdateAll` (전체 수식 갱신) 실행 시 뷰어 탭만 복구하고 발주탭 spill 수식은 복구하지 않았음

**수정 내용**:  
- `partnerForceUpdateAll` 함수에 **발주탭 spill 수식 heal** 호출 추가
- 전체 갱신 시 뷰어 탭 복구 후 → 발주탭 D1/L1 수식도 올바른 뷰어탭 이름으로 자동 재연결

**즉시 조치**: 관리/AS 도구 → 📊 수식·단가 복구 → 뉴파츠 선택 → 실행

**영향 범위**: 모든 협력업체 파일 (뷰어탭 이름이 "단가조회"와 다른 파일에서 동일 문제 방지)

---

## 2026-06-16 09:35 — 베스트고객 TOP10 정산금액 0원 버그 수정

### 파일: `_partnerBestCustomer.gs` (v1.2 → v1.3)

**증상**: 베스트고객 TOP10 분석 시 금액 관련 값이 모두 0으로 표시됨

**원인**:  
- 마감탭의 금액 열(L열) 헤더가 ARRAYFORMULA에 의해 **"단가"**로 생성됨
- `_pt_buildOrderTabColumnMap()`의 `unitPrice` 매핑 조건은 "정산단가", "확정단가", "정산금액"만 인식
- **"단가"** 단독 헤더는 해당 조건에 매치되지 않아 `unitPrice = -1` → 금액이 0으로 처리

**수정 내용**:  
- `_bc_mapColumns_()` 함수에 3단계 fallback 로직 추가:
  1. `full.unitPrice` (정산단가/확정단가/정산금액)
  2. `full.lineTotal` (합계/주문금액)  
  3. 헤더에서 "단가" 직접 스캔 (마감탭 전용 fallback)
- 근본 매핑(`_pt_buildOrderTabColumnMap`)은 다른 기능(월마감 이동 등)에 영향을 줄 수 있으므로 변경하지 않음

**영향 범위**: 베스트고객 분석 기능만 해당 (다른 기능 영향 없음)
