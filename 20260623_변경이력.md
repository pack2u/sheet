# 변경이력 (2026-06-23)

## 일시
- **2026-06-23 18:38 (KST)**

## 작업자
- Antigravity AI 코딩 어시스턴트

## 수정 및 추가된 기능 내역

### 1. `_partnerMenu.gs` 구문 오류 해결 (복구)
- **원인**: 메뉴 개편 중 중복/깨진 Conflict 코드 조각이 124행~172행에 삽입되어 clasp push 시 Syntax Error(Unexpected token) 발생.
- **해결**: 중복 찌꺼기를 완전히 삭제하고, 메뉴 구조를 정상화함.
- **검증**: 로컬 Node.js 구문 체크 완료 및 `clasp push` 업로드 완료.

### 2. 메뉴 2분할 구조 재구성 완성
사용자 환경 개선을 위해 일상 실행 메뉴와 시스템 진단/설정 메뉴를 최상위 독립 메뉴 2개로 분할하였습니다.

- **📦 대리발송 발주시스템 (일상 업무용)**
  - 스마트 검색
  - 일괄 마감이동
  - 발주 수집 및 이카운트 연동
  - 송장 수집/배포
  - 취소/반품 수집/배포
  - 마감 정리 (Dry-run 및 마감이동, 일일마감 수동저장)

- **💼 협력업체 관리 (관리자/진단용)**
  - 시트 생성 및 관리
  - 복구/보정 도구 (협력업체 복구시스템 등)
  - 통합 허브 단가 관리
  - 시스템 상태 확인 / 진단
  - **⚙️ 자동화 설정** (통합 자동 트리거 설치/제거 + 개별 레거시 설정 서브메뉴화)
  - **⏰ 통합 일일마감 (20시)** 자동 트리거 설정
  - 권한/동기화 락 제어

### 3. InvoiceMatchWebApp 구글 푸시 완료 (18:45 KST)
- **대상**: `InvoiceMatchWebApp` 내 2개 파일 (`Code.gs`, `appsscript.json`)
- **작업**: 로컬 수정 내역을 구글 앱스 스크립트 서버에 업로드 완료
- *참고: `CS_WebApp`은 로컬 변경 사항이 없어 푸시가 자동으로 스킵되었습니다.*

### 4. 탭 조회 성능 최적화 완료 (22:00 KST)
- **대상 파일**: `_partnerOrders.gs`, `_partnerExclusivePush.gs`
- **개선 내용**: 
  - 발주 수집, 송장 배포, 대리공급 Push 캐싱 시 모든 탭을 순회(`getSheets()`)하던 부분을 `getSheetByName`으로 다이렉트 조회하도록 개선.
  - 마감 탭이 수십 개 쌓여 있는 업체 시트 처리 속도가 획기적으로 향상됨.
  - **스킵 방지 검증**: `getSheetByName` 조회 실패 시에만 기존의 전체 탭 순회(`getSheets()`)가 작동하도록 폴백(Fallback) 안전장치를 구축하여 발주 누락을 원천 차단함.

### 5. 전체 트리거 Google Chat 알림 추가 (2026-06-24 07:15 KST)
- **대상 파일**: `_partnerWebApp.gs`, `ecount.gs`, `_partnerGmailInvoice.gs`
- **목적**: 모든 자동 트리거 실행 시 성공/실패 결과를 Google Chat 스페이스로 알림 전송
- **추가된 알림 대상**:
  - `_pep_unifiedDailyArchiveScheduled_` (05:00 통합 일일마감) — 성공 시 건수별 상세 + 실패 시 에러 알림
  - `runDailyEcountBatch` (06:00/12:00 이카운트 전체동기화) — 성공/실패 + 소요시간
  - `runDailyHubBatch` (12:50 통합허브 상태/재고) — 성공/실패 + 소요시간
  - `_gmi_triggerFetchNKInvoice_` (15:20 냅킨코리아 Gmail) — 매칭/미매칭 건수 + 에러 알림
- **기존 알림 유지**: 대리판매 마감, 대리발주 마감, 도서산간, 우편번호, 송장 수집/배포 등 기존 Chat 알림은 변경 없음

### 6. 카카오 송장매칭 웹앱 프록시 제거 → 다이렉트 방식 전환 (2026-06-24 07:33 KST)
- **대상 파일**: `_partnerExclusivePush.gs`
- **근본 원인**: 6/23에 사이드바 서버 함수를 `doPost` 웹앱 프록시 방식으로 변경했으나, 웹앱 URL 만료/배포 미반영으로 "분석 버튼 먹통" 현상 반복 발생
- **해결**: 
  - `getPartnerFileListForSidebar()` → `_imGetPartnerFileList_()` 직접 호출
  - `parseAndMatchInvoiceText()` → `_imParseAndMatch_()` 직접 호출
  - `applyInvoiceMatches()` → `_imApplyMatches_()` 직접 호출
  - 웹앱 URL 의존성 완전 제거 → 배포 상태 무관하게 작동
  - `google.script.run`은 현재 스크립트의 함수를 직접 실행하므로 편집자 권한 사용자도 정상 사용 가능

### 7. 협력업체 시트 송장매칭 v11 안정화 (2026-06-24 07:52 KST)
- **대상 파일**: `InvoiceMatch_협력업체용.gs`
- **근본 원인**: `getPartnerInvoiceMatchCode_()`에서 `openInvoiceMatchSidebarLocal.toString()`, `_getInvoiceMatchHtmlSimple_.toString()`으로 함수를 직렬화할 때, 내부의 이스케이프 문자(`\"`, `\n`, `</script>`)가 한 겹 벗겨지면서 업체 시트에 주입된 코드가 구문 오류를 발생시킴
- **해결 (v11)**:
  - `openInvoiceMatchSidebarLocal`과 `_getInvoiceMatchHtmlSimple_`의 `.toString()` 직렬화 **완전 제거**
  - 이 두 함수는 **안전한 문자열 리터럴 배열**(`.join("\n")`)로 직접 코드를 생성
  - 한글/이모지는 **유니코드 이스케이프**(`\uXXXX`)로 변환하여 인코딩 깨짐 방지
  - `parseAndMatchInvoiceTextLocal`, `applyInvoiceMatchesLocal` 등 나머지 함수는 `.toString()` 유지 (내부에 이스케이프 없어 안전)
- **적용 방법**: `clasp push` 후 → 허브 시트에서 `💼 협력업체 관리 > 🔧 관리/AS 도구 > 🛠️ 스크립트 재설치` 실행

---

## 2026-06-24 22:55 (KST) — 통합 일일마감 로젠 재설계 + 임시기록 초기화

### 변경 1: 통합 일일마감 로젠 섹션 전면 재설계 (`_partnerExclusivePush.gs`)

**기존 방식 → 새 방식:**
- 기존: `입력_로젠주문실적` 37열 직접 읽기 + `priceMap`으로 단가 별도 매칭 (불완전)
- 변경: **판매현황 C~Q열 그대로** + `입력_로젠주문실적` E→F 매칭으로 운송장번호만 추가

**핵심 변경:**
| 항목 | 기존 | 변경 후 |
|---|---|---|
| 데이터 소스 | 입력_로젠주문실적 37열 | **판매현황 C~Q** (15열) |
| 운송장번호 | 로젠 F열 직접 | 로젠 E→F 맵 매칭 |
| 단가 | priceMap 별도 구축 | 판매현황에 이미 포함 → **priceMap 제거** |
| 매칭 키 | 전화7자리+품목명 | **판매현황 O열 ↔ 로젠 E열** (사방넷번호/고유ID) |
| 아카이브 형식 | 공통 19열 헤더 | **섹션별 서브헤더** (로젠 16열 / 대리공급·대리판매 19열) |

### 변경 2: 대리공급_임시기록 초기화 (`_partnerExclusiveArchive.gs`)
- `_pea_core_` 함수 끝에 `_po_clearTempTabInvoicedRowsOnly_` 호출 추가
- 07:20 대리공급 전용발주 마감 시 송장번호 입력된 임시기록 행 자동 삭제
- 일괄마감 분리 후 누락되었던 초기화 로직 복구

### 변경 3: Chat 알림 개선 (`_partnerWebApp.gs`)
- `_trigger_exclusiveArchive_`의 Chat 알림에 임시기록 초기화 결과(삭제/유지 건수) 추가

### 검증
- Node.js 구문 체크 통과 (3개 파일 모두 OK)

---

## 2026-06-24 23:42 (KST) — 판매현황 스냅샷 + 송장 지연 매칭 시스템

### 배경
- 판매현황은 하루 2회 초기화 → 다음날 송장이 나와도 원본 데이터 소실
- 해결: 매일 판매현황을 스냅샷으로 보관 → 며칠 뒤 송장이 나와도 매칭 가능

### 변경 1: 판매현황 스냅샷 저장 (`_partnerExclusivePush.gs`)
- 새 함수 `_pep_saveSnapshotToHub_(srcSS)` 추가
  - 대리공급 Push 시 판매현황 C~Q를 허브 '판매현황_스냅샷' 탭에 누적 저장
  - 이미 열린 세트분리 시트(`srcSS`) 재사용 → 추가 API 호출 없음
  - 같은 날짜+매칭키 중복 자동 방지
- `partnerPushOrdersToExclusiveForms()` 끝에 스냅샷 저장 호출 추가

### 변경 2: 일일마감 스냅샷 기반 매칭으로 전환 (`_partnerExclusivePush.gs`)
- `_pep_archiveUnifiedDaily_()` 내 로젠 섹션 전면 재설계
- 기존: 판매현황 직접 읽기 → 로젠 맵만 매칭
- 변경: **스냅샷 탭에서 미매칭 행** × (로젠+대리공급) **통합 송장맵** 매칭
  - 로젠 송장: 입력_로젠주문실적 E→F
  - 대리공급 송장: 임시기록 P→X
- 매칭 성공 → 스냅샷 상태 "매칭완료" + 일일마감 기록
- 미매칭 → 스냅샷에 유지 (다음날 재시도)

### 변경 3: 스냅샷 정리 함수 (`_partnerExclusivePush.gs`)
- 새 함수 `_pep_cleanupSnapshot_()` 추가
  - "매칭완료" 행 삭제 + 7일 초과 미매칭 행 만료 삭제
  - 일일마감 끝에서 자동 호출

### 검증
- Node.js 구문 체크 통과

---

## 2026-06-25 00:15 (KST) — 통합 일일마감: 대리공급/대리판매 별도 수집 제거

### 배경
- 이전 대화에서 합의: "판매현황 = 전체 주문이므로 대리공급/대리판매를 따로 수집할 필요 없음"
- 스냅샷 + 송장 매칭만으로 일일마감 완성해야 함
- 그런데 코드에 대리공급 임시기록/대리판매 허브 직접 수집 레거시가 그대로 남아있었음

### 변경 1: `_pep_archiveUnifiedDaily_()` 재설계 (`_partnerExclusivePush.gs`)

**제거된 코드:**
- `allRows` 변수 (대리공급+대리판매 19열 배열)
- ② 대리공급_임시기록 직접 읽기 섹션 (~40행)
- ③ 대리판매 협력업체_발주허브 직접 읽기 섹션 (~45행)
- 3개 섹션(로젠/대리공급/대리판매) 분리 아카이브 기록 (~80행)

**변경된 구조:**
| 항목 | 기존 | 변경 후 |
|---|---|---|
| result.detail | `{ lozen, temp, hub }` | `{ matched, lozen, supply, skipped, noInvoice }` |
| 아카이브 포맷 | 3개 섹션 (로젠16열 / 대리공급19열 / 대리판매19열) | **단일 포맷** (헤더1줄 + 데이터, 출처 열로 구분) |
| 배경색 | 섹션별 단색 | **출처별** (로젠=#e8eaf6, 대리공급=#e8f5e9) |

### 변경 2: UI 알림 메시지 변경 (`_partnerWebApp.gs`)

**기존:**
```
로젠: 0건
대리공급: 150건
대리판매: 68건
합계: 218건
```

**변경 후:**
```
매칭 기록: 150건
 └ 로젠 송장: 80건
 └ 대리공급 송장: 70건
미매칭(다음날 재시도): 30건
```

### 변경 3: Chat 알림도 동일 구조로 변경

### 검증
- Node.js 구문 체크 통과 (2개 파일)
- clasp push 업로드 완료

---

## 2026-06-25 00:28 (KST) — 스냅샷 즉석 생성 폴백

### 문제
- 통합 일일마감 최초 실행 시 `판매현황_스냅샷` 탭이 없어서 진행 불가
- 스냅샷은 대리공급 Push 시 생성되는데, 최초 실행이나 Push 전에는 존재하지 않음

### 해결 (`_partnerExclusivePush.gs`)
- `_pep_archiveUnifiedDaily_()` 내에 **스냅샷 없을 때 폴백** 추가
- 스냅샷 탭 없거나 비어있으면 → 세트분리 시트의 판매현황에서 `_pep_saveSnapshotToHub_()` 즉석 호출
- 즉석 생성 성공 → 매칭 진행 / 실패 → 명확한 에러 메시지 반환

---

## 2026-06-25 00:35 (KST) — 아카이브 시트 합계 행 추가

### 변경 (`_partnerExclusivePush.gs`)
- 아카이브 시트 데이터 행 마지막에 **합계 행** 추가
- G열: 합계(수량 등) 총합 (천단위 구분)
- J열: 단품배송비 총합 (천단위 구분)
- Q열(출처): "로젠: N건 / 대리공급: N건" 요약
- A열: "★ 합계 (N건)" 표시
- 스타일: 짙은 회색(#37474f) + 흰색 글자 + 굵게

---

## 2026-06-25 00:42 (KST) — 매칭 0건 에러 → 정상 결과로 변경

### 문제
- 스냅샷은 있지만 매칭할 송장이 없을 때 (새벽, 송장 미수집 등) 에러로 처리됨
- "❌ 일일마감 오류: 기록 데이터 없음 (스냅샷 매칭 결과 0건)" 표시

### 해결 (`_partnerExclusivePush.gs`)
- `matchedRows === 0`일 때 `result.error` 설정 제거
- 에러 대신 `result.archived = 0`으로 정상 반환
- UI 알림: "매칭 기록: 0건 / 미매칭(다음날 재시도): N건" 으로 표시
- 스냅샷 없음 + 즉석 생성 실패 시에만 에러 메시지 표시

### 검증
- Node.js 구문 체크 통과
- clasp push 업로드 완료
