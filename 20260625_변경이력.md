# 변경이력 (2026-06-25)

## 작업자
- Antigravity AI 코딩 어시스턴트

---

## 2026-06-25 00:15 (KST) — 통합 일일마감: 대리공급/대리판매 별도 수집 제거

### 배경
- 이전 대화에서 합의: "판매현황 = 전체 주문이므로 대리공급/대리판매를 따로 수집할 필요 없음"
- 스냅샷 + 송장 매칭만으로 일일마감 완성해야 함
- 그런데 코드에 대리공급 임시기록/대리판매 허브 직접 수집 레거시가 그대로 남아있었음

### 변경 1: `_pep_archiveUnifiedDaily_()` 재설계 (`_partnerExclusivePush.gs`)

**제거된 코드:**
- `allRows` 변수 (대리공급+대리판매 19열 배열)
- ② 대리공급_임시기록 직접 읽기 섹션 (~40행)
- ③ 대리판매 협력업체_발주허브 직접 읽기 섹션 (~45행)
- 3개 섹션(로젠/대리공급/대리판매) 분리 아카이브 기록 (~80행)

**변경된 구조:**
| 항목 | 기존 | 변경 후 |
|---|---|---|
| result.detail | `{ lozen, temp, hub }` | `{ matched, lozen, supply, skipped, noInvoice }` |
| 아카이브 포맷 | 3개 섹션 (로젠16열 / 대리공급19열 / 대리판매19열) | **단일 포맷** (헤더1줄 + 데이터, 출처 열로 구분) |
| 배경색 | 섹션별 단색 | **출처별** (로젠=#e8eaf6, 대리공급=#e8f5e9) |

### 변경 2: UI 알림 메시지 변경 (`_partnerWebApp.gs`)

**기존:**
```
로젠: 0건
대리공급: 150건
대리판매: 68건
합계: 218건
```

**변경 후:**
```
매칭 기록: 150건
 └ 로젠 송장: 80건
 └ 대리공급 송장: 70건
미매칭(다음날 재시도): 30건
```

### 변경 3: Chat 알림도 동일 구조로 변경

### 검증
- Node.js 구문 체크 통과 (2개 파일)
- clasp push 업로드 완료

---

## 2026-06-25 00:28 (KST) — 스냅샷 즉석 생성 폴백

### 문제
- 통합 일일마감 최초 실행 시 `판매현황_스냅샷` 탭이 없어서 진행 불가
- 스냅샷은 대리공급 Push 시 생성되는데, 최초 실행이나 Push 전에는 존재하지 않음

### 해결 (`_partnerExclusivePush.gs`)
- `_pep_archiveUnifiedDaily_()` 내에 **스냅샷 없을 때 폴백** 추가
- 스냅샷 탭 없거나 비어있으면 → 세트분리 시트의 판매현황에서 `_pep_saveSnapshotToHub_()` 즉석 호출
- 즉석 생성 성공 → 매칭 진행 / 실패 → 명확한 에러 메시지 반환

---

## 2026-06-25 00:35 (KST) — 아카이브 시트 합계 행 추가

### 변경 (`_partnerExclusivePush.gs`)
- 아카이브 시트 데이터 행 마지막에 **합계 행** 추가
- G열: 합계(수량 등) 총합 (천단위 구분)
- J열: 단품배송비 총합 (천단위 구분)
- Q열(출처): "로젠: N건 / 대리공급: N건" 요약
- A열: "★ 합계 (N건)" 표시
- 스타일: 짙은 회색(#37474f) + 흰색 글자 + 굵게

---

## 2026-06-25 00:42 (KST) — 매칭 0건 에러 → 정상 결과로 변경

### 문제
- 스냅샷은 있지만 매칭할 송장이 없을 때 (새벽, 송장 미수집 등) 에러로 처리됨
- "❌ 일일마감 오류: 기록 데이터 없음 (스냅샷 매칭 결과 0건)" 표시

### 해결 (`_partnerExclusivePush.gs`)
- `matchedRows === 0`일 때 `result.error` 설정 제거
- 에러 대신 `result.archived = 0`으로 정상 반환
- UI 알림: "매칭 기록: 0건 / 미매칭(다음날 재시도): N건" 으로 표시
- 스냅샷 없음 + 즉석 생성 실패 시에만 에러 메시지 표시

### 검증
- Node.js 구문 체크 통과
- clasp push 업로드 완료

---

## 2026-06-25 00:51 (KST) — 대리공급 Push에서 스냅샷 생성 제거

### 변경 (`_partnerExclusivePush.gs`)
- 대리공급 Push 실행 시 `_pep_saveSnapshotToHub_()` 호출 제거
- 스냅샷은 일일마감(오전 5시) 시 판매현황에서 즉석 생성으로 대체
- Push가 더 가볍고 빠르게 동작

**변경된 흐름:**
```
기존: 대리공급 Push → 스냅샷 저장 → ... → 일일마감 → 스냅샷 읽기
변경: 대리공급 Push → (스냅샷 없음) → ... → 일일마감 → 즉석 생성 → 매칭
```

---

## 2026-06-25 00:55 (KST) — 비주문 항목 바이패스 (반품/도서산간 등)

### 문제
- 스냅샷 매칭키(B열)에 "반품", "반품비", "제주도서산간" 등 비주문 내용이 포함된 행은 송장 매칭 불가
- 이런 항목들이 "미매칭"으로 분류되어 7일 후 만료 삭제됨

### 해결 (`_partnerExclusivePush.gs`)
- 매칭키에 비주문 키워드가 포함되면 **송장 없이 바로 아카이브에 기록**
- 키워드 목록: `반품`, `반품비`, `제주도서산간`, `도서산간`, `추가배송비`
- 출처: "기타" / 운송장번호: 공란
- 스냅샷 상태: "매칭완료"로 처리 (다음날 재처리 방지)

### 검증
- Node.js 구문 체크 통과
- clasp push 업로드 완료

---

## 2026-06-25 06:20 (KST) — 일일마감 시트 전화번호 앞 0 소실 수정

### 문제
- 일일마감 아카이브 시트에서 전화번호가 숫자열로 저장되어 앞의 0이 빠짐
- 예: `01012345678` → `1012345678`, `0212345678` → `212345678`
- 원인: `getValues()` 호출 시 Google Sheets가 전화번호를 숫자로 자동 변환

### 해결 (`_partnerExclusivePush.gs`)

**1. 유틸 함수 추가:**
- `_pep_fixPhoneLeadingZero_(val)`: 숫자로 읽힌 전화번호에 앞 0 복원
- `_pep_detectPhoneColumns_(headers)`: 헤더에서 전화번호/휴대폰 열 자동 감지

**2. 스냅샷 저장 시 보정 (3곳):**
- `getValues()` 직후 전화번호 값 문자열 보정
- 스냅샷 탭 신규/기존 모두 전화번호 열에 텍스트 서식(`@`) 적용

**3. 아카이브 기록 시 보정 (2곳):**
- `setValues()` 전 이중 보정 + 전화번호 열 텍스트 서식(`@`) 적용

---

## 2026-06-25 06:32 (KST) — 비주문 항목 판별 조건 확장

### 변경 (`_partnerExclusivePush.gs`)

**기존 비주문 키워드:** 반품, 반품비, 제주도서산간, 도서산간, 추가배송비

**추가된 판별 조건:**
1. **키워드 추가**: `"제주도서"` (기존 "제주도서산간"과 별도)
2. **D열 [샘플] 감지**: 스냅샷 D열(index 3)에 `[샘플]` 포함 시 비주문 처리
3. **매칭키 한글 감지**: B열 매칭키에 한글이 포함되어 있으면 비주문 처리 (주문번호는 숫자/영문만)

**동작**: 비주문 항목은 송장 매칭 없이 바로 아카이브에 기록 (출처="기타")
