# 변경이력 (2026-06-26 ~ 2026-06-27)

## 2026-06-27 11:27 (KST) — 주말 차단 기능 추가

### 개요
- 토요일 09:00 ~ 월요일 05:00 사이에 모든 자동 트리거 실행 차단
- 트리거 자체는 매일 실행되지만, 함수 시작 시 `_pt_isWeekendBlackout_()` 체크 후 즉시 return

### 변경 파일 (6개 파일, 12개 함수)
| 파일 | 함수 | 설명 |
|---|---|---|
| `_partnerHelpers.gs` | `_pt_isWeekendBlackout_()` | **신규** 주말 차단 헬퍼 |
| `_partnerWebApp.gs` | `_pep_unifiedDailyArchiveScheduled_` | 통합 일일마감 |
| `_partnerWebApp.gs` | `_trigger_monthlySettle_` | 대리판매 마감 |
| `_partnerWebApp.gs` | `_trigger_exclusiveArchive_` | 대리공급 마감 |
| `_partnerWebApp.gs` | `_trigger_islandShipping_` | 도서산간 배송비 |
| `_partnerWebApp.gs` | `_trigger_fillZipAndShipping_` | 우편번호 채우기 |
| `_partnerWebApp.gs` | `_trigger_fetchInvoices_` | 송장 수집 |
| `_partnerWebApp.gs` | `_trigger_pushInvoices_` | 송장 배포 |
| `_partnerOrders.gs` | `partnerCollectOrdersSilent_` | 발주 수집 |
| `_partnerGmailInvoice.gs` | `_gmi_triggerFetchNKInvoice_` | NK Gmail 송장 |
| `_partnerExclusivePush.gs` | `partnerPushOrdersToExclusiveFormsSilent_` | 대리공급 Push |
| `ecount.gs` | `runDailyEcountBatch` | 이카운트 동기화 |
| `ecount.gs` | `runMorningSyncStatusBatch` | 상태 동기화 |
| `ecount.gs` | `runDailyHubBatch` | 허브 동기화 |
| `sendInventory.gs` | `runDailyInventoryAdjustBatch` | 재고조정 |

---

## 2026-06-27 08:53 (KST) — _partnerWebApp.gs 이카운트 트리거 비활성화 누락 수정

### 문제
- 6/26에 `ecount.gs`의 트리거 생성은 주석 처리했으나, `_partnerWebApp.gs`의 **통합 스케줄 배열**(`_ALL_SCHEDULED_TRIGGERS_`)에 `runDailyEcountBatch`가 여전히 활성 상태로 남아 있었음
- `setupAllScheduledTriggers()` 실행 시 이카운트 트리거가 다시 생성되어 새벽에 실행됨

### 변경 (`_partnerWebApp.gs`)
- `_ALL_SCHEDULED_TRIGGERS_` 배열에서 `runDailyEcountBatch` 2개 항목 주석 처리:
  - L1340: `06:00 이카운트 전체동기화 1` → 주석 처리
  - L1352: `12:00 이카운트 전체동기화 2` → 주석 처리

---

## 2026-06-27 09:02 (KST) — 이카운트 06시/12시 트리거 재활성화

### 변경 (`_partnerWebApp.gs`)
- `_ALL_SCHEDULED_TRIGGERS_` 배열에서 `runDailyEcountBatch` 재활성화:
  - `06:00 이카운트 전체동기화 1` ✅ 활성
  - `12:00 이카운트 전체동기화 2` ✅ 활성
- 기존 `ecount.gs`의 `02:10` 트리거는 주석 상태 유지 (제거됨)

### 후속 조치
- `clasp push` 배포 필요
- GAS 에디터 > ⏰ 트리거 목록에서 `runDailyEcountBatch` 잔존 트리거 수동 삭제 권장
- 또는 메뉴 `⏰ 통합 트리거 설치`를 다시 실행하면 기존 전체 정리 후 이카운트 제외한 스케줄로 재설치됨

---


## 작업자
- Antigravity AI 코딩 어시스턴트

---

## 2026-06-26 05:47 (KST) — 이카운트 오전 2시 트리거 비활성화

### 배경
- 이카운트 토큰 에러로 `runDailyEcountBatch` (오전 2시) 동기화가 계속 실패
- 매일 새벽 불필요한 에러 실행이 반복되는 상황

### 변경 (`ecount.gs`)

**비활성화된 트리거:**
- `runDailyEcountBatch` — 매일 새벽 2시 10분 (원본) / 2시 25분 (사본)

**수정된 함수 (4곳):**
1. `setupDailyTrigger()` (L565~571): 트리거 생성 주석 처리
2. `initializeEcountForSheet()` (L1171): 트리거 생성 주석 처리
3. `setupDailyTriggerForCopy()` (L1228): 트리거 생성 주석 처리
4. UI 안내 메시지: "❌ 토큰에러로 비활성화" 표시

**유지되는 트리거:**
| 시간 | 함수 | 역할 |
|---|---|---|
| 01:20 / 11:50 | `runDailyInventoryAdjustBatch` | 재고조정 자동 전송 |
| 07:00 | `runMorningSyncStatusBatch` | 판매 상태/재고 동기화 |
| 12:30 | `runDailyHubBatch` | 통합 허브 동기화 |

**삭제 대상 유지:**
- `removeDailyTrigger()` 함수에서는 `runDailyEcountBatch` 삭제 대상 유지 (기존 등록된 트리거 정리용)

### 후속 조치
- 시트 메뉴 > ⏰ 자동연동 켜기 실행 필요 (기존 트리거 삭제 + 새 트리거 설치)
- 이카운트 토큰 문제 해결 후 주석 해제하여 재활성화 가능

### 검증
- clasp push 52개 파일 배포 완료

---

## 2026-06-26 06:07 (KST) — 대리공급 Push에서 스냅샷 저장 복원

### 문제
- 6/25에 Push에서 스냅샷 생성을 제거하고, 일일마감 시 즉석 생성으로 대체했음
- 그러나 **오전 5시 일일마감 시점에는 판매현황이 이미 비어있어** 즉석 생성 불가
- 결과: 스냅샷에 이전 잔여 8건만 남아있고, 361건의 송장과 매칭 0건 → 시트 미생성

### Cloud 로그 (진단)
```
송장맵: 361건 (로젠 311 + 대리공급 50)  ← 충분
스냅샷: 8건만 남아있음 → 매칭 0건      ← 문제!
```

### 해결 (`_partnerExclusivePush.gs` L1620~1631)
- Push에서 `_pep_saveSnapshotToHub_(srcSS)` 호출 **복원**
- Push 시점(09:20, 14:30)에는 판매현황 데이터가 존재하므로 스냅샷 누적 가능
- `srcSS`는 L681에서 이미 열려있어 **추가 API 호출 없음**

### ~~복원된 흐름~~ (06:29에 재수정됨 — 아래 참조)

### 검증
- clasp push 52개 파일 배포 완료

---

## 2026-06-26 06:29 (KST) — 일일마감에서 항상 판매현황 직접 읽기로 변경

### 문제
- 06:07에 Push에서 스냅샷 저장을 복원했으나, 원래 설계 의도와 다름
- **스냅샷의 목적**: 미발송분만 기록으로 남기는 것
- **판매현황 읽기**: 일일마감(05:00) 실행 시 직접 판매현황을 읽어야 함
- 기존 코드의 버그: 스냅샷에 잔여 행이 있으면 판매현황을 읽지 않음 (조건문 문제)

### 변경 (`_partnerExclusivePush.gs`)

**1. Push에서 스냅샷 생성 다시 제거** (L1620~1621)
- 원래 설계대로 Push는 스냅샷을 건드리지 않음

**2. 일일마감에서 항상 판매현황 읽기** (L5850~5862)
- 기존: `if (!snapTab || snapTab.getLastRow() < 2)` → 비어있을 때만 판매현황 읽음
- 변경: **조건 없이 항상** `_pep_saveSnapshotToHub_(srcSS)` 호출
- 판매현황 읽기 실패 시 → 기존 스냅샷(미발송분)으로 계속 진행

### 올바른 흐름
```
일일마감(05:00) 실행:
  ① 판매현황 읽기 → 스냅샷에 새 행 추가 (중복키 제거)
  ② 기존 미발송분 + 신규 = 스냅샷 전체 대상
  ③ 송장맵(로젠+대리공급) 구축
  ④ 스냅샷 × 송장맵 매칭
  ⑤ 매칭됨 → 아카이브 시트 생성
  ⑥ 미매칭(미발송) → 스냅샷에 남김 (다음날 재시도)
  ⑦ 매칭완료 삭제 + 7일 초과 삭제
```

### 검증
- clasp push 배포 완료
- 수동 일일마감 테스트로 확인 가능

---

## 2026-06-26 06:55 (KST) — 이카운트 작업 메뉴 정리

### 삭제 (4개)
| 메뉴 | 함수 | 삭제 이유 |
|---|---|---|
| 이카운트 연동 초기화 (사본/첫 설치용) | `initializeEcountForSheet` | 초기 설치 완료 후 불필요 |
| 자동연동 켜기 — 사본 전용 시간 | `setupDailyTriggerForCopy` | 사본용이므로 원본에서 불필요 |
| 자동연동 켜기 (01:20/02:00/11:50/12:30) | `setupDailyTrigger` | 현재 트리거(06:00/12:00)와 다른 시간, 02:00 재설치 위험 |
| 자동연동 끄기 | `removeDailyTrigger` | 모든 이카운트 트리거 일괄 삭제 → 위험 |

### 보존 (4개)
| 메뉴 | 함수 | 보존 이유 |
|---|---|---|
| 자동연동 상태 확인 | `showDailyTriggerStatus` | 트리거 진단에 유용 |
| 자동연동 복구 실행 | `repairDailyTriggerHealth` | 트리거 장애 시 필요 |
| 이카운트 연동 진단 | `diagnoseEcountIntegration` | 문제 발생 시 필요 |
| 이카운트 계정설정 | `setupEcountCredentials` | 토큰 갱신 시 필요 |

### 변경 파일
- `menu.gs` L57~76: 메뉴 항목 4개 제거

---

## 2026-06-26 07:56 (KST) — 스냅샷 탭 이름 변경

### 변경
- `판매현황_스냅샷` → `판매현황_임시기록`
- 대리공급_임시기록과 네이밍 통일

### 변경 파일
- `_partnerExclusivePush.gs` L5498: `_SNAPSHOT_TAB_NAME_` 상수 값 변경
- 허브 시트에서 기존 탭 이름 수동 변경 필요

---

## 2026-06-26 08:47 (KST) — "대리발주 마감" → "대리공급 마감" 이름 변경

### 변경 (`_partnerWebApp.gs`)
- L1097: 함수 주석
- L1102, L1108: Logger 로그
- L1119, L1122~1123: Chat 알림 메시지
- L1342: 트리거 라벨

---

## 2026-06-26 16:43 (KST) — 발주 및 송장조회 자동입력 속도 최적화

### 문제
- C열(코드) 입력 시 품목명/단가 자동입력이 느려 사용자가 인지 못함
- 원인: 뷰어 탭 3000~5000행을 매번 순차탐색 (O(N×M))

### 변경 (`partnerOnEditSource.gs`)
- 뷰어 데이터 읽은 후 **해시맵** 구축 (코드 → {품목명, 상태, 단가})
- 순차탐색 for 루프 → `codeMap[inputCode]` O(1) 조회로 교체

### 효과
| 시나리오 | 기존 | 최적화 후 |
|---|---|---|
| 1행 입력 | ~2~3초 | ~1~2초 |
| 10행 붙여넣기 | ~5~8초 | ~2~3초 |
| 50행 붙여넣기 | ~15~30초 | ~3~4초 |

### 주의
- `clasp push` 후 **스크립트 재설치** 필요 (업체 시트 onEdit 반영)

---

## 2026-06-26 18:41 (KST) — 협력업체 시스템 성능 최적화 (3건)

### ① 임시기록 송장수집 배치화 (`_partnerOrders.gs` L3950~3980)
- 루프 내 setValue 3회/건 → 배열 수정 + setValues 2회 + RangeList 1회
- **50건 기준: 25초 → 3~5초**

### ② 폐기송장 허브 갱신 배치화 (`_partnerOrders.gs` L3535~3560)
- 루프 내 setValue 3회/건 → M/N/O 열 배치 setValues 1회 + RangeList 1회
- **30건 기준: 15초 → 3초**

### ③ 거래처코드 매핑 캐시 + 루프 통합 (`_partnerOrders.gs` L3085~3170)
- ①③ 동일 파일 2회 openById → 1회 루프로 통합 (openById 절반 감소)
- CacheService 캐시 추가 (TTL 6시간)
- **20개 업체 기준: 40초 → 0.5초 (캐시 히트 시)**

---

## 2026-06-26 21:20 (KST) — 세트분리 시트 독립 구조 분석

### 배경
- 세트분리 시트(`1vWdJgmbW_Gwm_2b1pP8mVBxpfYBbUiAduSwkStXxs0Y`)는 Pack2U와 **별도로 독립 운영**되는 시트
- 판매현황을 세트분리하고 로젠/대리발송/합배송으로 분류하는 역할
- Pack2U는 이 시트를 참조하여 데이터를 가져다 사용

### 분석 결과 (코드 역추적 기반)

**역추적으로 파악된 탭 구조 (7개):**

| 탭명 | GID | 역할 |
|------|-----|------|
| 대리공급업체 발주 | `1981160530` | 대리발송할 주문 목록 (D열 prefix로 업체 분기) |
| 판매현황 | - | 판매 단가·수취인·전화·주소 정보 |
| 합배송 전용 | `1403770726` | 합배송 대상 등록 (이름+전화+Q열UID) |
| 도서산간 | `1971071523` | 도서산간 배송비 대상 UID 목록 (P열) |
| 사방넷_송장매칭 | - | Pack2U가 기록 — 비협력업체 송장 결과 |
| (GID:1228263901) | `1228263901` | ★ 미확인 — 메인 세트분리 작업 탭 추정 |
| 사방넷 전용 | `1445333640` | 사방넷 주문 전용 (보조 소스) |

**세트분리 시트의 추정 역할:**
1. 쇼핑몰 판매 주문을 받아 세트 상품을 구성품 단위로 분리
2. 분리된 건을 배송 방식(로젠/대리발송/합배송)에 따라 분류
3. Pack2U 시스템이 각 탭을 읽어서 처리

**Pack2U 연동 관계:**
- **읽기**: 대리공급업체 발주, 판매현황, 합배송 전용, 도서산간
- **쓰기**: 대리공급업체 발주 V열(송장 역기록), P열(UID 자동생성), 사방넷_송장매칭 탭

### ⚠️ 직접 확인 필요 사항
1. GID `1228263901` 탭의 정체 (메인 세트분리 탭?)
2. 세트분리 수식/로직 (수식? Apps Script? 수동?)
3. 로젠/대리발송 분기가 시트 자체에서 이루어지는지
4. 합배송 전용 탭 데이터가 수동입력인지 자동생성인지
5. 세트분리 시트에 자체 Apps Script가 있는지

### 산출물
- [세트분리시트_주문분리시스템_정리서.md](file:///C:/Users/x299/.gemini/antigravity-ide/brain/46c9677f-341d-45f5-a726-d00504169d1f/20260626_세트분리시트_주문분리시스템_정리서.md)
