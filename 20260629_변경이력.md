# 변경이력 (2026-06-29)

## 2026-06-29 09:49 (KST) — 일일마감 저장 날짜: 전날(매출일) 기준으로 변경

### 개요
- **자동 실행(05:00 트리거)**: 마감 파일명을 전날(실제 매출 발생일) 날짜로 저장
  - 예: 6월 29일 05:00 실행 → `일일마감_(2026-06-28)` 파일명으로 저장
- **수동 실행(메뉴)**: 기존과 동일하게 당일 날짜로 저장 (변경 없음)

### 변경 파일

| 파일 | 변경 내용 |
|---|---|
| `_partnerExclusivePush.gs` | `_pep_archiveUnifiedDaily_(targetDateStr)` — 선택적 날짜 파라미터 추가. 전달 시 해당 날짜로 아카이브 파일명 및 중복 체크 수행, 미전달 시 기존처럼 당일 날짜 사용 |
| `_partnerWebApp.gs` | `_pep_unifiedDailyArchiveScheduled_` — 전날 날짜(`yesterdayStr`) 계산 후 `_pep_archiveUnifiedDaily_(yesterdayStr)` 호출. 주말 체크도 전날(매출일) 기준으로 변경. 소급 확인 d=2부터 시작 (전날은 메인 대상) |

### 상세 변경 사항

#### `_partnerExclusivePush.gs`
- **L5790**: `function _pep_archiveUnifiedDaily_(targetDateStr)` — 파라미터 추가
- **L5800**: `var archiveDate = targetDateStr || Utilities.formatDate(...)` — 전달 날짜 or 당일
- **L5874**: 중복 체크 시 `archiveDate` 사용 (기존 `todayStr`)
- **L6007**: 아카이브 파일명 `archiveDate` 사용 (기존 `today`)

#### `_partnerWebApp.gs`
- **L324~328**: `yesterday`, `yesterdayStr` 계산 추가
- **L329**: 아카이브 존재 확인 → `yesterdayStr` 기준
- **L342~344**: 소급 확인 `d=2`부터 (전날은 ①에서 처리)
- **L363~367**: 주말 체크 → 전날(`yesterdayDow`) 기준
- **L386**: `_pep_archiveUnifiedDaily_(yesterdayStr)` 호출

### 영향 범위
- 수동 실행(`partnerUnifiedDailyArchiveManual`) → **영향 없음** (파라미터 없이 호출 → 당일 날짜 사용)
- 하위호환 래퍼(`_pep_archiveLozenTemp_`) → **영향 없음** (파라미터 없이 호출)
- 자동 실행(05:00 트리거) → **전날 날짜로 저장됨**

---

## 2026-06-29 16:18 (KST) — 임시기록 → 전용양식 Push 메뉴 복원

### 개요
- 메뉴 v5.0 재구성(2026-06-23) 시 누락된 `📋 임시기록 → 전용양식 Push` 메뉴 항목 복원
- 함수(`partnerPushFromTempTabToExclusiveOwner` → `partnerPushFromTempTabToExclusive`)는 기존에 존재했으나 메뉴에서만 빠져있었음

### 변경 파일
| 파일 | 변경 내용 |
|---|---|
| `_partnerMenu.gs` | 22행: 3️⃣ 대리공급 Push 아래에 `📋 임시기록 → 전용양식 Push` 메뉴 항목 추가 |

---

## 2026-06-29 16:41 (KST) — 카카오 송장매칭 수취인 열 인식 오류 수정

### 원인
- 제이엠(JM) 등 일부 업체 전용양식의 헤더가 `수취주`로 되어 있으나, 수취인 열 자동 탐지 KEYWORDS에 포함되지 않아 "수취인 열 없음" 오류 발생

### 수정
- KEYWORDS 배열에 `"수취주"` 추가

### 변경 파일
| 파일 | 변경 내용 |
|---|---|
| `InvoiceMatch_협력업체용.gs` | 202행: KEYWORDS에 `"수취주"` 추가 |
| `_partnerWebApp.gs` | 858행: KEYWORDS에 `"수취주"` 추가 |

---

## 2026-06-29 18:35 (KST) — 발주 및 송장조회 탭 자동입력 속도 대폭 개선 (CacheService)

### 문제
- 업체가 C열(이카운트코드) 입력 시 품목명(D열)/단가(L열) 자동입력이 5분 이상 걸리거나 미적용
- **원인**: 매 편집마다 단가조회 탭의 3,600행 IMPORTRANGE 수식 데이터를 전체 getValues()로 읽음 → 5~30초 소요
- onEdit simple trigger 30초 제한 초과 시 스크립트 강제 종료 → 자동입력 실패

### 해결
- `CacheService.getScriptCache()`를 활용하여 단가 코드맵을 10분간 캐싱
- **첫 편집 시**: 기존과 동일하게 단가조회 탭 전체 읽기 (1회만 느림) + 캐시 저장
- **이후 10분간**: 캐시에서 즉시 로드 (약 0.05초) → 30초 제한 절대 초과 안 함
- 100KB CacheService 제한 대비 자동 분할 저장/읽기 구현

### 변경 파일
| 파일 | 변경 내용 |
|---|---|
| `partnerOnEditSource.gs` | 112~198행: CacheService 캐싱 로직 추가 (1단계: 캐시 읽기, 2단계: 캐시 미스 시 직접 읽기+저장). vData.length → codeMapSize 변수 교체 |

### 기술 상세
- 캐시 키: `P2U_VM_M` (메타), `P2U_VM_0` ~ `P2U_VM_N` (데이터 청크)
- TTL: 600초 (10분)
- 분할 기준: 90KB (100KB 제한에 안전 마진)
- 폴백: CacheService 에러 시 기존 getValues() 동작 100% 보존

### ⚠ 배포 시 주의
- `clasp push` 후 **스크립트 재설치** 필수 실행 (업체 시트 onEdit 코드 주입 필요)

---

## 2026-06-29 21:50 (KST) — 전화주문 송장매칭 + 미매칭 기록 구현

### 문제
- 전화주문(25~35%)은 O열(수하인/사방넷번호)이 비어있어 스냅샷에 저장되지 않음
- → 일일마감에서 완전 누락 (매칭 커버리지 ~70%)

### 해결 (3가지 변경)

| 변경 | 내용 | 효과 |
|---|---|---|
| 스냅샷 저장 | O열 비어있으면 J열(가게명)→O열 복사 + "TEL:P열(전화번호)" 매칭키 | 전화주문 스냅샷 저장 |
| 로젠 2차 매칭 | E열(주문번호) 비어있으면 "TEL:M열(전화번호)" 키 생성 | 전화주문 송장 연결 |
| 미매칭 기록 | 매칭 실패해도 송장 빈칸+출처"미매칭"으로 기록 | 100% 커버리지 |

### 변경 파일
| 파일 | 변경 내용 |
|---|---|
| `_partnerExclusivePush.gs` | 5643~5685행: 스냅샷 저장 시 O열 비어있으면 J열→O열 복사 + TEL:P열 매칭키 |
| `_partnerExclusivePush.gs` | 5823~5856행: 로젠 A~M(13열) 읽기, E열 없으면 TEL:M열 2차 키 |
| `_partnerExclusivePush.gs` | 5978~5990행: 미매칭도 기록 (송장빈칸 + 출처"미매칭") |
| `_partnerExclusivePush.gs` | 5905행: _lozenPhoneCount_ 카운터 추가 |
| `_partnerExclusivePush.gs` | 6011~6013행: 로젠(전화) 출처 건수 추적 |
| `_partnerExclusivePush.gs` | 6105~6115행: 출처별 배경색 (로젠(전화)=하늘색, 미매칭=주황, 기타=보라) |
| `_partnerWebApp.gs` | 388~407행: 로그/Chat 알림에 로젠(전화) 건수 추가 |

### 예상 효과
- 일일마감 커버리지: ~70% → **~98%+**
- 단골 분석 데이터: 전화주문 고객(J열 가게명 + P열 전화번호) 확보
