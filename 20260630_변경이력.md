# 변경이력 (2026-06-30)

## 2026-06-30 07:50 (KST) — 전화주문 일일마감 누락 완전 해결

### 문제
- 전화주문(일 30~50건)이 일일마감에 전혀 포함되지 않음
- **근본 원인**: 전화주문은 O열(수하인/사방넷번호)이 비어있고, P열(전화번호)도 비어있는 경우가 있어 매칭키를 생성하지 못하고 `continue`로 스킵됨
- 6/29에 추가한 TEL:전화번호 매칭키도 P열이 비어있으면 무효
- 추가 문제: 동일 전화번호로 여러 건 주문 시 첫 건만 처리되고 나머지 스킵

### 해결 (2가지 변경)

| 변경 | 내용 | 효과 |
|---|---|---|
| 스냅샷 무조건 저장 | 매칭키 없어도 행 데이터 해시 기반 폴백 키(`FB:`) 생성 → **판매현황 모든 행 저장** | 전화주문 100% 스냅샷 포함 |
| 동일 전화번호 순번 | 같은 TEL: 키에 `#2`, `#3` 순번 추가 (스냅샷+로젠 송장맵 양쪽) | 동일 전화번호 복수 주문 지원 |

### 변경 파일
| 파일 | 변경 내용 |
|---|---|
| `_partnerExclusivePush.gs` | 5643~5710행: 스냅샷 저장 시 `if (!matchKey) continue;` 제거 → 폴백 키(`FB:행데이터해시`) 생성. 동일 키 중복 시 순번(`#2`,`#3`) 추가 |
| `_partnerExclusivePush.gs` | 5870~5882행: 로젠 송장맵에서 동일 전화번호 복수 주문 지원 (`TEL:전화번호#2`, `#3` 순번 매핑) |

### 동작 변경 전후 비교

**Before:**
```
판매현황 100행 → 스냅샷: O열+P열 있는 70행만 저장 → 일일마감 70행
(전화주문 30행 누락)
```

**After:**
```
판매현황 100행 → 스냅샷: 모든 100행 저장 → 일일마감 100행
(전화주문도 미매칭 or 송장매칭으로 모두 포함)
```

### 영향 범위
- 대리공급 Push 시 호출되는 `_pep_saveSnapshotToHub_` → 판매현황 모든 행 저장
- 일일마감 시 `_pep_archiveUnifiedDaily_` → 변경 없음 (미매칭 기록은 이미 6/29에 구현됨)
- 기존 사방넷 주문 처리 → 영향 없음 (O열 매칭키 로직 동일)

---

## 2026-06-30 08:10 (KST) — 판매현황 요약행("계"/"합계") 스냅샷 제외

### 문제
- 판매현황 맨 아래 "2026/06 계", "합계" 요약행이 스냅샷에 포함되어 일일마감 합계가 3배로 뻥튀기

### 원인
- "계"/"합계" 텍스트는 A~B열에 있으나, 스냅샷은 C~Q열만 읽어서 필터 불가

### 해결
- A~B열을 별도로 읽어서 "계" 또는 "합계"가 포함된 행 스킵

### 변경 파일
| 파일 | 변경 내용 |
|---|---|
| `_partnerExclusivePush.gs` | 5578행: A~B열 별도 읽기(`abData`) 추가. 5652~5656행: A/B열 기반 요약행 필터 |

---

## 2026-06-30 08:17 (KST) — 협력업체_발주허브 초기화: 대리공급 마감으로 이관

### 문제
- 기존 일괄 마감(`partnerDailyArchiveAll`) 삭제 후, 발주허브 초기화(`_pms_cleanupHub_`)가 호출되지 않아 허브에 데이터가 무한 누적

### 해결
- 대리공급 마감(`_pea_core_`) 끝에 발주허브 초기화 로직 추가
- 송장번호 있는 행(N열, 마감 완료) → 삭제, 송장 없는 행 → 유지
- 완료 알림에 "발주허브 정리: N건" 표시 추가

### 변경 파일
| 파일 | 변경 내용 |
|---|---|
| `_partnerExclusiveArchive.gs` | 285~319행: 발주허브 초기화 로직 추가 (N열 송장번호 기준) |
| `_partnerExclusiveArchive.gs` | 63행: UI 알림에 "발주허브 정리" 건수 추가 |

---

## 2026-06-30 09:08 (KST) — JM 전용양식 우편번호 앞 0 보존

### 문제
- 우편번호(L열)가 숫자로 인식되어 앞자리 0 삭제 (예: "06123" → "6123")

### 해결
- L열 텍스트 형식(`setNumberFormat("@")`) 설정 추가 (3곳)
- 우편번호 값에 5자리 패딩 적용 (2곳)

### 변경 파일
| 파일 | 변경 내용 |
|---|---|
| `_partnerExclusivePush.gs` | 1503행: Push 시 우편번호 5자리 패딩 |
| `_partnerExclusivePush.gs` | 1580행: JM Push 배치 쓰기 시 L열 텍스트 형식 설정 |
| `_partnerExclusivePush.gs` | 4853행: 배치 채우기에서도 5자리 패딩 |
| `_partnerExclusivePush.gs` | 4868행: 배치 채우기 시 L열 텍스트 형식 설정 |
| `_partnerExclusivePush.gs` | 4929행: 탭 초기화 시 L열 텍스트 형식 설정 |

---

## 2026-06-30 11:03 (KST) — 자동 마감 트리거 문제 수정 + 디버깅 강화

### 문제
- 대리공급 마감 자동 트리거(07:20)에서 전용양식 데이터가 마감탭으로 이동되지 않음

### 원인 (잠재적)
- `_pea_core_` 내부 임시기록/발주허브 초기화 시 `getActiveSpreadsheet()` 사용
- 시간 기반 트리거에서는 `getActiveSpreadsheet()` = **null** → 초기화 실패
- 초기화 실패가 전체 실행에 영향을 미쳤을 가능성

### 해결
- `getActiveSpreadsheet()` → `openById(_PT.HUB_ID)` 변경 (2곳)
- 트리거 함수에 상세 디버깅 로그 추가 (파일 수, 에러 내역, 스택 트레이스)
- Chat 알림에 파일 수/에러 내역 포함

### 변경 파일
| 파일 | 변경 내용 |
|---|---|
| `_partnerExclusiveArchive.gs` | 275행: `getActiveSpreadsheet()` → `openById(_PT.HUB_ID)` |
| `_partnerExclusiveArchive.gs` | 291행: `getActiveSpreadsheet()` → `openById(_PT.HUB_ID)` |
| `_partnerWebApp.gs` | 1115~1155행: 트리거 함수 상세 로깅 추가 |

---

## 2026-06-30 13:55 (KST) — ★ 마감 이동 후 품목명/단가 소실 버그 수정

### 문제
- 월별 정산 마감 이동 후, 잔류 행의 D열(품목명)과 L열(단가)이 빈 상태로 남음
- "이전보다 더 안 좋아졌다" → 값 기반 전환 이후 발생한 퇴행 버그

### 원인
- `_pms_processOneFile_`의 마감 이동 로직:
  1. **351행**: `clearContent()`로 발주탭 전체 지움
  2. **354행**: B~C열만 복원
  3. **358행**: E~K열만 복원
  4. **362행**: M열 이후만 복원
  5. **A열**: `_pt_healOrderSpillFormulas`에서 수식 재설정
  6. ❌ **D열(품목명)**: 기존 ARRAYFORMULA 시절에는 A열 수식이 spill → D열 자동 채움이었으나, **값 기반 전환 후** 복원 코드 없음
  7. ❌ **L열(단가)**: 동일 문제

### 해결
- 잔류 데이터 복원 시 D열(index 3)과 L열(index 11) 값도 명시적으로 복원
- B~C → **D** → E~K → **L** → M~ 순서로 완전 복원

### 변경 파일
| 파일 | 변경 내용 |
|---|---|
| `_partnerMonthlySettle.gs` | 353~377행: D열/L열 값 복원 코드 추가 |

---

## 2026-06-30 14:12 (KST) — onEdit 캐시 빈 데이터 방지

### 변경 파일
| 파일 | 변경 내용 |
|---|---|
| `partnerOnEditSource.gs` | 빈 캐시 방지 + return 제거 + 빈 캐시 저장 차단 |

> ⚠️ **스크립트 재설치 필요** → 14:44 완료

---

## 2026-06-30 14:44 (KST) — D/L 백필 로직 통합 + 메뉴 라벨 수정

### 변경 내용
- D/L 백필 로직을 공용 함수 `_pt_backfillOrderDL()` 으로 추출
- 복구 메뉴의 인라인 백필 코드 (~50줄) → 공용 함수 1줄 호출로 대체
- 마감 이동 후에도 백필 자동 호출 추가
- 메뉴/다이얼로그 라벨: `수식·단가 복구` → `수식·품목명·단가 복구`

### 변경 파일
| 파일 | 변경 내용 |
|---|---|
| `_partnerHelpers.gs` | `_pt_backfillOrderDL()` 공용 함수 신규 |
| `_partnerMenu.gs` | 인라인 백필 → 공용 함수 호출, 메뉴 라벨 변경 |
| `_partnerMonthlySettle.gs` | heal 후 `_pt_backfillOrderDL()` 호출 추가 |
| `repairDialog.html` | 다이얼로그 라벨 `수식·품목명·단가 복구` 변경 |

---

## 2026-06-30 15:05 (KST) — ⚡ 빠른 품목명·단가 채우기 전용 버튼 추가

### 변경 내용
- 복구 다이얼로그에 **⚡ 빠른 품목명·단가 채우기** 전용 카테고리 추가
- 수식 복구 없이 D/L 백필만 실행 → **20분 → 2~3분** 소요
- 기존 '수식·품목명·단가 복구' → '수식 전체 복구'로 이름 변경 (역할 명확화)
- 결과 카운트: ✔(빈 항목 없음)도 성공으로 집계

### 변경 파일
| 파일 | 변경 내용 |
|---|---|
| `_partnerMenu.gs` | `_repairBatch_backfillDL_()` 함수 + 디스패치 분기 추가 |
| `repairDialog.html` | ⚡ 빠른 백필 UI 카테고리 + 기존 수식 복구 설명 수정 |

---

## 2026-06-30 16:10 (KST) — 🔧 복구시스템 메뉴 세분화 + 자동 연속 실행

### 변경 내용

#### 1. 복구 카테고리 세분화 (4개 → 6개)
| 기존 | 변경 후 | 시간 |
|---|---|---|
| 📊 수식 전체 복구 (뷰어+발주 묶음) | 📊 단가조회 수식 복구 + 📝 발주탭 수식 복원 | 각 ~5분 |
| 📋 헤더·양식 복구 | 📋 헤더·서식 재적용 | ~5분 |
| 📝 스크립트 재설치 | 🔧 스크립트 재설치 (자동 연속) | 자동 |
| 📑 탭 재생성 | 📑 탭 재생성 | 최종 |
| ⚡ 빠른 백필 | ⚡ 빠른 품목명·단가 채우기 | ~2분 |

#### 2. 스크립트 재설치 자동 연속 실행
- 37개 한번 실행 시 6분 제한 초과 → **10개씩 분할 실행**
- PropertiesService에 큐 저장 → 1분 후 트리거 자동 실행 → 완료 시 Chat 알림

#### 3. 불필요한 탭 패스 (속도 최적화)
- `_pt_findViewerSheet`: `getSheets()` 순회 대신 `getSheetByName()` 직접 시도 우선
- 각 복구 함수: 필요한 탭(1~2개)만 접근, 나머지 무시

### 변경 파일
| 파일 | 변경 내용 |
|---|---|
| `_partnerHelpers.gs` | `_pt_findViewerSheet` 최적화 (직접 이름 시도 우선) |
| `_partnerMenu.gs` | `_repairBatch_viewerFormula_` + `_repairBatch_orderFormula_` 분리 |
| `_partnerMenu.gs` | `_repairBatch_script_` 10개씩 자동 연속 + 트리거 체이닝 |
| `_partnerMenu.gs` | 디스패처에 viewerFormula/orderFormula 케이스 추가 |
| `repairDialog.html` | 6개 카테고리 UI + 3열 그리드 + 소요시간 뱃지 |

---

## 2026-06-30 16:44 (KST) — 📧 Gmail 송장수집 Chat 알림 상세화

### 변경 내용
- Chat 카드 제목: 매칭 결과에 따라 아이콘 변경 (✅/⚠️/📭)
- Chat 카드 부제목: 메일/파싱/매칭/미매칭 건수 한 줄 요약
- keyValues: 각 항목에 이모지 + 미매칭 시 수취인(송장번호) 상세 표시 (최대 5건)
- 오류 발생 시 오류 내용도 카드에 포함

### 변경 파일
| 파일 | 변경 내용 |
|---|---|
| `_partnerGmailInvoice.gs` | 트리거 Chat 알림 상세화 (title/subtitle/keyValues 개선) |
