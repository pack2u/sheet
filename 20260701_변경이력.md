# 변경이력 (2026-07-01)

## 2026-07-01 23:10 발주 수집 및 onEdit 성능 획기적 개선 (고속 패치 v2)

### 1. 발주 수집 (partnerCollectOrders) 최적화
- **스마트 수집 도입**: `DriveApp.getLastUpdated()`를 활용하여 마지막 수집 이후 수정된 파일만 골라서 수집.
  - 전체 수집(3~5분) → 스마트 수집(30초 내외)으로 시간 단축.
- **수동 선택**: 수집 시작 시 '스마트 수집(예)' 또는 '전체 수집(아니오)' 선택 팝업 제공.
- **상태 관리**: `LAST_ORDER_COLLECT_TIME`을 스크립트 속성에 저장하여 정확한 차분 수집 구현.

### 2. 업체 시트 onEdit 성능 2차 최적화 (v2)
- **Not Found 캐싱**: 뷰어(단가조회)에 없는 코드 입력 시에도 캐싱하여 반복적인 전체 탭 로드 방지.
- **TextFinder 단일 행 검색**: 캐시 미스 시에도 전체 탭 로드 대신 `createTextFinder`로 즉시 검색하여 딜레이 최소화.
- **API 호출 최소화**: 기존 값과 변경될 값(품목명, 단가, 적요)이 동일한 경우 `setValues` 호출 스킵.
- **구조 최적화**: 탭 조회 캐싱 및 불필요한 루프 제거.

### 수정 파일
| 파일 | 변경 |
|---|---|
| `_partnerDeploy.gs` | `_pt_listFiles`에 `modified` 필드 추가 |
| `_partnerOrders.gs` | `partnerCollectOrders` 스마트 수집 로직 및 차분 수집 시간 저장 |
| `partnerOnEditSource.gs` | onEdit v2 고속 패치 (Not Found 캐싱, TextFinder 연동) |

> ⚠️ **onEdit 개선 사항**을 업체 시트에 반영하려면 **💼 협력업체 관리 > 🛠️ 협력업체 복구시스템 > 스크립트 재설치** 메뉴를 실행해야 합니다.

---

## 2026-07-01 21:25 업체 시트 DB 단가 메뉴 주입

### 변경
- 업체 시트 onOpen에 `🗄️ DB 단가` 메뉴 추가 (코드 주입)
  - 🔍 DB 품목 검색 — 웹앱 API로 즉시 검색
  - 🔄 DB 단가 새로고침 — 그룹별 전체 단가 갱신 (오류 수정: `_findViewerTab_` 참조 문제 해결)
  - ⚙️ 모드 전환 — 설정탭 C20에 기존/DB 토글

### 수정 파일
| 파일 | 변경 |
|---|---|
| `priceManager.gs` | onOpen 메뉴 + dbPriceCode 함수 주입, _findViewerTab_ 참조 오류 수정 |
| `_partnerWebAppAPI.gs` | productSearch, vendorProducts API 엔드포인트 추가 |
| `_secrets.gs` | HUB_WEBAPP_URL 저장 |

> ⚠️ 업체 시트에 반영하려면 **스크립트 재설치** 필요

---

## 2026-07-01 20:35 Supabase DB Phase 1.5 — 전체 상품정보 동기화 + 단가 이력

### 완료 내역
- **상품 동기화**: 3,436개 전품목 DB 저장 완료 (배치 50건 + 실패 시 1건씩 재시도)
- **확장 컬럼**: warehouse, shop_name, supplier, purchase_price, retail_price, hub_base_price, hub_future_price
- **전체 데이터**: `all_data JSONB` — 상품정보 시트 전체 열 JSON 보존
- **그룹별 단가**: `group_prices JSONB` — AG~BM열 단가 데이터
- **단가 이력 테이블**: `price_history` — 가격 변경 자동 추적 (트리거 기반)
- **단가 조회 함수**: `get_price_at_date()` — SQL 함수로 과거 단가 조회

### 신규 메뉴
- `🔍 DB 품목 검색` — 코드/품목명 즉시 검색 (검색시간 표시)
- `📊 단가 이력 조회` — 품목별 가격 변경 내역

### 수정 파일
| 파일 | 변경 |
|---|---|
| `_partnerSupabase.gs` | 전체 열 동기화, 배치 50, 재시도 로직, 검색/이력 함수 |
| `_partnerMenu.gs` | DB 검색/이력 메뉴 추가 |
| `supabase_migration_v2.sql` | 컬럼 확장 + price_history + 트리거 + 함수 |

---

## 2026-07-01 17:30 Supabase DB 전환 Phase 1 시작
- **신규 파일**: `_partnerSupabase.gs` — DB 연동 헬퍼 (CRUD + 품목/발주 동기화)
- **신규 파일**: `supabase_migration.sql` — 테이블 생성 SQL (vendors, products_hub, orders, invoices)
- **수정**: `_secrets.gs` — Supabase 서비스 키 추가
- **수정**: `_partnerMenu.gs` — `💼 협력업체 관리 > 🗄️ DB 동기화` 서브메뉴 추가
  - 🔗 DB 연결 테스트
  - 📦 상품정보 → DB 동기화
  - 📋 발주허브 → DB 동기화
- **방식**: 하이브리드 (메뉴 수동 동기화 + onEdit 자동 동기화 예정)
- **다음 단계**: Supabase 대시보드에서 SQL 실행 → DB 연결 테스트 → 데이터 이관


## 2026-07-01 17:01 스크립트 재설치 429 에러 방지
- **대상**: `_partnerMenu.gs` (494~563행)
- **변경**: 배치 크기 10→3, 트리거 간격 1분→3분, 429 재시도 대기 5초→15초, 건별 대기 3초→8초
- **이유**: 재설치 실행 시 Google API 429 과부하로 진행 불가 → 느리지만 확실하게 완료되도록 조절


## 🚨 2026-07-01 16:33 긴급 — 카카오 송장 매칭 장애 복구
- **원인**: `_partnerLibrary.gs`의 `applyInvoiceMatches(matchData)` 함수가 기존 `_partnerExclusivePush.gs`의 `applyInvoiceMatches(fileId, matchesJson)`과 **이름 충돌**
  - GAS가 Library 버전(1파라미터)을 호출 → fileId를 JSON.parse 시도 → `Unexpected non-whitespace character after JSON` 에러
  - 전체 업체 카카오 송장 매칭 불가 상태
- **대상**: `_partnerLibrary.gs` (배포 보류 예정이었으나 clasp push에 포함됨)
- **조치**: `_partnerLibrary.gs` 내용 전체 비활성화 (주석만 남김)
- **배포**: clasp push 완료 (54개 파일)


## 2026-07-01 15:30 웹앱 전환 프로젝트 1단계 시작
- **신규 파일**: `_partnerWebAppAPI.gs` — 웹앱 대시보드 연동용 JSON API 엔드포인트
  - `doGet(e)` — action 파라미터로 라우팅 (dashboard/orders/vendors/stats)
  - 허브 시트 데이터를 JSON으로 반환 → Next.js 웹앱에서 호출
- **프로젝트**: `F:\Pack2U_협력업체웹앱\` — Next.js App Router + Tailwind CSS
  - 대시보드 (통계카드 + 업체랭킹 + 최근발주)
  - 발주 관리 (검색/필터), 업체 현황, 송장/정산 스켈레톤
  - Supabase 클라이언트 설정 (쇼핑앱과 동일 DB 공유)
  - DB 스키마 SQL 작성 (docs/schema.sql, 7개 테이블)
- **배포 필요**: clasp push 후 GAS 웹앱 배포 (새 배포)

## 2026-07-01 15:00 M열(고유ID) 보호 제거
- **대상**: `partnerOnEditSource.gs`
- **내용**: M열 clearContent 보호 로직 완전 제거 — 관리자가 고유ID 수정/붙여넣기 가능하도록
- **배포**: clasp push 완료, 업체 시트 반영은 스크립트 재설치 필요

## 2026-07-01 14:56 GEMINI.md 메뉴 구조 수정
- **대상**: `GEMINI.md`
- **내용**: 존재하지 않는 `🔧 관리/AS 도구` 서브메뉴 제거, 실제 코드 기반 정확한 메뉴 구조로 교체


## 2026-07-01 08:07 (KST) — 트리거 문제 2건 수정

### 문제 1: 대리판매 마감(07시) 트리거 간헐적 실패

| 원인 | 위치 | 상세 |
|---|---|---|
| `getActiveSpreadsheet()` null | `_partnerMonthlySettle.gs` 421행 | 시간 기반 트리거에서 null 반환 → 허브 정리 실패 |
| Lock 충돌 | `_partnerMonthlySettle.gs` 106행 | 05시 일괄마감이 6분 초과 시 07시 Lock 획득 실패 → 15초 대기 후 단순 return |

### 해결

| 수정 | 파일 | 내용 |
|---|---|---|
| 허브 접근 수정 | `_partnerMonthlySettle.gs` 421행 | `getActiveSpreadsheet()` → fallback `openById(_PT.HUB_ID)` |
| Lock 재시도 강화 | `_partnerMonthlySettle.gs` 104~120행 | `tryLock(15000)` 1회 → `tryLock(30000)` 3회 재시도 (총 90초 대기) + Lock 실패 시 Chat 알림 |

---

### 문제 2: 02시 이카운트 동기화 트리거가 아직 작동

| 원인 | 상세 |
|---|---|
| 이중 트리거 | `ecount.gs`의 `setupDailyTrigger()` / `initializeEcountForSheet()` 실행 시 `runDailyInventoryAdjustBatch` 01:20 트리거 등이 통합 트리거와 별도로 중복 생성됨 |
| 통합 트리거 비인식 | `ecount.gs`가 `_partnerWebApp.gs`의 통합 트리거 존재를 인식하지 못함 |

### 해결

| 수정 | 파일 | 내용 |
|---|---|---|
| 통합 트리거 감지 가드 | `ecount.gs` 554~565행 | `setupDailyTrigger()` 시작에 통합 트리거 감지 → 존재 시 설치 차단 + 경고 알림 |
| 초기화 가드 | `ecount.gs` 1182~1187행 | `initializeEcountForSheet()` 트리거 설치 섹션에 통합 트리거 감지 → 존재 시 스킵 |
| 감지 헬퍼 | `ecount.gs` 끝 | `_ecount_hasUnifiedTriggers_()` 신규 — 통합 트리거 함수 3개 이상 감지 시 true |

### 변경 파일 요약

| 파일 | 변경 |
|---|---|
| `_partnerMonthlySettle.gs` | `_pms_cleanupHub_`: null 방지, `partnerArchiveToMonthlySilent_`: Lock 3회 재시도 |
| `ecount.gs` | `setupDailyTrigger()`, `initializeEcountForSheet()`: 통합 트리거 가드 + `_ecount_hasUnifiedTriggers_()` 헬퍼 |

---

## 2026-07-01 08:17 (KST) — 대리판매 마감 성능 최적화 (993초 → 목표 300초 이하)

### 문제
- 대리판매 월별 마감 소요시간 **993초(16.5분)** → 너무 오래 걸림

### 원인 (파일당 병목)
| 병목 | API 호출 수 | 설명 |
|---|:---:|---|
| 잔류 데이터 분할 복원 | 5회 setValues | B-C, D, E-K, L, M~ 5번 분할 쓰기 |
| 빈 행 서식 초기화 | 7회 setFont* | setBorder + setBackground + setFontColor + setFontSize + setFontWeight + setFontStyle + setFontFamily |
| 뷰어탭 2회 접근 | 2회 | 품목명 맵 구축(232행) + heal 후 백필(413행) |
| flush 2번 | 2회 | 마감탭 쓰기 후 + 잔류 복원 후 |
| 고유ID 열 인덱스 루프 내 반복 | N회 | 행마다 헤더 순회 |

### 해결
| 최적화 | Before | After | 절감 |
|---|---|---|---|
| 잔류 복원 통합 | 5번 setValues | **1번** setValues (B~끝 통합) | -4 API 호출/파일 |
| 서식 초기화 제거 | 7번 setFont* | **삭제** (불필요) | -7 API 호출/파일 |
| 뷰어탭 캐싱 | 2번 접근 | **1번** (함수 시작 시 캐싱) | -1 API 호출/파일 |
| flush 통합 | 2번 | **1번** | -1 API 호출/파일 |
| 고유ID 인덱스 | 루프 내 매번 | **루프 밖 1회** | 루프 절감 |

**예상 효과**: 파일당 API 호출 ~15회 → ~5회 (약 60~70% 감소)

### 변경 파일
| 파일 | 변경 |
|---|---|
| `_partnerMonthlySettle.gs` | `_pms_processOneFile_` 전면 최적화 |

---

## 2026-07-01 08:24 (KST) — 대리공급_임시기록 초기화 조건 개선

### 문제
- 임시기록 탭이 마감 후에도 초기화 안 됨 (데이터 계속 누적)

### 원인
- `_po_clearTempTabInvoicedRowsOnly_()`: **X열(송장번호) 있는 행만 삭제**
- 송장 매칭이 안 된 행(취소 건, 오류 건 등)은 X열이 비어 있어 **영구 잔류**

### 해결
| 조건 | Before | After |
|---|---|---|
| X열 송장 있음 | ✅ 삭제 | ✅ 삭제 |
| X열 송장 없음 + 7일 이상 경과 | ❌ 유지 | ✅ **삭제** |
| X열 송장 없음 + 7일 미만 | ❌ 유지 | ❌ 유지 |

- C열(index 2) `일자-No.` (예: "2026/06/25-12")에서 날짜 추출
- 7일 이상 경과 시 송장 유무와 관계없이 삭제

### 변경 파일
| 파일 | 변경 |
|---|---|
| `_partnerOrders.gs` | `_po_clearTempTabInvoicedRowsOnly_` 7일 경과 행 삭제 조건 추가 |

---

## 2026-07-01 08:33 (KST) — 상태/재고 업데이트 시 단가 배포 자동 연계

### 변경
- `syncStatusOnly` 실행 시 `syncGroupPrices` 완료 후 `updateAllVendorSheets({ silent: true })` 자동 연계
- 메뉴/트리거/수동 어디서든 실행하면 허브 동기화 + 업체 시트 단가 배포가 한 번에 완료
- 메뉴 라벨: `3️⃣ [상태 반영] 판매 상태/재고만 업데이트` → `3️⃣ [통합] 상태/재고/단가 업데이트 + 배포`

### 영향 범위
| 호출 경로 | 효과 |
|---|---|
| 수동 메뉴 3️⃣ | 허브 동기화 + 업체 시트 배포 |
| 트리거 07:00 (`runMorningSyncStatusBatch`) | 아침 동기화 + 업체 시트 배포 |
| 트리거 12:30 (`runDailyHubBatch`) | 점심 동기화 + 업체 시트 배포 |

### 변경 파일
| 파일 | 변경 |
|---|---|
| `priceManager.gs` | `syncStatusOnly` → `updateAllVendorSheets` 자동 연계 추가 |
| `_partnerMenu.gs` | 메뉴 라벨 변경 |

---

## 2026-07-01 09:43 (KST) — 일일마감 주말 날짜 합산 버그 수정

### 문제
- 월요일(6/30) 05:00 실행 시 `yesterday = 6/29(일)` → `일일마감_(2026-06-29)` 파일 생성
- 금~일 3일치 데이터가 일요일 날짜 파일에 모두 합산됨

### 원인
| 실행일 | yesterday | 파일명 | 문제 |
|---|---|---|---|
| 6/28(토) | - | - | 주말 차단 → 미실행 |
| 6/29(일) | - | - | 주말 차단 → 미실행 |
| 6/30(월) | 6/29(일) | 일일마감_(2026-06-29) | ❌ 금~일 합산 |

### 해결
- `yesterday`가 **주말**이면 → **직전 금요일**로 보정
  - 일요일(0) → -2일 → 금요일
  - 토요일(6) → -1일 → 금요일

| 실행일 | yesterday (보정 후) | 파일명 | 결과 |
|---|---|---|---|
| 6/30(월) | 6/27(금) | 일일마감_(2026-06-27) | ✅ 금요일 파일 |

### 변경 파일
| 파일 | 변경 |
|---|---|
| `_partnerWebApp.gs` | `_pep_unifiedDailyArchiveScheduled_` 주말 보정 로직 추가 |

---

## 2026-07-01 09:50 (KST) — 일일마감 시간 전면 변경: 05:00 전날 → 23:30 당일

### 변경 개요
- **실행 시간**: 다음날 05:00 → **당일 23:30**
- **날짜 기준**: yesterday (전날) → **todayStr (당일)**
- **주말 보정**: 제거 (불필요 — 주말은 이미 `_pt_isWeekendBlackout_`으로 차단)

### Before vs After
| 항목 | Before | After |
|---|---|---|
| 실행 시간 | 다음날 05:00 | **당일 23:30** |
| 파일 날짜 | yesterday (전날) | **today (당일)** |
| 주말 보정 | 필요 (월→금 보정) | **불필요** |
| 금요일 마감 | 월 05:00 → 금 날짜 (보정) | **금 23:30 → 금 날짜 (직접)** |
| 코드 복잡도 | 높음 (보정 로직) | **낮음 (직접 날짜)** |

### 검증
| 요일 | 실행 | 파일명 | 결과 |
|---|---|---|---|
| 월 23:30 | ✅ | 일일마감_(월요일) | ✅ |
| 금 23:30 | ✅ | 일일마감_(금요일) | ✅ |
| 토 23:30 | ❌ 주말 차단 | - | ✅ |
| 일 23:30 | ❌ 주말 차단 | - | ✅ |

### 변경 파일
| 파일 | 변경 |
|---|---|
| `_partnerWebApp.gs` | 함수 날짜 로직 (yesterday→today), 트리거 시간 (05:00→23:30), 주석 업데이트 |

> ⚠️ 트리거 재설치 필요: GAS에서 `setupUnifiedDailyArchiveTrigger()` 실행 또는 통합 트리거 재설치

---

## 2026-07-01 10:33 (KST) — 발주 및 송장조회 탭 onEdit 성능 최적화

### 문제
- 자동입력(C→D/L) 기능이 느림 (2~4초)
- 셀 칼라/조건부서식이 가끔 작동 안 됨 (30초 타임아웃)
- 다중행 붙여넣기 시 4~8초 소요

### 최적화 내역 (6가지)

| # | 항목 | Before | After | 절감 |
|---|---|---|---|---|
| 1 | 단가조회 탭 조건부서식 | 매번 재구축 (6회 API) | 배경색만 초기화 (1회) | **83%↓** |
| 2 | 붙여넣기 서식 제거 | 6회 개별 API | `setTextStyle` 2회 | **67%↓** |
| 3 | B열 날짜 읽기 | 별도 getValues (중복) | batchData 재활용 | **1회↓** |
| 4 | 보호열 clearContent | A/D/L/M 4개 (최대 4회) | A/M만 (최대 2회) | **50%↓** |
| 5 | D/L열 쓰기 | 항상 setValues | 변경 감지 후 스킵 | **조건부↓** |
| 6 | 캐시 TTL | 10분 | 30분 | 미스 빈도 **1/3** |

### 시나리오별 예상 효과

| 시나리오 | Before | After |
|---|---|---|
| 단가조회 탭 편집 | 2~5초 | **<0.3초** |
| C열 1행 입력 (캐시 히트) | 2~4초 | **1~2초** |
| 다중행 붙여넣기 | 4~8초 | **2~3초** |

### 변경 파일
| 파일 | 변경 |
|---|---|
| `partnerOnEditSource.gs` | onEdit 코드 전면 최적화 |

> ⚠️ 업체 시트에 반영하려면 **스크립트 재설치** 필요

---

## 2026-07-01 12:50 (KST) — D/L열 보호(clearContent) 복원

### 문제
- 최적화 시 D(품목명)/L(단가) 열의 clearContent를 제거했으나, 이로 인해:
  - D/L 수동 입력 시 "입력 차단" 토스트가 안 뜸
  - D/L 하단의 보이지 않는 콘텐츠(스페이스 등)가 정리되지 않아 자동입력 차단
  - 수동 입력값이 유지되어 자동복구 미작동

### 수정 내용
- D(4), L(12) 열을 보호열(clearContent)에 다시 추가
- D/L 편집 시에도 "입력 차단" 토스트 표시 복원
- D/L은 clearContent 후 반드시 setValues로 복원 (변경 감지 제거, 항상 쓰기)

### 변경 파일
| 파일 | 변경 |
|---|---|
| `partnerOnEditSource.gs` | D/L 보호 복원, 변경 감지 제거 |

> ⚠️ 업체 시트에 반영하려면 **스크립트 재설치** 필요
