# 변경이력 (2026-07-02)

## 2026-07-02 08:17 대리공급 마감이동 결과에 임시기록 초기화 결과 표시 추가

### 배경
- `대리공급_임시기록` 초기화가 동작하지 않는 문제 조사
- 기존 `_pea_core_()` (대리공급 마감이동)에 이미 초기화 코드 존재 (L272~286)
- 그러나 결과 alert에 초기화 건수가 표시되지 않아 동작 여부 확인 불가

### 변경
1. **`_partnerExclusiveArchive.gs`**: 대리공급 마감이동 완료 alert에 `📋 임시기록 정리: 삭제 N건, 유지 N건` 표시 추가
2. **`_partnerMonthlySettle.gs`**: 대리판매 마감에 추가했던 임시기록 초기화 코드 원복 (동작하지 않아 제거)

### 수정 파일
| 파일 | 변경 |
|---|---|
| `_partnerExclusiveArchive.gs` | 마감 결과 alert에 임시기록 초기화 건수 표시 추가 |
| `_partnerMonthlySettle.gs` | 임시기록 초기화 코드 원복 (제거) |

---

## 2026-07-02 08:25 웹앱 전환 2단계 — Next.js 실데이터 연동 전면 개편

### 개요
- 1단계(목업 UI)에서 2단계(Supabase 실데이터 연동)로 전환
- 하이브리드 데이터 소스: Supabase DB 우선 → GAS API 폴백 → 빈 데이터
- 전체 5개 페이지 + 공통 모듈 4개 구현 완료

### 신규 파일
| 파일 | 역할 |
|---|---|
| `lib/queries.ts` | DB 쿼리 모듈 (대시보드/발주/업체/송장/정산) + GAS 폴백 |
| `components/StatusBadge.tsx` | 7가지 상태별 색상 배지 컴포넌트 |
| `components/LoadingSkeleton.tsx` | 스켈레톤 로딩 (카드/테이블/헤더) |
| `components/DataSourceBadge.tsx` | 데이터 소스 인디케이터 (Supabase/GAS/Mock) |

### 수정 파일 (전면 개편)
| 파일 | 변경 |
|---|---|
| `app/page.tsx` | 대시보드: 실데이터 연동 + 60초 자동갱신 + 글래스 이펙트 |
| `app/orders/page.tsx` | 발주 관리: 검색/날짜/상태 필터 + 페이지네이션(50건) |
| `app/vendors/page.tsx` | 업체 현황: 발주 건수/금액 집계 + 활성/비활성 필터 |
| `app/invoices/page.tsx` | 송장 현황: 통계카드 4개 + 매칭/미매칭/배포 필터 |
| `app/settlements/page.tsx` | 정산 관리: 월별 필터 + 업체별 정산 테이블 |

### 빌드 결과
- ✅ TypeScript 컴파일 성공 (3.6초)
- ✅ 정적 페이지 생성 성공 (8/8 페이지)

---

## 2026-07-02 08:42 Supabase DB 스키마 v2 + GAS 동기화 함수 추가

### 개요
- DB 스키마 v2: GAS 동기화 컬럼명과 웹앱 쿼리 컬럼명 통일
- vendors 동기화 함수 신규 구현
- 통합 동기화 메뉴 추가

### 변경
| 파일 | 변경 |
|---|---|
| `docs/schema.sql` | v2: `qty`/`unit_price`/`message`/`note`/`invoice_no` 컬럼명 통일 + anon RLS |
| `_partnerSupabase.gs` | orders 컬럼명 수정 + `syncVendorsToDbOwner()` + `syncAllToDbOwner()` 추가 |
| `_partnerMenu.gs` | DB 동기화 서브메뉴에 업체/통합 동기화 항목 추가 |

### DB 테이블 생성 방법
1. Supabase 대시보드 → SQL Editor
2. `docs/schema.sql` 내용 복사 → 실행
3. GAS 메뉴 → 🗄️ DB 동기화 → 🔄 통합 DB 동기화 실행

---

## 2026-07-02 11:24 웹앱 정적 빌드 및 FTP(SFTP) 배포 완료

### 개요
- Next.js 정적 빌드(Static Export)로 변환하여 FTP 호스팅에 배포
- 라이브 URL: **https://siot.com/pack2u/**

### 설정 변경
| 파일 | 변경 |
|---|---|
| `next.config.ts` | `output: "export"`, `basePath: "/pack2u"`, `trailingSlash: true` 추가 |
| `.env.production` | `.env.local` 복사 (Supabase URL/키 빌드 시 주입) |
| `deploy_pack2u/` | 배포용 정리 폴더 생성 (32개 파일, 1.1MB) |
| `deploy_pack2u/.htaccess` | Apache URL Rewrite 규칙 추가 |

### 배포 정보
| 항목 | 값 |
|---|---|
| 호스트 | siot.com (SFTP 포트 22) |
| 계정 | siot00 |
| 원격 경로 | /siot00/www/pack2u/ |
| 라이브 URL | https://siot.com/pack2u/ |

### 배포된 페이지
| 페이지 | URL |
|---|---|
| 대시보드 | /pack2u/ |
| 발주 관리 | /pack2u/orders/ |
| 업체 현황 | /pack2u/vendors/ |
| 송장 현황 | /pack2u/invoices/ |
| 정산 관리 | /pack2u/settlements/ |

### 재배포 방법
```
1. npm run build (F:\Pack2U_협력업체웹앱\app\)
2. deploy_pack2u 폴더 갱신 (out/ → deploy_pack2u/)
3. FileZilla로 deploy_pack2u/ 내용을 /siot00/www/pack2u/에 업로드
```

---

## 2026-07-02 12:30 호스팅 `_next` 폴더 차단 우회

### 배경
- 카페24 호스팅이 `_`로 시작하는 폴더 접근을 차단 (보안 정책)
- Next.js 기본 빌드의 `_next/` 폴더가 404 반환

### 해결
- `_next/` → `nextstatic/`으로 폴더명 변경
- HTML/JS 내부의 모든 `/_next/` 경로를 `/nextstatic/`으로 치환
- **JS 변수명 `_next`는 보존** (하이픈 포함 이름 → 구문 오류 방지)

### 수정 대상
| 항목 | 변경 |
|---|---|
| `deploy_pack2u/_next/` | → `deploy_pack2u/nextstatic/` |
| HTML 내 경로 | `/pack2u/_next/` → `/pack2u/nextstatic/` |
| JS 내 경로 | `/_next/` → `/nextstatic/` (경로만, 변수명 제외) |

---

## 2026-07-02 13:00 웹앱 GAS 업무 원격 실행 기능 추가

### 개요
- 웹앱 대시보드에서 GAS 업무를 직접 실행할 수 있는 **원격 실행 패널** 추가
- GAS doGet에 `action=run` 엔드포인트 추가
- API 키 인증 + LockService 동시실행 방지

### GAS 측 변경
| 파일 | 변경 |
|---|---|
| `_partnerWebAppAPI.gs` | doGet에 `case "run"` 추가 + `_api_runTask_()` 함수 구현 |

### 지원 작업 (5개)
| 태스크 | GAS 함수 | 예상 시간 |
|---|---|---|
| 1️⃣ 발주 수집 | `partnerCollectOrders()` | 1~3분 |
| 5️⃣ 송장 수집 | `partnerFetchInvoices()` | 1~2분 |
| 7️⃣ 송장 배포 | `partnerPushInvoices()` | 1~2분 |
| 3️⃣ 대리공급 Push | `partnerPushOrdersToExclusiveForms()` | 1~2분 |
| 🔄 DB 동기화 | `syncAllToDbOwner()` | 30초 |

### 웹앱 측 변경
| 파일 | 변경 |
|---|---|
| `lib/gasApi.ts` | [NEW] GAS 원격 실행 모듈 (TaskType/TASKS 정의 + runGasTask) |
| `app/page.tsx` | 대시보드에 ⚡ 업무 실행 패널 + 결과 토스트 추가 |
| `.env.production` | `NEXT_PUBLIC_GAS_API_URL` 추가 |

### 보안
- API 키: `pack2u_run_x7k9m2026`
- LockService: 동시 실행 방지 (2초 타임아웃)
- Google Chat 알림: 실행/에러 시 자동 알림

---

## 2026-07-02 14:02 Supabase DB 자동 동기화 트리거 추가

### 배경
- 웹앱이 Supabase에서 데이터를 읽으므로, GAS 작업 후 DB 동기화 필요
- 기존에는 수동 실행만 가능했음

### 변경
| 파일 | 변경 |
|---|---|
| `_partnerWebApp.gs` | `_trigger_syncDb_()` 함수 추가 + `_ALL_SCHEDULED_TRIGGERS_` 배열에 17:00 등록 |

### 트리거 스케줄 (18개 → 기존 17 + 신규 1)
```
16:30  송장 배포
17:00  ★ Supabase DB 동기화 (신규)  ← 송장 배포 30분 후
```

### 적용 방법
1. `clasp push`
2. GAS 메뉴 > 자동화 설정 > ⏰ 통합 자동 트리거 설치 (전체) 재실행

---

## 2026-07-02 15:40 웹앱 UI 전면 리뉴얼 + 업체 그룹 할인 설정

### 변경사항

1. **대시보드 (page.tsx) 컴팩트 리뉴얼**
   - 통계 미니카드 + 업무실행 버튼을 한 줄로 배치
   - 랭킹+최근발주 테이블 화면 전체 높이 활용
   - 패딩/여백 최소화, 행 높이 ~20px

2. **업체 관리 (vendors/page.tsx) 전면 재작성**
   - 인라인 편집: ✏️ → 단가그룹, 할인율, 담당자, 메모 등 수정 → 💾 저장
   - 단가 그룹 프리셋: 대리발송 5/10/15/20%, 마진 5/10%, 직영
   - vendors 테이블에 `price_group`, `discount_rate`, `partner_url` 컬럼 추가

3. **뉴파츠 파트너 페이지 개선**
   - `products_hub` 테이블(3,436건) 직접 연동
   - `group_prices` JSONB에서 그룹별 단가 추출
   - 5% 할인단가 자동 적용 (코드 자동완성 시)
   - 열 드래그 리사이즈 기능

4. **레이아웃 분리 (LayoutShell.tsx)**
   - `/partner/` 경로: 사이드바 없음 (외부 업체 전용)
   - 그 외 경로: 관리자 사이드바 포함

### Supabase SQL (vendor_groups.sql)
```sql
ALTER TABLE vendors ADD COLUMN IF NOT EXISTS price_group TEXT;
ALTER TABLE vendors ADD COLUMN IF NOT EXISTS discount_rate NUMERIC DEFAULT 0;
ALTER TABLE vendors ADD COLUMN IF NOT EXISTS partner_url TEXT;
```

### 수정 파일
| 파일 | 변경 |
|---|---|
| `app/page.tsx` | 대시보드 컴팩트 UI |
| `app/vendors/page.tsx` | 그룹할인 설정 + 인라인 편집 |
| `app/partner/newparts/page.tsx` | products_hub 연동 + 열 리사이즈 |
| `app/partner/layout.tsx` | 파트너 전용 레이아웃 |
| `components/LayoutShell.tsx` | 경로 기반 사이드바 분기 |
| `lib/partnerApi.ts` | products_hub 기반 + 그룹단가 함수 |

---

## 2026-07-02 16:04 업체 동기화에 단가 그룹/할인율 추가

### 변경
- `_partnerSupabase.gs` > `_sb_syncVendors_`: 뷰어탭 K2에서 단가그룹, 파일명에서 소비자 할인율 자동 추출
- 허브 그룹 역매핑 (`_pt_getHubGroups()`) + NNN 패턴 감지 (`_pt_getConsumerRateFromK2`)
- DB에 `price_group`, `discount_rate` 저장

---

## 2026-07-02 16:22 웹앱 송장/정산 페이지 데이터 연동 수정

### 문제
- 송장 현황/정산 현황이 모두 0으로 표시
- 원인: 비어있는 `invoices`/`settlements` 테이블에서 읽고 있었음

### 해결
- `app/invoices/page.tsx`: `orders` 테이블의 `invoice_no` 기반으로 전면 재작성
- `app/settlements/page.tsx`: `orders` 테이블에서 업체별/월별 자동 집계로 변경

---

## 2026-07-02 18:35 발주탭 자동 정리 기능 추가

### 신규 파일
| 파일 | 역할 |
|---|---|
| `_partnerOrderCleanup.gs` | 빈행 제거 + D/L열(품목명/단가) 복원 |

### 변경
- 메뉴 추가: `💼 협력업체 관리 > 🧹 발주탭 자동 정리 (빈행+복원)`
- `_partnerOrders.gs`: 발주수집 시 `_pt_autoCleanupBeforeCollect_` 자동 호출
- 성능 최적화: 빈행 없고 D/L열 정상이면 뷰어탭 로드 건너뜀 (+0.3초/파일)

---

## 2026-07-02 21:15 트리거 스케줄 전면 개편

### 최종 스케줄 (15개 / 20개 제한)

```
06:00  이카운트 전체동기화 1
07:00  대리판매 마감
07:20  대리공급 마감
── 업무 시작 ──────────────────────────────────
08:00  발주 수집 + 판매현황 갱신 (1회전)
09:20  대리공급 Push + 우편번호 (1회전)
── 점심 ──────────────────────────────────────
12:00  이카운트 전체동기화 2
12:30  통합 DB 동기화 [Supabase]
12:40  통합허브 상태/재고 업데이트
── 오후 2회전 ─────────────────────────────────
14:05  발주 수집 + 판매현황 갱신 (2회전)
14:30  대리공급 Push + 우편번호 (2회전)
── 송장 처리 ──────────────────────────────────
15:10  넵킨코리아 Gmail 송장수집
15:40  송장 수집
16:20  송장 배포
17:00  발주허브-DB + 협력업체-DB 동기화 [Supabase]
── 야간 ──────────────────────────────────────
23:30  통합 일일마감
```

### 연쇄 호출 구조

**발주 수집 트리거** (`partnerCollectOrdersSilent_`):
```
① partnerCollectOrders(true)          — 발주 수집 (~3~4분)
   └ _pt_autoCleanupBeforeCollect_()  — 빈행 정리 + D/L열 복원 (자동)
② partnerApplyVoidedInvoicesSilent_() — 폐기송장 적용 (~10초)
③ partnerRebuildSalesUploadSheet(true)— 판매현황 갱신 (~30초)
```

**대리공급 Push 트리거** (`partnerPushOrdersToExclusiveFormsSilent_`):
```
① partnerPushOrdersToExclusiveForms(true) — 대리공급 Push (~2~3분)
② _trigger_fillZipAndShipping_()          — 우편번호+택배비 (~30초~1분)
```

### 변경 전/후 비교

| 시간 | 변경 전 (20개) | 변경 후 (15개) | 비고 |
|---|---|---|---|
| ~~02:00~~ | ~~이카운트 동기화~~ | ❌ 제거 | 토큰 403 에러 |
| 06:00 | 이카운트 동기화 1 | 동일 | |
| 07:00 | 대리판매 마감 | 동일 | |
| 07:20 | 대리공급 마감 | 동일 | |
| ~~09:00~~ | ~~발주 수집~~ | → **08:00** | 1시간 앞당김 |
| **08:00** | — | 발주 수집 + 판매현황 | 판매현황 통합 |
| ~~09:15~~ | ~~도서산간 (1회전)~~ | ❌ 제거 | 트리거 절약 |
| 09:20 | Push (1회전) | **Push + 우편번호** | 우편번호 통합 |
| ~~09:25~~ | ~~우편번호 (1회전)~~ | ❌ 제거 | Push에 통합 |
| 12:00 | 이카운트 동기화 2 | 동일 | |
| **12:30** | — | **DB 동기화 [Supabase]** | 신규 |
| 12:40 | 허브 상태/재고 (12:50) | 허브 상태/재고 | 10분 앞당김 |
| 14:05 | 발주 수집 (2회전) | **발주 수집 + 판매현황** | 판매현황 통합 |
| ~~14:20~~ | ~~도서산간 (2회전)~~ | ❌ 제거 | 트리거 절약 |
| 14:30 | Push (2회전) | **Push + 우편번호** | 우편번호 통합 |
| ~~14:40~~ | ~~우편번호 (2회전)~~ | ❌ 제거 | Push에 통합 |
| 15:10 | 넵킨 Gmail (15:20) | 넵킨 Gmail | 10분 앞당김 |
| 15:40 | 송장 수집 (15:50) | 송장 수집 | 10분 앞당김 |
| 16:20 | 송장 배포 (16:30) | 송장 배포 | 10분 앞당김 |
| 17:00 | DB 동기화 | DB 동기화 [Supabase] | 라벨 명확화 |
| 23:30 | 통합 일일마감 | 동일 | |

### 제거 항목 (5개)
- ❌ 02시 이카운트 동기화 (토큰 에러)
- ❌ 도서산간 트리거 × 2 (1회전/2회전)
- ❌ 우편번호 독립 트리거 × 2 (1회전/2회전 → Push에 통합)

### 검증 중 발견·수정한 문제
- ⚠️ `PEP_AUTO_PUSH=ON` 시 발주수집에서 Push 연쇄 호출 → **6분 초과 + 09:20 Push와 중복**
- ✅ PEP_AUTO_PUSH 연쇄 호출 제거 완료. Push는 별도 트리거에서만 실행

### 수정 파일
| 파일 | 변경 |
|---|---|
| `_partnerWebApp.gs` | `_ALL_SCHEDULED_TRIGGERS_` 배열 전면 교체 (20→15개) |
| `_partnerOrders.gs` | 판매현황 갱신 자동 호출 추가 + PEP_AUTO_PUSH 연쇄 제거 |
| `_partnerExclusivePush.gs` | Push 후 우편번호 채우기 연쇄 추가 |
| `_partnerOrderCleanup.gs` | 발주탭 빈행 정리 + D/L열 복원 (신규 파일) |

### 적용 방법
GAS 메뉴: `💼 협력업체 관리 > ⚙️ 자동화 설정 > ⏰ 통합 자동 트리거 설치 (전체)` 실행

---

## 2026-07-02 21:50 뉴파츠 파트너 UI 전면 개선

### 변경 내용

1. **헤더 순서 정렬**: 시트 `발주 및 송장조회` 탭과 동일한 순서로 정렬
2. **배송메시지 + 적요 추가**: 입력/현황 모두에서 표시 및 입력 가능
3. **상태 색상 배지**: 접수완료(🔵), 웹접수(🟢), 배송중(🔷), 배송완료(⚪), 취소(🔴), 반품(🟠), 대기(🟡)
4. **품목명 검색 드롭다운**: 품목명 칸 클릭 시 실시간 검색 → 코드+품목+단가 표시 → 선택하면 자동입력
5. **엑셀 붙여넣기**: Ctrl+V로 탭 구분 데이터 자동 파싱 + 이카운트코드 있으면 단가 자동 조회
6. **엑셀 복사**: 📋 복사 버튼으로 발주현황을 탭 구분 텍스트로 클립보드 복사
7. **열 리사이즈 수정**: 핸들 넓이 7px + hover 시 초록색 라인 + table-layout:fixed 적용

### 수정 파일
| 파일 | 변경 |
|---|---|
| `app/partner/newparts/page.tsx` | 전면 재작성 (시트 헤더 순서, 상태 배지, 드롭다운, 붙여넣기, 복사) |
| `lib/partnerApi.ts` | OrderInput에 note(적요) 필드 추가 + submitOrder에서 note 저장 |

---

## 2026-07-02 22:07 모든 UI에서 ₩ 기호 제거

### 변경
- `queries.ts` `formatKRW()` 함수에서 ₩ 접두어 제거 → 숫자만 콤마 표시
- `partner/newparts/page.tsx` 인라인 ₩ 2곳 제거

---

## 2026-07-02 22:15 뉴파츠 5탭 확장 (대리발송 + 월마감)

### 변경
- **대리발송 탭** (🚚): daily_archive에서 대리공급/전용 데이터 조회, 월 필터, 📋 복사
- **월마감 탭** (📊): settlements 테이블, 정산월/금액/상태/배지
- `partnerApi.ts`: `fetchExclusiveOrders()` + `fetchMonthlySettlements()` 추가

---

## 2026-07-02 23:18 웹앱 마이그레이션 Phase 1 시작 — DB v3 스키마

### 개요
- 협력업체 시트 시스템 전체를 웹앱으로 마이그레이션하는 3단계 계획 수립
- Phase 1: DB 스키마 확장 + GAS 동기화 강화

### 신규 파일
| 파일 | 역할 |
|---|---|
| `docs/migrate_v3.sql` | v2→v3 DB 마이그레이션 (신규 3테이블 + 컬럼 확장 + RLS) |

### 변경 파일
| 파일 | 변경 |
|---|---|
| `lib/supabase.ts` | v3 타입 확장 (Vendor/Order/Invoice/Settlement + ExclusiveOrder/VoidInvoice/InvoiceMatchLog) |

### 신규 테이블
| 테이블 | 용도 |
|---|---|
| `exclusive_orders` | 대리공급 전용양식 데이터 |
| `void_invoices` | 폐기/반송 송장 관리 |
| `invoice_match_log` | 송장 매칭 이력 |

---

## 2026-07-02 23:25 Phase 1-2: GAS 동기화 강화 (6개 함수)

### 개요
- 핵심 GAS 워크플로우 완료 시 자동으로 Supabase DB에 동기화

### 신규 함수 (_partnerSupabase.gs)
| 함수 | 동기화 대상 |
|---|---|
| `_sb_syncInvoices_()` | 송장 → invoices 테이블 |
| `_sb_syncInvoiceMatch_()` | 매칭 결과 → orders + invoice_match_log |
| `_sb_syncExclusiveOrders_()` | 전용양식 Push → exclusive_orders |
| `_sb_syncVoidInvoices_()` | 폐기송장 → void_invoices |
| `_sb_syncCancelReturns_()` | 취소/반품 → cancel_returns |
| `_sb_syncSettlement_()` | 월별 마감 → settlements |
| `_sb_markDistributed_()` | 배포 완료 → orders.distributed_at |

### 호출 삽입 파일
| 파일 | 위치 |
|---|---|
| `_partnerOrders.gs` | 송장 수집 완료 후 (invoices + match) |
| `_partnerOrders.gs` | 폐기송장 적용 후 (void_invoices) |
| `_partnerExclusivePush.gs` | Push 완료 후 (exclusive_orders) |
| `_partnerMonthlySettle.gs` | 마감 완료 후 (settlements) |
| `_partnerCancelReturn.gs` | 수집 완료 후 (cancel_returns) |

---

## 2026-07-02 23:38 Phase 2: 관리자 대시보드 구축

### 개요
- 4개 관리자 페이지 + admin API + 사이드바 v2 구현
- 빌드 성공 확인

### 신규 파일 (웹앱)
| 파일 | 역할 |
|---|---|
| `app/lib/adminApi.ts` | 관리자 전용 API (8개 쿼리 함수) |
| `app/app/admin/hub/page.tsx` | 발주허브 (필터+테이블+통계) |
| `app/app/admin/invoice/page.tsx` | 송장 매칭 (4탭: 미매칭/수집/이력/폐기) |
| `app/app/admin/settle/page.tsx` | 월별 정산 (월별합계+필터+상세) |
| `app/app/admin/cancel/page.tsx` | 취소/반품 (유형별 통계+필터) |

### 수정 파일
| 파일 | 변경 |
|---|---|
| `app/components/Sidebar.tsx` | 운영/관리/파트너 3섹션 구분 |

---

## 2026-07-02 23:54 Phase 3: 파트너 페이지 템플릿화

### 개요
- `/partner/[slug]` 동적 라우팅으로 모든 업체를 단일 템플릿으로 통합
- DB에서 업체 설정(이름, 할인율, 그룹키, 탭 구성) 자동 로드
- 빌드 시 43개 업체 + newparts slug 자동 정적 생성 (55페이지)

### 구조
```
/partner/[slug]/
├── page.tsx           ← 서버 래퍼 (generateStaticParams)
└── PartnerClient.tsx  ← 클라이언트 컴포넌트 (기존 뉴파츠 코드 기반)
```

### 신규/수정 파일
| 파일 | 역할 |
|---|---|
| `app/app/partner/[slug]/page.tsx` | 서버 래퍼 + generateStaticParams |
| `app/app/partner/[slug]/PartnerClient.tsx` | 동적 업체 설정 기반 파트너 UI |
| `app/lib/partnerApi.ts` | `fetchVendorBySlug`, `fetchActiveVendors` 추가 |

### 삭제
| 파일 | 사유 |
|---|---|
| `app/app/partner/newparts/` | [slug] 동적 라우팅으로 대체 |

