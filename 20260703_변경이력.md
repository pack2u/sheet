# 변경이력 (2026-07-03~06)

---

## 2026-07-06 22:55 🐛 스크립트 재설치 연속 실행 버그 수정

### 문제
- 10개 업체 설치 후 3분 대기 → 다음 배치 실행 안 됨
- **근본 원인**: `_repairScript_continueAuto_` 함수 끝에서 **모든** `_repairScript_continueAuto_` 트리거를 삭제 → **방금 생성한 다음 배치 트리거까지 삭제!**

### 수정 (`_partnerMenu.gs`)
1. 트리거 정리 코드를 `remaining === 0` (완료 시)에만 실행하도록 이동
2. `_repairScript_scheduleNext_`에 에러 핸들링 + 트리거 20개 제한 대응 추가
3. 트리거 삭제 시 try/catch 추가

---

## 2026-07-06 22:45 🔧 단가조회 탭 #REF 자동 복구

### 문제
- 단가조회(뷰어) 탭 A3의 ARRAYFORMULA가 스필 실패 → A열 전체 #REF!
- 원인: A4 이하에 잔류 데이터 → ARRAYFORMULA 스필 공간 없음
- 연쇄 영향: 자동입력(onEdit)이 뷰어 데이터를 읽으므로 → 코드맵 비어있음 → 자동입력 불가 (수시간 지연)

### 수정 (3곳)
1. **라이브러리 `p2u_partnerOnOpen()`**: 시트 열 때 A3 #REF 감지 → A4이하 자동 클리어 → 즉시 복구
2. **`adminInstallVendorOrderDateAutofillScripts_`**: 전체 재설치 시 #REF 자동 복구
3. **`installAllVendorScriptsNoUI`**: NoUI 재설치에도 동일 적용

### 효과
- 시트 열기만 하면 #REF 자동 복구 → 자동입력 즉시 작동
- 스크립트 재설치 시에도 #REF 자동 해결

---

## 2026-07-06 18:25 🔄 송장 매칭 라이브러리 패턴 전환

### 변경
- `InvoiceMatch_협력업체용.gs`의 `getPartnerInvoiceMatchCode_()` → 1000줄+ 인라인 → 3줄 래퍼로 전환
- `_partnerLibrary.gs`에 공개 API 3개 추가:
  - `p2u_openInvoiceMatchSidebar()`: 사이드바 HTML 생성 + 표시
  - `p2u_parseAndMatchInvoiceText(rawText)`: 텍스트 파싱 + 전용양식 매칭
  - `p2u_applyInvoiceMatches(matchesJson)`: 매칭 결과 전용양식 반영

### 효과
- clasp push만으로 **모든 업체 시트에 즉시 반영** (스크립트 재설치 불필요)
- 단, 최초 1회는 스크립트 재설치 필요 (래퍼 코드 교체)

---

## 2026-07-06 17:55 📬 송장 매칭 다중 송장 파싱 지원

### 문제
- `김현정 259421380120 / 259421380131 / 259421380142` 형식 인식 불가
- 기존 정규식이 `$`(줄 끝)에 숫자만 기대 → `/` 구분자 포함 시 전체 스킵

### 수정
1. **GAS** (`InvoiceMatch_협력업체용.gs`): `_parseInvoicePairs_` + 인라인 코드에 동일 패턴 추가
2. clasp push로 즉시 반영 (라이브러리 전환 완료)

---


## 2026-07-06 15:45 🔥🔥 임시기록 저장 위치 원복 (근본 원인)

### 문제 (근본 원인)
- **7/3**: 웹앱 호환 목적으로 `getActiveSpreadsheet()` → `openById(_PT.HUB_ID)` 변경
- 임시기록이 **상품정보 시트 → 통합 HUB**로 잘못 이동됨
- Push는 HUB에 기록, 마감은 HUB를 초기화 → 사용자가 보는 상품정보 시트엔 아무것도 없음

### 수정 (4파일)
| 파일 | 수정 |
|---|---|
| `_partnerExclusivePush.gs` (732행, 4572행) | `_PT.HUB_ID` → `_PT.INFO_SS_ID` |
| `_partnerMonthlySettle.gs` (190행) | `_PT.HUB_ID` → `_PT.INFO_SS_ID` |
| `_partnerWebApp.gs` (649행) | `_PT.HUB_ID` → `_PT.INFO_SS_ID` |
| `_partnerExclusiveArchive.gs` (84행) | `_PT.HUB_ID` → `_PT.INFO_SS_ID` |

### 참고
- `_PT.INFO_SS_ID` = 상품정보 시트 (`1Lz-ykUAQBpeEnZU1T_qdJeX9d9L10h6z6qYwHQna2QE`)
- `_PT.HUB_ID` = 통합 관리 HUB (`1qRIEw--DcF44CqiO24C9vI74pYbN8VbqCimjNuHK5fk`)
- 통합 HUB에 잘못 생성된 `대리공급_임시기록` 탭은 수동 삭제 필요

---

## 2026-07-06 15:28 🔥 임시기록 유령 행 + getRange 버그 수정

### 문제
- `clearContent()`로 임시기록을 비워도 **빈 행이 물리적으로 남아** `getLastRow()` > 1 반환
- `_pep_loadTempTabState_`에서 `getRange(2, 1, getLastRow(), ...)` → 행 수 계산 오류 (1행 초과 읽음)
- 빈 행에서 핑거프린트 키 생성 → 기존 데이터로 오인 → 새 데이터 전부 스킵

### 수정
1. **`_pep_loadTempTabState_`**: `getLastRow()` → `getLastRow() - 1` (행 수 보정)
2. **빈 행 스킵**: `if (!uid && !code) continue;` 추가
3. **`partnerRebuildTempRecords`**: 기존 데이터 `deleteRows`로 완전 삭제 후 강제 쓰기

---

## 2026-07-06 15:10 🔥 임시기록 저장 안 되는 치명적 버그 수정

### 문제
- 대리공급 Push 실행 시 `대리공급_임시기록` 탭에 데이터가 **전혀 기록되지 않음**
- 원인: 826행 핑거프린트 비교 조건 `<=` 오류
  - `_fpOccT_(0) <= _fpCntT_(0)` → `true` → **항상 스킵**
  - 임시기록이 비어있어도 "이미 있음"으로 판단하여 모든 행 스킵

### 수정
- **파일**: `_partnerExclusivePush.gs` 826행
- `_fpOccT_ <= _fpCntT_` → `_fpOccT_ < _fpCntT_`
- 빈 임시기록(0 < 0 = false) → 정상적으로 기록됨

### 복구 방법
- 시트에서 `3️⃣ 대리공급 Push` 다시 실행 → 임시기록 재생성
- 이미 전용양식에 Push된 건은 스킵되고, 임시기록에만 기록됨

---

## 2026-07-06 14:40 🐛 오후 재Push 임시기록 초기화 제거

### 문제
- `오후 재Push` 실행 시 `대리공급_임시기록` 탭이 전체 삭제됨
- 오전+오후 Push된 데이터가 모두 소실 → 마감/정산 불가

### 수정 내용
- **파일**: `_partnerExclusivePush.gs` (`partnerAfternoonResetAndPush`)
- 임시기록 탭 초기화 코드 블록 **제거** (4489~4501행)
- 확인 다이얼로그에서 임시기록 초기화 안내 제거, "유지됩니다" 문구 추가
- 로그 메시지 "임시기록: 유지(삭제 안 함)"으로 변경

### 동작 변경
| 항목 | 수정 전 | 수정 후 |
|---|---|---|
| 전용양식 탭 | 초기화 ✅ | 초기화 ✅ (동일) |
| 임시기록 탭 | **전체 삭제** ❌ | **유지** ✅ |
| 소스 P열 | 초기화 ✅ | 초기화 ✅ (동일) |

---

## 2026-07-05 22:15 🔐 접근 제어 + 설정 관리 시스템

### 변경 내용
- **DB 스키마** (`20260705_migrate_v6.sql`)
  - `allowed_emails` 테이블 신규 (직원 이메일 등록/관리)
  - `vendors.allowed_email` 컬럼 추가 (파트너사 접속 이메일)
- **설정 페이지** (`/admin/settings`)
  - 직원 이메일 CRUD (추가/삭제/활성 토글)
  - 파트너사 기능 설정 (탭별 체크박스 ON/OFF)
  - 파트너사 접속 이메일 설정
- **접근 제어 API** (`adminApi.ts`)
  - `checkEmailAccess()`: 관리자/파트너/미등록 분류
  - 관리자 이메일 → admin 전체 + 모든 파트너 접근
  - 파트너 이메일 → 해당 업체만 접근
- **Google 인증 토큰 캐시** (`googleSheets.ts`)
  - sessionStorage 기반 토큰 캐시 (팝업 1회만)
- **판매현황 localStorage 캐시**
  - 💾 저장 / 🗑 초기화 버튼 + 페이지 재접속 시 자동 복원

### 수정 파일
- `docs/20260705_migrate_v6.sql` (신규)
- `app/admin/settings/page.tsx` (신규)
- `lib/adminApi.ts` (접근 제어 함수 추가)
- `components/Sidebar.tsx` (설정 메뉴 추가)
- `lib/googleSheets.ts` (토큰 캐시 + 합계 키워드)

---

## 2026-07-05 21:40 🖥️ 시트 통합 관리 — ResizableTable 전면 적용 + 판매현황 파싱 수정

### 변경 내용
- **ResizableTable 컴포넌트** 신규 생성 (`components/ResizableTable.tsx`)
  - 열 헤더 드래그로 너비 조절 가능
  - localStorage에 열 너비 저장/복원 (새로고침해도 유지)
  - "↺ 열 너비 초기화" 버튼 포함
  - hover 시 셀 전체 내용 tooltip 표시
- **모든 데이터 테이블 교체**: 판매현황 / 로젠 송장 / 대리공급 업체별 → ResizableTable 적용
- **판매현황 컬럼 매핑 전면 재설계** (`lib/googleSheets.ts`)
  - 사방넷 열(14~16) 우선 사용, 주문서/플랫폼 원본 폴백 체인
  - col 14 "주문자명(사방넷)" = "이름/주문번호" → "/" 분리하여 주문번호+수취인 추출
  - col 15 "전화번호(사방넷)" → 전화
  - col 16 "배송지(사방넷)" → 주소
  - 거래처명이 "대리발송-개/인" 업체명일 때 → 주문자명을 수취인으로 대체
- **디버그 표시**: 불러오기 후 전체 헤더 인덱스 + colMap 매핑 결과 표시

### 수정 파일
- `app/components/ResizableTable.tsx` (신규)
- `app/app/admin/sheets/page.tsx`
- `app/lib/googleSheets.ts`

---

## 2026-07-04 20:28 🔧 뉴파츠/부원 카카오 송장매칭 스크립트 재설치

### 문제
- 뉴파츠와 부원 업체 시트에서 카카오 송장 매칭 사이드바의 **🔍 분석 버튼**을 눌러도 아무 반응 없음
- 권한 승인 완료 후에도 동일 증상 지속
- 원인: 7/3 라이브러리 패턴 전환 이후 업체 시트에 주입된 `InvoiceMatch` 코드가 업데이트되지 않아 `google.script.run.parseAndMatchInvoiceTextLocal()` 호출 시 함수를 찾지 못함

### 해결
- `debug.gs`에 `debugReinstallInvoiceMatch_NewParts_BuWon()` 디버그 함수 추가
- Apps Script 에디터에서 직접 실행하여 두 업체에 최신 코드 재주입
- `createViewerNoticeScript_()` 호출로 onOpen + onEdit + InvoiceMatch + DbPrice + 검색발주 코드 일괄 재설치

### 수정 파일
- `debug.gs`: `debugReinstallInvoiceMatch_NewParts_BuWon()` 함수 추가
  - `_pt_listFiles(true)` 로 협력업체 파일 목록 조회
  - 파일명에 "뉴파츠" 또는 "부원" 포함된 시트만 필터링
  - `createViewerNoticeScript_(ss)` 호출로 스크립트 재설치

### 향후 참고
- 코드 수정 후 **모든 업체 시트에 반영**하려면: `💼 협력업체 관리 → 🛠️ 협력업체 복구시스템 → 스크립트 재설치`
- 특정 업체만 재설치하려면: `debugReinstallInvoiceMatch_NewParts_BuWon()` 함수의 `targets` 배열 수정
- ⚠️ 재설치 후 업체 직원이 **권한 재승인**이 필요할 수 있음 (새 스코프 추가 시)

---

## 2026-07-04 19:58 🔧 이카운트 프록시 v2 마이그레이션 (GCP 조직 정책 우회)

### 문제
- 이카운트 동기화가 소유자 계정(`pack2u@pack2u.co.kr`)에서도 `403 Permission denied` 에러로 실패
- **모든 직원 계정에서 동일 증상** — 특정 사용자 권한 문제가 아님

### 근본 원인 분석

```
GAS (ScriptApp.getOAuthToken())
  → IAM Credentials API (generateIdToken)
    → 403 Permission denied ❌
      → 조직 정책이 차단!
```

1. **`iam.allowedPolicyMemberDomains`**: 프로젝트 IAM에 `allUsers` 추가 불가 (조직 허용 도메인만 가능)
2. **`iam.disableServiceAccountKeyCreation`**: 서비스 계정 키 생성 불가 (`enforce: true`)
3. **GAS OAuth 토큰 제한**: `ScriptApp.getOAuthToken()`이 반환하는 토큰으로 IAM Credentials API 호출 시 Google Workspace 관리자 정책에 의해 차단
4. **조직 정책 수정 불가**: `pack2u@pack2u.co.kr`이 프로젝트 Owner여도 `orgpolicy.policies.create` 권한 없음 (Google Workspace 최고관리자만 가능)

### 이카운트 IP 화이트리스트 확인

- 프록시 우회 시도(직접 호출) → 이카운트 응답:
  ```
  [107.178.193.68] 허용되지 않은 IP입니다. ERP > API인증키발급 > IP등록을 진행하시기 바랍니다.
  ```
- **결론**: 이카운트 API는 IP 화이트리스트를 적용하므로 프록시(고정 IP) 반드시 필요

### 해결: 개인 GCP 프로젝트에 프록시 v2 배포

**전략**: 조직 정책이 없는 **개인 Gmail 계정**(`pack2you6974@gmail.com`)의 GCP 프로젝트에 새 프록시 배포

#### 기존 vs 신규 비교

| 항목 | 기존 (v1, 실패) | 신규 (v2, 성공) |
|---|---|---|
| GCP 프로젝트 | `pack2u-proxy` (조직 GCP) | `ecount-proxy-personal` (개인 GCP) |
| 프로젝트 번호 | `329334961005` | `770649904697` |
| 소유자 | `pack2u@pack2u.co.kr` | `pack2you6974@gmail.com` |
| 조직 정책 | ❌ 차단 (allUsers, SA 키 생성) | ✅ 제한 없음 |
| 서비스 유형 | Cloud Run (gen2) | Cloud Functions (gen2) |
| 인증 방식 | IAM ID Token (403 실패) | **없음** (allUsers 허용) |
| 보안 | IAM + API Key | **API Key만** (`X-Proxy-Key`) |
| 고정 IP | `34.64.87.69` | `34.64.85.19` |
| 리전 | `asia-northeast3` | `asia-northeast3` |
| 프록시 URL | `ecount-proxy-43iravsbpa-du.a.run.app` | `asia-northeast3-ecount-proxy-personal.cloudfunctions.net/ecount-proxy` |

#### 신규 인프라 구성

```
ecount-proxy-personal (개인 GCP 프로젝트)
├── 고정 IP: 34.64.85.19 (ecount-nat-ip)
├── Cloud Router: ecount-router
├── Cloud NAT: ecount-nat (→ ecount-nat-ip)
├── VPC Connector: ecount-connector (10.8.0.0/28)
└── Cloud Function: ecount-proxy (Gen2, Python 3.12)
    ├── 런타임: python312
    ├── 메모리: 256MB
    ├── 타임아웃: 60초
    ├── VPC: ecount-connector (ALL_TRAFFIC)
    ├── 인증: allUsers (인증 불필요)
    └── 환경변수: PROXY_KEY=pack2u-ecount-proxy-2026
```

#### 프록시 코드 (main.py)
- 위치: `F:\Pack2U_상품정보sheet\ecount-proxy-v2\main.py` (배포 후 삭제됨)
- 기능: GAS에서 받은 요청을 이카운트 API로 전달 (고정 IP 경유)
- 보안: `X-Proxy-Key` 헤더로 API 키 검증
- 의존성: `functions-framework`, `requests`

### 수정 파일
- **`ecount.gs`** (핵심 변경):
  - `_ECOUNT_PROXY_URL_`: 새 Cloud Function URL로 변경
  - `_ECOUNT_PROXY_KEY_`: 동일 유지 (`pack2u-ecount-proxy-2026`)
  - `_ECOUNT_PROXY_SA_`: 삭제 (더 이상 불필요)
  - `_getCloudRunIdToken_()`: **함수 완전 삭제** (IAM 인증 불필요)
  - `_ecountFetchViaProxy_()`: `Authorization` 헤더 제거, `X-Proxy-Key`만 전송

### 이카운트 ERP 설정
- **OAPI설정 → IP등록 → `34.64.85.19` 추가 완료**
- 기존 IP `34.64.87.69`도 당분간 유지 (기존 프록시 사용 가능성)

### 비용 참고
- Cloud Function: 무료 티어 (월 200만 호출)
- VPC Connector: **~$7/월** (최소 e2-micro 2대)
- Cloud NAT: **~$1/월** + 데이터 처리 비용
- 고정 IP: 사용 중이면 무료
- **예상 월 비용: ~$8~10**

### 향후 수정 시 참고

1. **프록시 코드 수정**: `gcloud functions deploy` 재실행 (개인 계정 `pack2you6974@gmail.com`으로 전환 필요)
   ```bash
   gcloud config set account pack2you6974@gmail.com
   gcloud config set project ecount-proxy-personal
   gcloud functions deploy ecount-proxy --gen2 --runtime=python312 --region=asia-northeast3 --source=./ecount-proxy-v2 --entry-point=ecount_proxy --trigger-http --allow-unauthenticated --vpc-connector=ecount-connector --egress-settings=all
   ```

2. **API 키 변경 시**: `ecount.gs`의 `_ECOUNT_PROXY_KEY_`와 Cloud Function 환경변수 `PROXY_KEY` 모두 변경

3. **기존 프록시(pack2u-proxy) 완전 삭제 시**:
   ```bash
   gcloud config set account pack2u@pack2u.co.kr
   gcloud config set project pack2u-proxy
   gcloud run services delete ecount-proxy --region=asia-northeast3
   # VPC Connector, Cloud NAT, Cloud Router, 고정 IP도 순서대로 삭제
   ```

---

## 2026-07-04 18:00 🧹 자동 트리거 정리

### 문제
- 불필요한 찌꺼기 트리거가 다수 남아있음 (특히 02시 이카운트 동기화 등)

### 해결
- 요청된 시간표 외 모든 트리거 삭제
- 유지된 트리거 목록:

| 시간 | 트리거 |
|---|---|
| 06:00 | 이카운트 전체동기화 1 |
| 07:00 | 대리판매 마감 |
| 07:20 | 대리공급 마감 |
| 08:00 | 발주 수집 + 판매현황 갱신 (1회전) |
| 09:20 | 대리공급 Push + 우편번호 (1회전) |
| 12:00 | 이카운트 전체동기화 2 |
| 12:40 | 통합허브 상태/재고 업데이트 |
| 14:05 | 발주 수집 + 판매현황 갱신 (2회전) |
| 14:20 | 대리공급 Push + 우편번호 (2회전) |
| 15:35 | 넵킨코리아 Gmail 송장수집 |
| 15:40 | 송장 수집 |
| 16:20 | 송장 배포 |
| 23:30 | 통합 일일마감 |

### 수정 파일
- `_debug_triggers.gs`: 트리거 삭제/재설치 유틸리티 함수



## 2026-07-04 11:40 🐛 대리판매 마감 시 대리공급_임시기록 초기화 안 됨 수정

### 문제
- `대리판매 발주 마감이동` (07:00 트리거 `_trigger_monthlySettle_`) 실행 시 `대리공급_임시기록` 탭이 초기화되지 않음
- 7/2에 `getActiveSpreadsheet()`로 추가 시도 → 트리거에서 동작 안 해서 원복한 이력 있음
- `_pms_core_()` 함수에 임시기록 초기화 코드가 아예 없었음

### 원인
- `_pms_core_`에 임시기록 초기화 코드 부재 (대리공급 마감 `_pea_core_`에만 존재)
- 7/2 시도 실패 원인: `getActiveSpreadsheet()` → 트리거 컨텍스트에서 null/다른 시트 반환

### 수정
1. **`_partnerMonthlySettle.gs`**: `_pms_core_` 파일 루프 **전**에 `openById(_PT.HUB_ID)` 방식 임시기록 초기화 추가 (7/3 `_pea_core_` 수정과 동일 패턴)
2. **`_partnerMonthlySettle.gs`**: 완료 메시지에 `📋 임시기록 정리: 삭제 N건, 유지 N건` 표시 추가
3. **`_partnerWebApp.gs`**: 일괄 마감 3단계 초기화의 `getActiveSpreadsheet()` → `openById(_PT.HUB_ID)` 변경 (트리거 안전)

### 수정 파일
| 파일 | 변경 |
|---|---|
| `_partnerMonthlySettle.gs` | `_pms_core_` L184~201: `openById(_PT.HUB_ID)` 임시기록 초기화 추가 + 결과 메시지 표시 |
| `_partnerWebApp.gs` | L644: `getActiveSpreadsheet()` → `openById(_PT.HUB_ID)` 트리거 안전 변경 |

---

## 2026-07-04 01:00 🏗️ DB 마감 체계 구현 (GAS 마감과 동일 구조)

### 신규 테이블 (Supabase)
- `settle_partner_sales`: 대리판매 월별 마감 상세 행
- `settle_exclusive`: 대리공급 월별 마감 상세 행
- `settlements`: settle_type 컬럼 추가 (대리판매/대리공급 구분)
- 마이그레이션: `docs/20260704_migrate_v5.sql`

### GAS 동기화 함수 (`_partnerSupabase.gs`)
- `_sb_syncDailyArchive_(archiveRows)` — 통합 일일마감 → daily_archive
- `_sb_syncPartnerSettle_(vendor, month, rows)` — 대리판매 마감 → settle_partner_sales + settlements
- `_sb_syncExclusiveSettle_(vendor, month, rows)` — 대리공급 마감 → settle_exclusive + settlements  
- `_sb_markArchived_(table, uniqueIds)` — 라이브 테이블 마감 표시

### GAS 마감 함수 훅 추가
- `_pep_archiveUnifiedDaily_` (L6255~): 일일마감 시트 기록 직후 DB 동기화
- `_pms_processOneFile_` (L391~): 대리판매 마감 완료 후 DB 상세+요약 동기화
- `_pea_processOneFile_` (L475~): 대리공급 마감 완료 후 DB 상세+요약 동기화

---

## 2026-07-04 00:40 🐛 전용양식 DB 동기화 열 매핑 버그 수정

### 원인
- `_tempPendingRows_`의 열 인덱스가 소스 탭 구조와 맞지 않았음
- `row[0]`을 vendor_name으로 사용했지만, 실제 [0]은 소스 A열(관련없는 데이터)
- `row[5]`를 수량으로 사용했지만, 실제 수량은 [6]=G열
- `row[6]`을 수취인으로 사용했지만, 실제 수취인은 [12]=M열
- **결과**: exclusive_orders에 잘못된 데이터가 저장되거나, 빈 데이터로 저장됨

### 수정 (`_partnerExclusivePush.gs` L1231~)
| 필드 | 수정 전 (잘못된 인덱스) | 수정 후 (올바른 인덱스) |
|---|---|---|
| vendor_name | row[0] (소스 A열) | row[22] (prefix) → 업체명 변환 |
| unique_id | 없음 | row[15] (P열, 고유ID) |
| qty | row[5] (소스 F열) | row[6] (G열, 수량) |
| recipient | row[6] (소스 G열) | row[12] (M열, 수취인) |
| phone | row[7] (소스 H열) | row[8] (I열, 전화) |
| address | row[8] (소스 I열) | row[9] (J열, 주소) |
| delivery_msg | 없음 | row[10] (K열, 배송메시지) |
| note | row[12] (소스 M열) | row[16] (Q열, 보내는분) |

---

## 2026-07-04 00:15 🔄 전용양식 페이지 — Supabase 직접 조회로 전환

### 원인
- GAS 웹앱 배포 업데이트 불가 (세트분리 시트 소유자가 외부 업체)
- `exclusivePreview` API가 미배포 상태에서 페이지 로딩 에러

### 수정
- `app/admin/exclusive/page.tsx` 전면 재작성
  - GAS `exclusivePreview` API 호출 제거
  - Supabase `exclusive_orders` 테이블에서 직접 조회
  - 날짜별 필터 (오늘/전체/특정일)
  - 업체별 필터 (클릭으로 토글)
  - Push 실행은 기존 GAS `run` action 유지 (이미 배포됨)
- `lib/adminApi.ts`의 `fetchExclusivePreview()` 함수는 더 이상 사용하지 않음

---

## 2026-07-03 21:50 🆕 웹앱 전용양식 Push 미리보기 + 실행 기능

### 개요
- 웹앱 관리자 페이지(`/admin/exclusive`)에서 **세트분리 소스 탭 데이터를 업체별로 분류하여 미리보기** + **전체 Push 실행** 가능
- 기존 GAS Push 로직(`partnerPushOrdersToExclusiveForms`)을 100% 재사용

### GAS 변경
1. **`_partnerExclusivePush.gs`** (L728)
   - `getActiveSpreadsheet()` → `openById(_PT.HUB_ID)` 변경 (웹앱 doGet에서 null 반환 문제 해결)
2. **`_partnerWebAppAPI.gs`**
   - `exclusivePreview` action 추가 → 소스 탭 읽기 + prefix 분류 + 업체별 건수/상세 JSON 반환
   - `syncDb` task: `syncAllToDbOwner()` → `_sb_syncOrders_()` + `_sb_syncVendors_()` 직접 호출 (getUi 에러 방지)

### 웹앱 변경
1. **`lib/adminApi.ts`** — `fetchExclusivePreview()` 함수 추가
2. **`app/admin/exclusive/page.tsx`** — [NEW] 전용양식 Push 관리자 페이지
   - 요약 카드 (전체/대기/완료/미매핑 건수)
   - 업체별 미니 카드 (prefix + 업체명 + 건수)
   - 탭 전환 (전체/업체별/미매핑)
   - 데이터 테이블 (품목코드, 품목명, 수량, 수취인, 주소, 단가)
   - Push 실행 버튼 → Push 후 자동 DB 동기화
3. **`components/Sidebar.tsx`** — 사이드바 메뉴에 "🏭 전용양식 Push" 추가

---

## 2026-07-03 17:44 🐛 DB 동기화 트리거 에러 수정

### 원인
- `_trigger_syncDb_` → `syncAllToDbOwner()` 호출
- `syncAllToDbOwner()`에서 `SpreadsheetApp.getUi()` 직접 호출 (confirm/alert)
- 트리거 컨텍스트에서 `getUi()` 사용 불가 → "Cannot call SpreadsheetApp.getUi() from this context"

### 수정 (`_partnerWebApp.gs`)
- `syncAllToDbOwner()` 대신 `_sb_syncOrders_()` + `_sb_syncVendors_()` 핵심 로직 직접 호출
- Chat 알림에 발주/업체별 결과 상세 표시

---

## 2026-07-03 16:55 🐛 대리공급 임시기록 초기화 안됨 수정

### 원인
- `_pea_core_()` 함수에서 임시기록/허브 초기화 코드가 **파일 루프 뒤**에 배치
- 협력업체 40+개 파일을 `openById`로 순회하며 전용양식 마감 처리 → **6분 GAS 시간 제한 초과**
- 시간초과 시 임시기록 초기화 코드(274행~)에 **도달하지 못함**

### 수정 (`_partnerExclusiveArchive.gs`)
1. 임시기록 초기화 + 허브 초기화를 **파일 루프 전으로 이동** (최우선 실행)
2. 파일 루프에 **5분 시간초과 안전 체크** 추가 (`Date.now() - startMs > 5분`)
3. 시간초과 시 남은 업체 미처리 경고를 `result.errors`에 기록

### 영향 범위
- 07:20 자동 트리거 (`_trigger_exclusiveArchive_` → `_pea_core_`)
- 수동 전용마감 (`partnerArchiveExclusiveForm` → `_pea_core_`)
- 05:00 일괄마감은 별도 경로이므로 영향 없음

---

## 2026-07-03 16:20 ✨ 웹앱 UI 개선 — 체크박스 일괄 처리 + 인라인 수정 + 모바일 가로방향

### 변경 내용

| 파일 | 변경 |
|---|---|
| `PartnerClient.tsx` | ✅ 체크박스 다중선택 + 상단 일괄 삭제/취소 버튼 + 인라인 수정확인(✅저장/✖취소) |
| `admin/hub/page.tsx` | ✅ 동일 패턴 적용 (confirmAction 제거 → 체크박스 일괄 처리) |
| `globals.css` | ✅ touch-btn 클래스 + 모바일 가로방향(landscape) 미디어쿼리 |

### 세부 변경
- **발주현황(history) 탭**: 각 행에 체크박스 열 추가, 송장배정/취소된 행은 🔒 표시
- **관리자 허브**: 기존 개별 confirmAction 다이얼로그 제거 → 체크박스 일괄 confirm()으로 전환
- **수정 모드**: 기존 상단 배너 대신 행 내부 인라인 ✅저장/✖취소 버튼
- **모바일**: `@media (orientation: landscape) and (max-height: 500px)` 터치 타겟 확대

---

## 2026-07-03 15:27 🐛 넵킨코리아 Gmail 송장수집 자동 실행 안됨 수정

### 원인
- 트리거 실행 시각: **15:10** (`_partnerWebApp.gs:1396`)
- 시간 윈도우: **15:30~16:30** (`_partnerGmailInvoice.gs:28`)
- 15:10에 실행되면 시간 윈도우(15:30) 이전이라 즉시 `return` → 무시됨

### 수정
- `_partnerGmailInvoice.gs:28` — `_GMI_TRIGGER_START_MIN = 30` → `0`
- 시간 윈도우를 **15:00~16:30**으로 확장하여 15:10 트리거에서 정상 실행

---

## 2026-07-03 15:18 🐛 발주 자동수집 시 "접수완료" 상태 미기록 버그 수정

### 원인
- `partnerCollectOrdersSilent_()` (5분 자동 트리거)에서 `opt_noWriteBack=true`로 호출
- 이로 인해 허브 수집은 되지만, 업체 시트 N열(상태)에 "접수완료" 역기록이 안 됨
- 결과: 고유ID만 부여되고 조건부서식(셀 색상)이 작동하지 않음

### 수정
- `_partnerOrders.gs:2592` — `partnerCollectOrders(true)` → `partnerCollectOrders(false)`
- 자동 트리거에서도 상태값을 업체 시트에 역기록하도록 변경

### 영향 범위
- 자동 트리거(5분 간격) 실행 시 업체 시트에 "접수완료" 상태가 정상 기록됨
- 조건부서식에 의한 셀 색상 변경 정상 작동

---

## 2026-07-03 15:00 GAS 복구시스템 메뉴 전면 개선

### 변경 파일
- `repairDialog.html`: UI 전면 개편
  - 단계 번호(❶~❻) + 안전도 배지(가장 안전/안전/느림/⚠최종수단)
  - 사용 순서 가이드 ("가벼운 도구부터 먼저 시도하세요!")
  - 기술 용어 제거 → 증상 중심 설명 ("코드 입력해도 품목명이 안 채워질 때")
  - 실행 버튼 텍스트 동적 변경 ("🔧 발주탭 복구 실행")
- `_partnerMenu.gs`:
  - 메뉴명: "🛠️ 협력업체 복구시스템" → "🔧 업체시트 관리·복구"
  - 다이얼로그 타이틀 한글화 (formula→단가, header→헤더)
  - 탭 재생성 spill 호출에 최신 시스템 안전 주석 추가

### 메뉴 카테고리 (변경 전→후)
| 변경 전 | 변경 후 |
|---|---|
| ⚡ 빠른 품목명·단가 채우기 | ❶ 품목명·단가 빈칸 채우기 |
| 📊 단가조회 수식 복구 | ❷ 단가조회 화면 복구 |
| 📝 발주탭 수식 복원 | ❸ 발주탭 자동입력 복구 |
| 📋 헤더·서식 재적용 | ❹ 양식·디자인 초기화 |
| 🔧 스크립트 재설치 | ❺ 상품검색 스크립트 재설치 |
| 📑 탭 재생성 | ❻ 탭 처음부터 다시 만들기 |

---

## 2026-07-03 14:20 발주현황 필터링 + 취소 + 허브 양방향 관리

### 업체 페이지 (PartnerClient.tsx)
- **발주현황 필터**: 기본=미배송+당일배송만 표시, 체크박스로 전체보기 전환
- **발주 취소**: 🚫 버튼 → 2단계 확인 → status='취소'로 변경
- 취소된 주문은 "취소됨" 표시, 수정/삭제 불가

### 관리시스템 허브 (admin/hub/page.tsx)
- **주문 수정**: ✏️ 인라인 편집 → 저장/취소
- **주문 취소**: 🚫 status='취소'로 변경
- **주문 삭제**: 🗑 DB에서 완전 삭제
- 허브에서 변경 → 업체 페이지 자동 반영 (동일 DB)

### API 추가
| 파일 | 함수 | 설명 |
|---|---|---|
| `partnerApi.ts` | `cancelOrder()` | 업체용 주문 취소 |
| `partnerApi.ts` | `fetchVendorOrders(filter)` | pending/all 필터 지원 |
| `adminApi.ts` | `updateHubOrder()` | 관리자 수정 (제한 없음) |
| `adminApi.ts` | `cancelHubOrder()` | 관리자 취소 |
| `adminApi.ts` | `deleteHubOrder()` | 관리자 삭제 |

---

## 2026-07-03 13:31 발주현황 탭 수정/삭제 기능 추가

### 변경
- `partnerApi.ts`: `updateOrder()`, `deleteOrder()` API 추가 (송장 미배정 주문만 가능)
- `PartnerClient.tsx`: 발주현황 탭 각 행에 ✏️수정 / 🗑삭제 버튼 추가
  - 수정: 인라인 편집 모드 (파란 입력 필드) → ✅저장 / ✖취소
  - 삭제: 2단계 확인 (확인/취소 버튼) → DB에서 삭제
  - 🔒 송장 배정된 주문은 수정/삭제 불가 (잠금 표시)

---

## 2026-07-03 12:20 뉴파츠 웹앱 활성화 + 업체별 기능 제어 + 신기능 3종

### 개요
- Supabase `vendors` 테이블에 뉴파츠 slug/tab_config 설정
- 탭 5개 → 7개 확장 (전용양식, 전용마감, 송장매칭 추가)
- 공지사항 배너 시스템 (notices 테이블 + 상단 UI)
- 카카오 송장매칭 웹앱 내 실행 기능

### DB 변경 (Supabase)
| 항목 | 내용 |
|---|---|
| `vendors` UPDATE | 뉴파츠: slug='newparts', tab_config JSONB 설정 |
| `notices` 테이블 신규 | 중앙 공지사항 (타입별: info/warning/urgent) |
| `orders` 인덱스 | 미배송 주문 검색 성능 개선 |

### 수정 파일 (웹앱)
| 파일 | 변경 |
|---|---|
| `partnerApi.ts` | fetchNotices, parseKakaoInvoiceText, matchInvoicesToOrders, applyInvoiceMatches 추가 (~157줄) |
| `PartnerClient.tsx` | 탭 7개 확장, 공지배너, 전용마감 탭, 카카오 송장매칭 UI 추가 |
| `20260703_migrate_v4.sql` | DB 마이그레이션 스크립트 신규 |

### 탭 구조 변경
```
변경 전: 발주입력 | 발주현황 | 대리발송 | 월마감 | 단가조회
변경 후: 발주입력 | 발주현황 | 전용양식 | 전용마감 | 월마감 | 단가조회 | 송장매칭
```

### 빌드 결과
- Next.js 16.2.9 빌드 성공 (54 페이지, /partner/newparts 포함)

---

## 2026-07-03 10:04 GAS 라이브러리 패턴 전환 (스크립트 재설치 영구 제거)

### 배경
- onEdit/onOpen 코드 수정 시마다 38개+ 업체 시트에 스크립트 재설치 필요 (20분~1시간)
- 429 Rate Limit 빈발, 업무 중단 시간 발생

### 변경
1. `_partnerLibrary.gs`: onEdit/onOpen 핵심 로직을 라이브러리 공개 함수로 이전
   - `p2u_partnerOnEdit(e)`: 코드 자동완성, 단가 매핑, 입력미완 체크
   - `p2u_partnerOnOpen()`: 공지 팝업, 메뉴 등록
2. `partnerOnEditSource.gs`: 200줄+ → 1줄 wrapper (`Pack2U.p2u_partnerOnEdit(e)`)
3. `priceManager.gs` (createViewerNoticeScript_):
   - manifest에 `Pack2U` 라이브러리 의존성 추가 (developmentMode: true)
   - onOpen/onEdit 코드를 라이브러리 호출 1줄로 축소

### 기존 버그 수정 포함
- TextFinder 실패 시 전체 로드 폴백 차단 → 제거 (코드오류 버그)
- hasEH 범위 E~K 확장 + D/L열 쓰기 hasC 보호 (입력미완 버그)
- CacheService ssId별 키 분리 (라이브러리 캐시 공유 대응)

### 효과
- **이후 코드 수정**: `clasp push`만 하면 전체 업체에 즉시 반영
- **스크립트 재설치**: 이번 1회 재설치 후 영구 불필요
- 429 에러/재시도 문제 해결

## 2026-07-03 09:54 입력미완 메시지 안 사라지는 버그 수정 (partnerOnEditSource.gs)

### 배경
- 발주탭에서 수량/수취인/전화/주소를 모두 입력했는데 "⚠️입력미완" 메시지가 N열에서 안 사라짐
- 모든 업체 시트에서 동일 증상

### 원인
1. `hasEH` 조건이 E~H열(5~8)만 감지 → 배송메시지(I=9)/적요(J=10)/송장번호(K=11) 편집 시 onEdit가 return되어 N열 갱신 안 됨
2. D/L열이 C열 편집 없이도 항상 덮어쓰기(205~206행) → E~H열만 편집해도 품목명/단가가 빈값으로 초기화되는 부작용

### 변경
1. `hasEH` 범위를 `startCol <= 11`로 확장 → E~K열 편집 시에도 입력미완 갱신
2. D/L/J열 쓰기를 `if (hasC)` 블록으로 보호 → C열 편집 시에만 품목명/단가/적요 갱신

### 영향
- 모든 업체 시트에 영향 (onEdit 자동완성)
- clasp push 후 **스크립트 재설치 필요**

## 2026-07-03 09:48 올팩 코드오류 버그 수정 (partnerOnEditSource.gs)

### 배경
- 올팩 발주탭에서 이카운트코드 입력 시 단가조회 탭에 코드가 있는데도 "🚨코드오류 (미등록)" 표시
- 원인: onEdit 코드의 TextFinder 검색(matchEntireCell) 실패 시 즉시 `{ nf: true }` 설정
- 단가조회 탭 C열에 불가시 문자(zero-width space 등)가 있으면 TextFinder 정확 매칭 실패
- `{ nf: true }` 설정 때문에 전체 로드 폴백(cleanCode_ 정규화 매칭)이 실행 안 됨

### 변경
- `partnerOnEditSource.gs` 108행: TextFinder 실패 시 `{ nf: true }` 즉시 설정 **제거**
- 대신 전체 로드 폴백(114행)이 실행되어 `cleanCode_()` 정규화로 양쪽 코드를 비교
- 이로써 불가시 문자/공백이 있어도 정상 매칭 가능

### 영향
- 올팩 포함 모든 업체 시트에 영향 (onEdit 자동완성)
- clasp push 후 **스크립트 재설치 필요** (올팩 + 영향 업체)

## 2026-07-03 09:21 대리공급 Push 트리거 시간 변경 (14:30→14:20)

### 변경
- `_partnerWebApp.gs` L1393: 2회전 Push 트리거 14:30 → 14:20
- `_partnerOrders.gs` L2617: 주석 업데이트
