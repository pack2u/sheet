# 협력업체 복구시스템 메뉴 개선 + 웹앱 현황 기획서

## 작성일: 2026-07-03

---

## 1부: 현재 웹앱 시스템 변경 현황

### 웹앱 프로젝트 구조 (`F:\Pack2U_협력업체웹앱`)

```
📁 app/
├── 📁 app/
│   ├── 📁 admin/          ← 관리시스템 (허브, 송장, 정산, 취소/반품)
│   │   ├── hub/page.tsx    ← 📦 발주 허브 (✏️수정/🚫취소/🗑삭제 완료)
│   │   ├── invoice/        ← 🔗 송장 매칭
│   │   ├── settle/         ← 💰 정산 관리
│   │   └── cancel/         ← 🚫 취소/반품
│   ├── 📁 partner/[slug]/  ← 업체별 페이지 (동적 라우팅)
│   │   ├── page.tsx        ← SSG 페이지 (42개 업체)
│   │   └── PartnerClient.tsx ← 업체 UI 전체 (7탭 구조)
│   ├── layout.tsx          ← 공통 레이아웃
│   └── page.tsx            ← 메인 페이지
├── 📁 lib/
│   ├── partnerApi.ts       ← 업체용 API (발주, 검색, 송장, 수정/삭제/취소)
│   ├── adminApi.ts         ← 관리자용 API (허브 수정/취소/삭제)
│   ├── supabase.ts         ← Supabase 클라이언트 + 타입
│   └── queries.ts          ← 유틸 (포맷, 날짜 등)
└── next.config.ts          ← output:"export", basePath:"/pack2u"
```

### 뉴파츠 업체 페이지 — 7탭 구조 (완료)

| 탭 | 기능 | 상태 |
|---|---|---|
| 📝 발주 입력 (`orders`) | 코드 자동완성 + 클립보드 붙여넣기 + 발주 접수 | ✅ 완료 |
| 📋 발주 현황 (`history`) | 미배송+당일 필터 / 수정(✏️) / 취소(🚫) / 삭제(🗑) | ✅ 완료 |
| 📦 전용양식 (`exclusive`) | 대리공급 전용양식 발주 내역 조회 | ✅ 완료 |
| 📊 전용 월마감 (`exclusive_settle`) | 월별 전용양식 정산 조회 | ✅ 완료 |
| 💰 월별 정산 (`settle`) | 월별 발주 마감 정산 내역 | ✅ 완료 |
| 🔍 단가조회 (`prices`) | 품목 검색 + 단가 확인 | ✅ 완료 |
| 📬 송장매칭 (`invoice_match`) | 카카오톡 송장 텍스트 자동 매칭 | ✅ 완료 |

### 관리시스템 — 허브 기능 (완료)

| 기능 | 설명 | 동기화 |
|---|---|---|
| ✏️ 주문 수정 | 인라인 편집 → 코드/품목/수량/수취인/단가 등 변경 | ✅ 업체→자동 반영 |
| 🚫 주문 취소 | status='취소'로 변경 | ✅ 업체→자동 반영 |
| 🗑 주문 삭제 | DB 완전 삭제 | ✅ 업체→자동 반영 |

### DB 스키마 (Supabase)

| 테이블 | 역할 | SQL |
|---|---|---|
| `vendors` | 업체 정보 + tab_config | migrate_v4.sql |
| `orders` | 발주 데이터 | schema.sql |
| `products_hub` | 상품 정보 + 단가 | migrate_v2.sql |
| `notices` | 공지사항 | migrate_v4.sql |
| `settlements` | 월별 정산 | schema.sql |
| `exclusive_orders` | 전용양식 | schema.sql |
| `cancel_returns` | 취소/반품 | schema.sql |
| `invoice_match_log` | 송장 매칭 로그 | migrate_v4.sql |

### 미실행 SQL

> [!IMPORTANT]
> `docs/20260703_migrate_v4.sql`을 Supabase SQL Editor에서 실행해야 notices 테이블, invoice_match_log 테이블, vendors.tab_config 컬럼이 생성됩니다.

### 배포

- `npx next build` → `app/out/` 생성 → FTP로 서버의 `/pack2u/` 경로에 업로드
- 접속: `https://도메인/pack2u/partner/newparts/`

---

## 2부: GAS 복구시스템 메뉴 개선 기획

### 현재 문제점

1. **복구 메뉴가 개발 과정의 흔적을 담고 있음**
   - "수식 복구", "spill heal", "백필" 같은 개발 용어가 메뉴에 그대로 노출
   - 직원들이 어떤 상황에서 어떤 복구를 써야 하는지 판단하기 어려움

2. **이전 시스템으로 복구될 위험**
   - D/L열 값 기반 전환(2026-06-17) 이전의 수식 복구 로직이 일부 잔존
   - `_pt_injectOrderSpillFormulas`가 불필요한 수식을 재생성할 수 있음
   - 탭 재생성 시 최신 헤더(15열)와 다른 구버전 헤더가 적용될 수 있음

3. **복구 카테고리 설명이 기술적**
   - "spill heal + D/L 백필 포함"은 직원이 이해 불가
   - "AA1 보정", "K2 감지" 등 내부 셀 참조가 그대로 노출

### 개선 방향

#### 핵심 원칙
- 직원 눈높이의 **증상 중심** 메뉴 (원인이 아닌 증상으로 설명)
- **최신 시스템 기준**으로만 복구 (2026-07-03 기준)
- 위험도 표시로 **어떤 것부터 시도할지** 순서 안내

### 복구 메뉴 구조 (개선안)

```
🛠️ 협력업체 복구시스템 (다이얼로그)
│
├── ⚡ 품목명·단가 빈칸 채우기         ← backfillDL (가장 안전, 먼저 시도)
│   "코드는 입력했는데 품목명이나 단가가 비어있을 때"
│   예상: ~2분 | 데이터 변경 없음
│
├── 📊 단가조회 탭 복구               ← viewerFormula
│   "단가조회 화면에 #REF! 에러가 뜨거나 데이터가 안 보일 때"
│   예상: ~5분 | 단가조회 탭만 수정
│
├── 📝 발주탭 복구                    ← orderFormula
│   "거래처명/품목명/단가가 자동으로 안 채워질 때"
│   예상: ~5분 | 발주탭 수식만 수정
│
├── 📋 양식·디자인 초기화              ← header
│   "1행 헤더가 깨지거나 색상이 사라졌을 때"
│   예상: ~5분 | 디자인만 수정 (데이터 안전)
│
├── 🔧 자동완성 스크립트 재설치         ← script
│   "코드 입력해도 자동완성이 안 될 때"
│   예상: 10개씩 자동 분할 실행 | 완료 시 Chat 알림
│
└── 📑 탭 처음부터 다시 만들기          ← tabs (⚠️ 최종 수단)
    "위 방법으로 해결이 안 될 때 — 탭을 새로 만듭니다"
    예상: ~10분 | ⚠️ 기존 데이터 덮어씀!
    [옵션] 단가조회탭 / 발주탭 / 검색입력탭 / 스크립트
```

### 코드 변경 계획

#### 파일 1: `repairDialog.html` — UI 전체 개선

| 항목 | 변경 내용 |
|---|---|
| 메뉴 아이콘 | 감성적인 아이콘 + 증상 설명 |
| 카테고리 타이틀 | 기술용어 → 직원 친화적 표현 |
| 증상 설명 | 개발 용어 제거, "이럴 때 사용" 더 구체적으로 |
| 상세 정보 | "접근: 발주탭 + 단가조회탭" → "걸리는 시간: ~2분" |
| 순서 안내 | "❶ 먼저 시도 → ❷ → ❸ → 최종수단" 단계 표시 |
| 색상 체계 | 안전(초록) → 보통(파랑) → 주의(빨강) 직관적 표시 |

#### 파일 2: `_partnerMenu.gs` — 복구 로직 최신화

| 함수 | 변경 내용 |
|---|---|
| `_repairBatch_orderFormula_` | `_pt_injectOrderSpillFormulas` 호출 제거 (값 기반 전환 완료 이후 불필요) |
| `_repairBatch_header_` | 최신 헤더 15열 확인 + 누락 열 자동 보정 |
| `_repairBatch_tabs_` | 최신 `_pt_applyRow3Formulas` 사용 확인 + 소비자용 dcMul 처리 검증 |
| `_openRepairDialog_` | 타이틀 매핑 업데이트 |
| `executeRepairBatch` | 카테고리명 → 로깅 개선 |

#### 파일 3: `_partnerMenu.gs` — 메뉴 항목 이름 변경

```
현재: 🛠️ 협력업체 복구시스템
변경: 🔧 업체시트 관리·복구  (더 친근하게)
```

### 검증 항목

- [ ] "단가조회 복구" 실행 시 최신 3행 수식(`_pt_applyRow3Formulas`) 적용 확인
- [ ] "발주탭 복구" 시 D/L열 값 기반 유지 (spill 수식 미삽입) 확인
- [ ] "탭 재생성" 시 최신 헤더 15열 + 조건부서식 적용 확인
- [ ] 소비자용 업체에 할인율(dcMul) 정상 적용 확인
- [ ] "자동완성 재설치" 시 최신 `createViewerNoticeScript_` 코드 주입 확인
- [ ] 10개 초과 시 3개씩 자동 분할 + Chat 완료 알림 확인

### 충돌/의존성 체크

| 체크 항목 | 상태 |
|---|---|
| `_pt_injectOrderSpillFormulas` 최신? | ✅ 이미 2026-06-17 이후 D/L 값 변환만 수행 (안전) |
| `_pt_healOrderSpillFormulas`는 유지? | ✅ 기존 spill 수식 정리용 (안전) |
| `createViewerNoticeScript_`가 최신? | ✅ `priceManager.gs:4215`에 위치, 단일 소스 |
| `_pt_applyRow3Formulas` 최신? | ✅ `_partnerDeploy.gs:128`에 위치, 단일 소스 |
| 탭 재생성(`_partnerMenu.gs:779`) | ⚠️ `_pt_injectOrderSpillFormulas` 호출 중 — 안전하지만 불필요한 호출이므로 정리 권장 |

---

## 작업 우선순위

1. **repairDialog.html** — UI/문구 개선 (충돌 없음, 즉시 가능)
2. **_partnerMenu.gs** — 복구 로직 최신화 + 불필요 호출 정리
3. **검증** — 뉴파츠 시트에서 각 복구 기능 테스트

> [!TIP]
> `_pt_applyRow3Formulas`, `createViewerNoticeScript_` 함수의 최신 상태를 먼저 확인한 후 복구 로직을 업데이트하면 안전합니다.

---

## 추천 추가 개선

1. **복구 이력 로깅**: 누가 언제 어떤 복구를 실행했는지 `설정` 탭이나 PropertiesService에 기록
2. **자동 진단**: 복구 메뉴 열 때 현재 시트 상태를 자동 체크하여 "추천 복구" 표시
3. **웹앱 연동**: 웹앱 관리 페이지에서도 복구 트리거 실행 가능하도록 (중장기)
