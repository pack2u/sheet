# 20260705 변경이력

## 17:00 — 시트 직접 조회 UI 컴팩트화
- `page.tsx`: 전체 UI 리디자인 — 패딩/마진 50% 축소, 글꼴 11px
- 대리공급 탭에 전화번호/주소/배송메시지 열 추가
- 업체별 테이블 기본 열림 (open)
- 통계카드 → 인라인 뱃지로 변경

## 18:00 — 시트 통합 관리 대시보드로 확장 (쓰기 기능 3종 추가)

### 추가된 기능
1. **전용양식 Push (3️⃣)**: 대리발송 탭 → 각 업체 전용양식 시트에 발주 쓰기
2. **발주 수집 (1️⃣)**: 각 업체 발주탭 → 허브 협력업체_발주허브에 집계
3. **송장 배포 (7️⃣)**: 허브 매칭된 송장 → 각 업체 발주탭 N열에 기록

### 변경 파일
- **신규** `lib/sheetActions.ts`: Push/수집/배포 3대 쓰기 로직
  - GAS `_PEP_VENDOR_DIRECT_MAP_` 전체 이식 (12개 업체 열 매핑)
  - Supabase `vendors` 테이블에서 sheet_id 조회
  - 중복 방지, 진행률 콜백 지원
- **수정** `lib/googleSheets.ts`:
  - `SHEET_IDS.HUB` 추가 (허브 시트 ID)
  - `TAB_NAMES.HUB_ORDERS`, `TAB_NAMES.HUB_TEMP` 추가
  - gapi 타입에 `append` 메서드 추가
  - `appendSheet()` 함수 추가
- **수정** `app/admin/sheets/page.tsx`:
  - 제목: "시트 직접 조회" → "시트 통합 관리"
  - 통합 액션 바 추가 (1️⃣ 발주수집, 3️⃣ 전용Push, 7️⃣ 송장배포)
  - 대리공급 탭에 🚀 전용양식 Push 버튼 추가
  - 업체명 Supabase 연동 (prefix 미등록 표시)
  - 액션 실행 로그 표시 영역 추가
