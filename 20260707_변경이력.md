# 20260707 변경이력

## 11:15 — 단가조회 & 발주 자동채움 속도 개선 (Phase 1 + Phase 2)

### 문제
- 업체들이 단가조회 탭 로딩과 발주 자동채움이 느리다고 보고
- 단가조회: IMPORTRANGE 16회 호출 → 시트 열 때 10~30초
- 발주 자동채움: 캐시 미스 시 뷰어 탭 전체 로드 → 5~15초

### Phase 1: 캐시 프리워밍 + TTL 확장

#### 변경 파일: `_partnerLibrary.gs`
1. **onOpen 캐시 프리워밍**: 시트 열 때 `p2u_partnerOnOpen()`에서 뷰어 탭 전체를 읽어 CacheService에 미리 저장 → 이후 onEdit 시 항상 캐시 히트
2. **캐시 TTL 90분**: 1800초 → 5400초 확장 (단가는 하루 1~2회만 변경)
3. **단일 행도 캐시 저장**: 전체 로드 발생 시 다중행/단일행 무관하게 캐시 저장

### Phase 2: IMPORTRANGE 16회 → 1회 통합

#### 변경 파일: `_partnerDeploy.gs`
1. **`_data` 숨김 탭 자동 생성**: `_pt_ensureDataTab()` 추가
   - `=IMPORTRANGE(hub, "전체 그룹 단가표!A:Z")` 1회만 호출
   - 숨김 + 보호(경고) 설정
2. **`_pt_applyRow3Formulas` v2**: `_data` 탭 로컬 참조 수식
   - `_pt_applyRow3FormulasLocal_()`: XLOOKUP이 `'_data'!C:C` 등 로컬 참조
   - 동적 열(G, I, J)은 INDIRECT로 `'_data'!` 참조
   - `_pt_applyRow3FormulasLegacy_()`: 기존 IMPORTRANGE 방식 폴백 보존
3. **자동 폴백**: `_data` 탭 생성 실패 시 기존 방식으로 자동 전환

#### 변경 파일: `_partnerMaintenance.gs`
- **`partnerMigrateToDataTab()`**: 기존 업체 일괄 마이그레이션 함수 추가

#### 변경 파일: `_partnerMenu.gs`
- 📊 상태 확인/진단 메뉴에 `⚡ _data 탭 마이그레이션 (속도 최적화)` 추가

### 예상 효과
| 항목 | 기존 | 개선 후 |
|---|---|---|
| 발주 코드 입력 (콜드 스타트) | 5~15초 | 0.5~1초 |
| 발주 코드 입력 (캐시 히트) | 0.5~1초 | 0.5~1초 |
| 단가조회 탭 로딩 | 10~30초 | 3~8초 |

## 13:20 — OC(부엉이커피) 대리공급 전용양식 설정 추가

### 변경 파일: `_partnerExclusivePush.gs`
1. **`_PEP_EXCLUSIVE_FORM_HEADERS_`에 OC 추가**: 12열 (팩투유 기본 양식)
   - 송장번호, 적요, 받는사람, 전화번호, 주소, 상품명, 수량, 배송메세지, 보내는사람, 보내는분전화, 보내는분주소, 이카운트코드
2. **`_PEP_VENDOR_DIRECT_MAP_`에 OC 추가**: 소스→전용양식 열 매핑 (12열)
3. **`_PEP_VENDOR_LABELS_`에 OC 추가**: "부엉이커피"

## 13:23 — 단가조회 #REF! 버그 수정 (INDIRECT 제거)

### 원인
- Phase 2에서 G3/I3/J3 수식에 `INDIRECT("'_data'!" & ...)` 사용
- IMPORTRANGE 스필 범위 내 셀을 INDIRECT로 참조 시 #REF! 발생 (Google Sheets 제한)
- "단가조회 복구" 실행해도 동일한 INDIRECT 수식이 다시 적용되어 수정 불가

### 수정 내용: `_partnerDeploy.gs`
- `_pt_applyRow3FormulasLocal_()` 함수 수정
  - **INDIRECT 완전 제거** → K2 값을 GAS에서 직접 읽어 컬럼 문자로 변환
  - 예: K2=7 → `'_data'!G:G` 직접 참조 (A~F열과 동일 패턴)
- `_pt_colLetter_()` 유틸 함수 추가: 열 번호 → 열 문자 변환 (1→A, 7→G, 26→Z, 27→AA)

### 적용 방법
- 허브에서 **"단가조회 복구"** (뷰어 3행 수식 일괄 복구) 재실행

## 13:27 — 품목명·단가 백필 함수 보강 (#REF! 내성)

### 변경 파일: `_partnerHelpers.gs`
- `_pt_backfillOrderDL()` 수정
  - `.getValues()` → `.getDisplayValues()` 변경 (에러 값 문자열로 감지)
  - price에 `#REF!`/`#N/A` 필터 추가 (기존엔 코드만 필터)
  - **`_data` 탭 폴백**: 뷰어 G열 #REF! 감지 시 `_data` 탭에서 직접 단가 조회
  - 복구 순서: 수식 복구(3행) → 값 채우기(D/L) → 일관된 결과 보장

## 13:53 — _data 탭 방식 롤백 → 기존 IMPORTRANGE 방식 복원

### 원인
- `_data` 탭이 `전체 그룹 단가표!A:Z` (26열 전체)를 IMPORTRANGE하여 오히려 느려짐
- 복구 시 `_data` 탭 생성 + IMPORTRANGE 설정 추가 시간 소요
- #REF! 문제 + 속도 문제 동시 발생

### 수정: `_partnerDeploy.gs`
- `_pt_applyRow3Formulas()` → `_pt_applyRow3FormulasLegacy_()` 직접 호출로 변경
- `_data` 탭 자동 생성 로직 비활성화 (코드는 보존, 호출만 제거)
- **캐시 프리워밍(Phase 1)은 유지** (onEdit 속도 개선은 유효)

### 복구 방법
- "단가조회 복구" 재실행 → 기존 IMPORTRANGE 수식으로 복원됨
- 이미 생성된 `_data` 탭은 수동 삭제 또는 방치해도 무방

## 14:22 — 발주수집 시 품목명·단가 채우기 제거 (시간초과 방지)

### 원인
- 발주수집 시 매 업체마다 `_pt_autoCleanupBeforeCollect_` 실행
- 이 함수가 뷰어탭 로드 + D/L열 복원을 수행하여 6분 시간초과 발생

### 수정: `_partnerOrders.gs`
- 발주수집 루프 내 `_pt_autoCleanupBeforeCollect_()` 호출 주석 처리
- 빈행 정리+품목명·단가 복원은 별도 메뉴(**🧹 발주탭 자동 정리**)에서 수동 실행

## 14:35 — A열 값 기반 전환 + 뷰어탭 이름 "단가조회" 통일

### 배경
- A1의 ARRAYFORMULA가 `'뷰어탭이름'!$AA$1`을 참조 → 뷰어탭 이름 변경 시 #REF!
- 뷰어탭 이름이 업체마다 다름 ("팩투유 단가조회", "리바이 뷰어" 등)
- ARRAYFORMULA 스필 충돌 위험

### 수정

#### `_partnerLibrary.gs` (onEdit)
- C열 입력 시 **A열에 거래처명 값 직접 기록** (설정탭 B5에서 읽기)
- D/L/B열과 동일한 값 기반 패턴

#### `_partnerHelpers.gs`
- `_pt_injectOrderSpillFormulas`: A1 ARRAYFORMULA → "거래처명(자동)" 텍스트
  - 기존 ARRAYFORMULA 표시값 보존 후 수식 제거
  - 빈 A셀에 설정 B5 거래처명 자동 채움
- `_pt_healOrderSpillFormulas`: A열 복구도 값 기반으로 변경
  - #REF! 에러값 → 빈값 변환 후 거래처명 채움

#### `_partnerMaintenance.gs`
- `partnerUnifyViewerTabNameOwner()` 신규 추가
  - 전체 업체 뷰어탭 이름 → "단가조회" 변경
  - A열 ARRAYFORMULA → 값 기반 일괄 마이그레이션

#### `_partnerMenu.gs`
- 메뉴: "📋 뷰어탭 이름 통일 + A열 마이그레이션" 추가
- "_data 탭 마이그레이션" 메뉴 제거 (롤백됨)

### 실행 방법
- **💼 협력업체 관리 > 📊 상태 확인 / 진단 > 📋 뷰어탭 이름 통일 + A열 마이그레이션**

## 15:12 — onEdit 속도 최적화 (설정탭 캐시 + 쓰기 최소화)

### 수정: `_partnerLibrary.gs`

#### 거래처명(vendorName) 캐시 통합
- **기존**: C열 편집 시 매번 `설정!B5` 접근 (~150ms/회)
- **변경**: vendorName을 codeMap 캐시 메타에 포함 (`{c:청크수, vn:'업체명'}`)
- onOpen 프리워밍 시 1회만 읽어 캐시 → 이후 90분간 재사용
- 캐시에 없을 때만 fallback으로 설정탭 접근

#### 쓰기 횟수 최소화
- **기존**: A, B, D, L, J, N열 개별 setValues (최대 5~6회)
- **변경**: A+B+C(원본)+D를 1~4열 묶어서 1회 쓰기 (인접 열 합침)
- setValues 호출: 최대 6회 → 2~3회

#### 최적화 결과 (캐시 히트 경로)
- **기존**: ~650-850ms
- **변경**: ~300-500ms (거의 즉시)

## 15:57 — 송장매칭 품목명 인식 개선

### 배경
- 한진 택배 등 전용양식에 품목명이 포함되어 있으나, 헤더가 없어 itemIdx 감지 실패
- 동일 수취인 + 다품목(바디/캡) 주문 시 정확한 품목 매칭 필요

### 수정: `_partnerHelpers.gs` — `_pt_ingestInvoiceSheetTabIntoMap`
- 헤더 기반 `itemIdx` 감지 실패 시 **데이터 샘플링 기반 품목명 열 추정** 추가
- 2~5행 데이터에서 품목명 패턴 탐색:
  - 고점수: 용기, 캡, 바디, 뚜껑, Ø, 파이, PET, PP, 세트, 팩, 박스, 접시
  - 중점수: 한글+숫자 조합
  - 제외: 순수 숫자, 주소 패턴, 전화번호 패턴
- 기존 `_pt_scoreInvoiceCandidate` 스코어링 로직은 이미 구현되어 있었음 → 품목명 열만 정확히 잡으면 자동 작동

## 16:21 — 카카오 송장매칭 품목명 기반 매칭 추가

### 배경
- 한진 택배 등의 엑셀 데이터를 붙여넣으면 품목명이 포함되어 있음
- 동일 수취인(누림트레이딩) + 다품목(바디 10개/캡 10개)일 때 정확한 행 배정 필요
- 기존: 이름만 매칭 → FIFO(선입선출) 순서로 배정 → 바디/캡 구분 불가

### 수정 파일

#### `InvoiceMatch_협력업체용.gs` — `_parseInvoicePairs_`
- **TSV(탭 구분) 형식 자동 감지**: 탭 3개 이상인 줄이 60% 이상이면 TSV 모드
- TSV 모드에서 열 자동 감지:
  - 1차: 헤더 키워드 (송장번호/수취인/품목)
  - 2차: 데이터 샘플링 (10~14자리 숫자 = 송장, 한글 2~15자 = 수취인, 용기/캡/PET = 품목)
- 반환값에 `itemName` 필드 추가: `{ tracking, name, itemName }`

#### `_partnerWebApp.gs` — `_imParseAndMatch_`
- 동일 수취인 큐에 2건 이상 + 파싱된 itemName 존재 시 **품목명 스코어링 적용**
- 토큰 매칭 (+10/토큰) + 완전 포함 (+50) + 바디/캡/소/중/대 구분 (+100/-200)
- 결과에 `itemName` 포함하여 UI 표시

#### `invoiceMatchSidebar.html`
- 결과 테이블에 "품목" 열 추가
- 품목명이 있을 때 15자까지 축약 표시

## 16:35 — 카카오 송장매칭 Gemini AI 기반 전환 ★ 대규모 변경

### 배경
- 택배사마다 형식이 달라(한진/로젠/CJ/우체국) 정규식 파싱에 매번 코드 수정 필요
- UTF-16 인코딩, 헤더 유무, 송하인/수취인 구분 등 경우의 수가 너무 많음
- Gemini 3.5 Flash로 전환하여 유지보수 제로 달성

### 수정 파일

#### `_partnerExclusivePush.gs` — Gemini 파싱 함수 3개 추가
1. **`_parseInvoicePairsWithGemini_(rawText)`**: 텍스트 → JSON 파싱
   - 프롬프트: 수취인/송장번호/품목명 추출, 송하인/택배사 제외
   - `responseMimeType: "application/json"` 사용
   - 실패 시 `null` 반환 → 폴백 트리거
2. **`_parseInvoiceImageWithGemini_(base64Data)`**: 이미지 → JSON 파싱 (OCR+파싱 통합)
3. **`parseAndMatchInvoiceImage(fileId, base64Data)`**: 이미지 직접 분석+매칭

#### `_partnerWebApp.gs` — `_imParseAndMatch_` 수정
- 시그니처에 `preParsedPairs` 파라미터 추가
- 파싱 순서: `preParsedPairs` → Gemini → 기존 정규식(폴백)
- 로그에 파싱방식 기록 (`gemini` / `regex-fallback` / `pre-parsed`)

#### `invoiceMatchSidebar.html` — UI 업데이트
- "🔍 분석" → "🤖 AI 분석" 
- "🖼️ 이미지 OCR" → "🤖 이미지 AI"
- 이미지 탭: 2단계(OCR→파싱) → 1단계(AI 직접 분석)로 통합
- 기존 엑셀/텍스트 파일 업로드 + UTF-16 인코딩 처리는 유지

### 아키텍처 변경
```
[기존]
텍스트: rawText → _parseInvoicePairs_(정규식) → 5단계 이름 매칭
이미지: 이미지 → ocrImageToText(OCR) → 텍스트 → _parseInvoicePairs_ → 매칭

[변경]
텍스트: rawText → Gemini Flash(JSON) → 5단계 이름 매칭
                    ↓ 실패 시
                    → _parseInvoicePairs_(정규식 폴백)
이미지: 이미지 → Gemini Vision(JSON) → 5단계 이름 매칭
                    ↓ 실패 시
                    → ocrImageToText → _parseInvoicePairs_ 폴백
```

---

## 2026-07-15 09:55 — 협력업체 템플릿 파일 ID 교체

### 문제
- 업체 시트 생성 시 "템플릿 파일 접근 실패: 입력한 ID에 해당하는 항목이 없습니다" 에러 발생
- 기존 템플릿 파일(`1ZT9hqXXOSuSYRS6gYaJUhvVpTHDql6HokqExJdPcbiA`)이 삭제됨

### 변경 내용
| 파일 | 변수 | 변경 |
|---|---|---|
| `_partnerDeploy.gs` L20 | `_PT.TEMPLATE_ID` | 신규 템플릿 ID로 교체 |
| `priceManager.gs` L9 | `MASTER_TEMPLATE_ID` | 동일하게 신규 ID로 교체 |

- 신규 템플릿: `1UL96S9_mNpJF4Yyxye1fIljOp0pInx-ps03N-NrILho`

---

## 2026-07-15 11:09 — N열 상태값 수식 파싱 오류 (#ERROR!) 수정

### 문제
- N열의 `상태(자동)` ARRAYFORMULA 수식 내에서 `AND()` 및 `OR()` 함수 사용 시 구글 시트 엔진에서 "수식 파싱 오류가 발생했습니다" (#ERROR!) 오류 발생

### 해결 방법
- `_partnerHelpers.gs` 내 `_pt_injectOrderSpillFormulas` 수식 정의 수정
- ARRAYFORMULA 호환을 위해 `AND`를 곱셈 연산자(`*`)로, `OR`을 덧셈 연산자(`+`)로 교체
- 변경 수식:
  - `AND(...)` → `ISNUMBER(SEARCH("품절",st)) * NOT(ISNUMBER(SEARCH("품절+7",st)))`
  - `OR(...)` → `((E2:E="") + (E2:E=0) + (F2:F="") + (G2:G="") + (H2:H="") > 0)`
