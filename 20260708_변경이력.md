# 20260708 변경이력

## 08:25 — 협력업체용 카카오 송장매칭 Gemini AI 파싱 동기화

### 배경
- 7/7에 허브 전용양식 시트(`_partnerWebApp.gs`)에 적용된 Gemini AI 파싱이 협력업체 개별 시트용 코드(`InvoiceMatch_협력업체용.gs`)에 미반영
- 7/6 라이브러리 패턴 전환 완료 → 허브의 함수만 수정하면 모든 업체 시트에 자동 반영

### 변경 파일 1: `InvoiceMatch_협력업체용.gs` — parseAndMatchInvoiceTextLocal()
1. **3단계 파싱 체인 추가**: 정규식만 → TSV헤더(`_parseStructuredTSV_`) → Gemini AI(`_parseInvoicePairsWithGemini_`) → 정규식(폴백)
2. **수량 열(qtyCol) 자동 탐지**: "수량", "qty", "quantity", "갯수", "개수" 키워드로 헤더 탐색
3. **송하인/택배사 itemName 필터**: 팩투유/한진/로젠/CJ 등 이름이 itemName에 들어오면 자동 제거
4. **품목명 기반 행 스코어링**: 동일 수취인 다품목(바디/캡) 시 토큰매칭(+10), 완전포함(+50), 바디/캡 구분(+100/-200) 적용
5. **nameItemRowMap + rowCapacity**: 같은(이름+품목) 조합은 같은 행에 append, 수량 기반 용량 체크
6. **반환값 확장**: `itemName`, `parseMethod` 필드 추가

### 변경 파일 2: `InvoiceMatch_협력업체용.gs` — _getInvoiceMatchHtmlSimple_() HTML UI
1. 분석 버튼: "🔍 분석" → "🤖 AI 분석"
2. 결과 테이블: "품목" 열 추가 (15자 축약 표시)
3. 요약에 파싱 방식(tsv-header/gemini/regex-fallback) 표시

### 변경 파일 3: `_partnerExclusivePush.gs` — Gemini 폴백 모델 수정
- 폴백 체인: gemini-3.5-flash → ~~gemini-2.0-flash~~ → gemini-1.5-flash
- 변경: gemini-3.5-flash → gemini-1.5-flash (2단계로 축소)
- 사유: Gemini 2.0 서비스 종료

### 영향 범위
- **라이브러리 패턴**: `clasp push`만 하면 모든 업체 시트에 즉시 반영 (스크립트 재설치 불필요)
- 허브 전용양식 시트: 이미 적용됨 (7/7 변경)
- 협력업체 개별 시트: 이번 변경으로 동기화 완료

## 08:50 — 협력업체용 송장매칭 사이드바에 엑셀 첨부 + 이미지 AI 탭 추가

### 배경
- 허브 관리자용 사이드바(`invoiceMatchSidebar.html`)에는 엑셀 파일 업로드, 이미지 AI 분석 기능이 있으나
- 협력업체용 사이드바(`InvoiceMatch_협력업체용.gs` 내 `_getInvoiceMatchHtmlSimple_()`)에는 textarea만 존재
- 업체에서도 엑셀 파일 드래그앤드롭 및 이미지 OCR 기능 필요

### 변경 파일 1: `InvoiceMatch_협력업체용.gs` — _getInvoiceMatchHtmlSimple_() v11
1. **탭 바 추가**: 📋 텍스트/엑셀 | 🤖 이미지 AI
2. **엑셀 업로드 드롭존**: 드래그앤드롭 + 클릭 파일 선택
   - XLSX.js CDN 추가 (클라이언트 사이드 엑셀→TSV 변환)
   - UTF-16 LE BOM(사방넷 엑셀) 자동 감지 + 디코딩
3. **이미지 AI 탭**: 이미지 붙여넣기(Ctrl+V) / 드래그앤드롭 / 파일 선택
   - 이미지 프리뷰 + 삭제 버튼
   - `parseAndMatchInvoiceImageLocal()` 호출
4. **이미지 파일 자동 탭 전환**: 엑셀 드롭존에 이미지 파일이 들어오면 자동으로 이미지 AI 탭 전환
5. **결과 테이블 열 순서**: 이름 | 품목 | 송장번호 | 행 (허브와 동일)

### 변경 파일 2: `InvoiceMatch_협력업체용.gs` — parseAndMatchInvoiceImageLocal() 신규
- Gemini Vision(`_parseInvoiceFileWithGemini_`)으로 이미지→JSON 파싱
- 결과를 TSV 텍스트로 변환 → `parseAndMatchInvoiceTextLocal()` 재사용
- `parseMethod: "gemini-image"` 표시

### 변경 파일 3: `_partnerLibrary.gs` — p2u_parseAndMatchInvoiceImage() 신규
- 업체 시트에서 `Pack2U.p2u_parseAndMatchInvoiceImage(base64)` 호출용 래퍼

### 변경 파일 4: `InvoiceMatch_협력업체용.gs` — getPartnerInvoiceMatchCode_()
- 주입 코드에 `parseAndMatchInvoiceImageLocal` 래퍼 추가

### 배포
- **clasp push**만으로 즉시 반영 (라이브러리 패턴)
- **스크립트 재설치 필요**: 주입 코드(`getPartnerInvoiceMatchCode_`)에 새 함수 추가됨

## 09:22 — 로엔그린 B열 '이슈' Deploy 헤더 누락 수정

### 문제
- `_partnerExclusivePush.gs`의 `_PEP_EXCLUSIVE_FORM_HEADERS_`와 `_PEP_VENDOR_DIRECT_MAP_`에는 B열 '이슈'가 이미 추가되어 있었으나
- `_partnerDeploy.gs`의 Deploy 헤더(`headerCsv`)에 반영 누락
- 결과: 시트 생성/헤더 갱신 시 '이슈' 열 없이 19열로 생성

### 수정: `_partnerDeploy.gs`
- 로엔그린(LG) `headerCsv`: `"송장번호|보내는사람..."` → `"송장번호|이슈|보내는사람..."`
- 마지막 열: `운송장번호` 중복 → `택배수량`으로 수정 (20열)

## 09:30 — 전용양식 B열 "적요"→"이슈" 일괄 변경 + 헤더 갱신 메뉴 등록

### 배경
- `_partnerExclusivePush.gs`의 `_PEP_EXCLUSIVE_FORM_HEADERS_` 배열은 이미 "이슈"로 통일되어 있었으나
- `_partnerDeploy.gs`의 `EMBEDDED_VENDOR_EXCLUSIVE_MASTER_ROWS_` (Deploy 헤더)에는 B열이 여전히 "적요"로 남아있었음
- 또한 `partnerRepairExclusiveFormHeaders` 함수가 메뉴에 등록되지 않아 실행 불가

### 변경 파일 1: `_partnerDeploy.gs` — Deploy 헤더 B열 일괄 수정
- **변경된 업체** (B열 적요→이슈): AP, AJ, KR, TY, HP, HR, BW, OC, GS, GW, NK, IW, HU, SW
- **변경 없음**: PS (B열이 "받는사람"), JC (B열이 "거래처명"), LG (이미 "이슈")
- **유지**: GS/GW의 16번째 열 "적요", HR의 "적요(배송메시지)"/"적요" (데이터 필드, B열 아님)

### 변경 파일 2: `_partnerMenu.gs` — 메뉴 등록
- `📊 상태 확인 / 진단` 서브메뉴에 `🔧 전용양식 헤더 일괄 업데이트` 추가

### 적용 방법
- `clasp push` 후 **💼 협력업체 관리 > 📊 상태 확인/진단 > 🔧 전용양식 헤더 일괄 업데이트** 실행
- 모든 업체의 전용양식 탭 1행 헤더가 `_PEP_EXCLUSIVE_FORM_HEADERS_` 정의에 맞춰 갱신됨

---

## 09:56 — 하나팩(HP) 전용양식 설정 누락 보정

### 문제
- HEADERS, LABELS, DIRECT_MAP 3곳 모두 HP 미등록 → 헤더 업데이트/Push 불가
- Deploy 헤더에만 존재했음

### 변경 파일: `_partnerExclusivePush.gs`
- `_PEP_EXCLUSIVE_FORM_HEADERS_`: HP 11열 추가
- `_PEP_VENDOR_LABELS_`: HP: "하나팩" 추가
- `_PEP_VENDOR_DIRECT_MAP_`: HP 소스→타겟 매핑 추가 (보내는사람 앞/받는사람 뒤 구조)

---

## 16:23 — 송장매칭 동일이름 한 행 몰림 버그 수정

### 문제
- 부원 등 품목명/수량 열이 없는 양식에서 동일 이름(김교근, 이애란 등)이 2건 이상이면
  2번째 송장이 첫 번째 행에 append되어 한 셀에 2개 송장이 몰림

### 원인
- `nameItemRowMap` 재사용 시 품목명·수량 열 모두 없으면 **무제한 append**로 동작
- 큐에 같은 이름 행이 남아있어도 재사용 우선

### 수정 (`InvoiceMatch_협력업체용.gs` 551~585행)
- 품목명 있음 → 같은 행 append (기존 동작 유지)
- 품목명 없고 수량 열 있음 → 수량 기반 용량 체크 (기존 동작 유지)
- **품목명도 없고 수량 열도 없음** → 큐에 같은 이름 행이 남아있으면 새 행 배정, 큐 소진 시에만 마지막 행에 append

---

## 16:36 — 실제 실행 경로(_partnerWebApp.gs) 슬래시 전처리 + 동일이름 분배 수정

### 문제
- `InvoiceMatch_협력업체용.gs`만 수정했으나, 허브 사이드바의 실제 호출 경로는 `_partnerWebApp.gs`의 `_imParseAndMatch_`
- 슬래시(`/`) 포함 송장번호 여전히 인식 불가

### 수정 (`_partnerWebApp.gs` 907~993행)
- **슬래시 전처리**: `이애란 259421457945 / 259421457956` → 개별 줄로 분리
- **동일이름 분배**: `InvoiceMatch_협력업체용.gs`와 동일한 3분기 로직 적용

---

## 16:46 — 슬래시 그룹 추적 (같은 줄 송장은 같은 행에 배정)

### 문제
- 슬래시 전처리로 개별 줄 분리 후, 4건이 2:2가 아닌 1:3으로 편향 배분
- 원인: 같은 줄의 `/` 구분 송장이 분리되면 별개 pair로 처리되어 순서 의존적 분배

### 수정 (`_partnerWebApp.gs`)
- **_slashGroupMap**: 전처리 시 같은 줄의 송장번호에 동일 그룹 ID 부여
- **파싱 후 주입**: pairs에 `_slashGroup` 속성 부여
- **slashGroupRowMap**: 매칭 시 같은 그룹은 무조건 같은 행에 append
- 그룹 등록은 첫 매칭 시에만 (이후 동일 그룹은 해당 행에 append)

---

## 16:50 — 부원(BW) 전용양식 13열 레이아웃 변경

### 변경 전 (18열)
- A:송장번호, B:이슈, C:받는사람, D:전화번호, E:주소, F:우편번호, G:상품명(수량 포함)

### 변경 후 (13열)
- A:송장번호 | B:이슈 | C:받는사람 | D:전화번호 | E:주소 | F:우편번호 | G:(빈열) | H:상품명 | I:수량 | J:배송메세지 | K:보내는사람 | L:보내는주소 | M:보내는전화

### 수정 파일: `_partnerExclusivePush.gs`
1. **_PEP_EXCLUSIVE_FORM_HEADERS_**: BW 13열 헤더 추가
2. **_PEP_VENDOR_DIRECT_MAP_**: 새 열 위치에 맞게 매핑 업데이트
   - 상품명: G(6) → H(7)
   - 수량: 상품명에 합침 → I(8) 별도
   - 배송메세지: M(12) → J(9)
   - 보내는사람: P(15) → K(10)
   - 보내는주소: Q(16) → L(11)
   - 보내는전화: R(17) → M(12)
3. **BW 상품명+수량 합치기 로직 제거** (2곳)
   - 기존: "품목명 2개---1박스" 합침 → G열
   - 변경: H열=품목명만, I열=수량(별도)

---

## 17:00 — 전용양식 이슈(B열) 송장수집 반영 버그 4건 수정

### 문제
- 전용양식 B열 이슈 내용이 송장수집 시 허브/임시기록에 반영되지 않음

### 원인 및 수정 (`_partnerOrders.gs`)
1. **송장번호 없는 행에서만 수집** → 송장 유무 무관 수집으로 변경
2. **UID 패턴 `^[A-Z]{2,3}[-_]`만 감지** → `\d{4}-ph-`(대리공급 UID)도 추가
3. **AX열(49) 직접 확인 누락** → AX열 우선 확인 후 역방향 탐색 폴백
4. **허브 기록도 송장 없는 행만** → 송장 유무 무관 기록

---

## 17:14 — 중앙 사이드바 다중 송장 분배 오류 수정

### 문제
- 같은 이름 4건(2그룹)이 1행에 전부 몰림 (2:2 분배 안 됨)
- 업체 시트에서는 수량 열 기반 배분으로 정상 작동했으나, 중앙은 `nameItemRowMap` 재사용으로 그룹 무시

### 원인
- 새 그룹의 첫 pair가 `slashGroupRowMap`에 없을 때 → `nameItemRowMap`에서 동일 이름으로 이전 그룹의 행 재사용

### 수정 (`_partnerWebApp.gs`)
1. **공백 구분 다중 송장 전처리(패턴2) 추가**: `오영임 259421457993 259421458004` → 개별 줄로 분리 + 같은 그룹 ID
2. **핵심: `_slashGroup`이 있는 pair는 `nameItemRowMap` 재사용 스킵** → 큐에서 새 행 할당
   - `if (assignedRows.length === 0 && !p._slashGroup)` 조건 추가
3. 디버그 로그 추가: `[송장매칭] 슬래시 그룹:` JSON 출력

---

## 17:20 — 이슈 수집 조건 복구

- `_partnerOrders.gs`: 송장번호가 없는 행에서만 이슈 수집하도록 복구
- 변경 전: `if (!_bIssue) continue;` → 발송완료 행도 딸려옴
- 변경 후: `if (!_bIssue || _bInvoice) continue;` → 미발송 행만 수집

---

## 17:34 — A열(거래처명) 백필 추가

### 문제
- A열이 ARRAYFORMULA에서 값 기반으로 전환(7/7)됐으나, 품목명 채우기(D/L 백필)에서 A열을 누락

### 수정 (`_partnerHelpers.gs` → `_pt_backfillOrderDL`)
1. 설정탭 B5(거래처명) 조회 추가
2. C열에 코드가 있고 A열이 비어있으면 → 거래처명 자동 채우기
3. `vMap`이 비어있어도 A열 채우기는 계속 진행 (`hasVMap` 플래그)

---

## 22:23 — 전용양식 미발주 엑셀 다운로드 기능 구현

### 기능
- 전용양식 C열~ 미발주(송장번호 없는) 데이터를 **엑셀(.xlsx)로 PC 직접 다운로드**
- 구글드라이브 불필요 (SheetJS 브라우저 생성)
- 카톡으로 전송 용도

### UI (중앙 사이드바)
- `📥 미발주 엑셀 다운로드` 버튼 → 체크박스 목록 표시
- `전체선택` / `전체해제` 버튼
- `📥 선택 항목 다운로드` — 체크된 행만 엑셀 생성
- `📦 전체 미발주 다운로드` — 미발주 전체 엑셀 생성
- 각 행에 수취인 · 상품명 ×수량 표시 (헤더 자동 감지)

### 수정 파일
1. `_partnerExclusivePush.gs`: `getExclusiveFormDataForDownload(fileId)` 함수 추가
2. `invoiceMatchSidebar.html`: CSS + HTML + JS 추가 (체크리스트 UI + SheetJS 다운로드)

---

## 22:32 — 엑셀 저장 위치 선택 + 자동 번호

- `showSaveFilePicker` API로 저장 위치 선택 대화상자 표시
- 같은 날 2번째부터 파일명에 `-2`, `-3` 자동 추가
- 미지원 브라우저는 기본 다운로드 폴더에 자동 저장 (폴백)
- 수정: `invoiceMatchSidebar.html`

---

## 23:00 — 중복 발주 자동 감지

### 기능
- **발주탭(대리판매) + 전용양식(대리공급)** 모두 대상
- 같은 수취인 + 같은 품목 조합이 2건 이상이면 중복으로 감지
- 이미 송장번호가 입력된(발송완료) 행은 제외
- **중복 행에 노란색(#FFF9C4) 강조 표시** 자동 적용
- 업체별 중복 건수 요약 다이얼로그 표시

### 메뉴 위치
```
📦 대리발송 발주시스템 > 🔍 중복 발주 감지 (발주탭+전용양식)
```

### 수정 파일
1. `_partnerOrderAudit.gs`: `partnerCheckDuplicateOrdersOwner()` + 유틸 함수 4개 추가
2. `_partnerMenu.gs`: 메뉴 항목 추가
