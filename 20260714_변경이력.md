# 20260714 변경이력

## 09:24 — 와이에스(YS) 전용양식 신규 등록

### 배경
- 와이에스 업체의 전용양식을 첨부 이미지(와이에스 송장틀.xlsx) 기반으로 신규 등록
- 이미지 헤더: 받는사람|전화번호1|전화번호2|우편번호|주소|상품명1|운임구분|수량(A타입)|배송메시지|보내는사람(지정)|전화번호1(지정)|주소(지정) (12열)
- 시스템 규칙에 따라 A=송장번호, B=이슈 추가 → 총 14열

### 변경 파일: `_partnerExclusivePush.gs`

#### 1. `_PEP_EXCLUSIVE_FORM_HEADERS_`에 YS 헤더 14열 추가
```
YS: ["송장번호","이슈","받는사람","전화번호1","전화번호2","우편번호","주소","상품명1","운임구분","수량(A타입)","배송메시지","보내는사람(지정)","전화번호1(지정)","주소(지정)"]
```

#### 2. `_PEP_VENDOR_LABELS_`에 YS 라벨 추가
```
YS: "와이에스"
```

#### 3. `_PEP_VENDOR_DIRECT_MAP_`에 YS 소스→전용양식 열 매핑 추가
- totalCols: 14
- fixedValues: { 8: "선불" } — 운임구분 I열 고정
- phoneTargetCols: [3, 12] — D(전화번호1), M(전화번호1-지정) 텍스트 서식
- 소스→전용양식 열 매핑:
  | 소스 열 | 소스 헤더 | 전용양식 열 | 전용양식 헤더 |
  |---------|-----------|------------|-------------|
  | M(12) | 거래처명 | C(2) | 받는사람 |
  | I(8) | 모바일 | D(3) | 전화번호1 |
  | J(9) | 주소1 | G(6) | 주소 |
  | E(4) | 품목명 | H(7) | 상품명1 |
  | G(6) | 수량 | J(9) | 수량(A타입) |
  | K(10) | 배송메세지 | K(10) | 배송메시지 |
  | Q(16) | 보내는분 | L(11) | 보내는사람(지정) |
  | R(17) | 보내는분전화 | M(12) | 전화번호1(지정) |
  | S(18) | 보내는분주소 | N(13) | 주소(지정) |

### 배포 필요
- `clasp push` 후 협력업체 파일에 전용양식 탭 생성 필요
- 메뉴: 💼 협력업체 관리 > ➕ 시트 생성 또는 전용양식 탭 생성

---

## 16:17 — 대리공급업체 무관 접두사(DS 등) 제외 및 2순위 품목명 앞 영문 2글자 인식 보완

### 배경
- 이카운트코드 앞 2자리(예: DS)가 대리공급업체 테이블에 존재하지 않아 **제외**되는 경우, 차순위(2순위)로 품목명(E열)의 앞부분에 있는 영문 2글자(예: GW)를 인식하여 정상적으로 업체를 분류하도록 1순위/2순위 구조를 정교화.
- 품목명 앞부분의 영문 2글자 추출 시 한글, 대괄호(`[대리]`), 공백 등이 섞여 있더라도 정확하게 매칭할 수 있도록 정규식 및 로직을 개선.

### 변경 파일: `_partnerExclusivePush.gs`

#### 1. `partnerPushOrdersToExclusiveForms` 함수 수정
- **품목명 앞 영문 2글자 추출 보완**: `rawName.replace(/^[^a-zA-Z]*/, "").match(/^([a-zA-Z]{2})/)` 방식을 적용하여, 품목명 가장 앞에 위치한 영문 2글자만 정확히 식별(기존의 무작위 첫 영문 매칭 개선).
- **1순위/2순위 우선순위 재구조화**:
  - **1순위**: 이카운트코드(`codePfx`)가 `DIRECT_MAP` 또는 `LABELS`에 존재하면 `pfx`로 선택.
  - **2순위**: 1순위 코드 제외(미등록 등) 시, 품목명 앞 영문 2글자(`namePfx`)가 `DIRECT_MAP` 또는 `LABELS`에 존재하면 `pfx`로 선택.

#### 2. `partnerRebuildTempRecords` 함수 수정
- 메인 Push 함수와 동일하게 1순위(이카운트코드)/2순위(품목명 앞 영문 2글자) 업체코드 매핑 로직을 이식.
- 임시기록에 등록되는 업체 코드 열(`tRow.push(pfx)`)에 `codePfx` 대신 1순위/2순위 검증을 거친 최종 `pfx`가 기록되도록 개선.

### 배포 필요
- `clasp push` 로컬 파일 배포 필요.
