# 변경이력 — 2026-07-15 (작성: 2026-07-16 00:27)

## 1. 준테크(JT) 업체 전용양식 배열 등록
**파일:** `_partnerExclusivePush.gs`

- `_PEP_VENDOR_LABELS_`에 `JT: "준테크"` 추가
- `_PEP_EXCLUSIVE_FORM_HEADERS_`에 JT 19열 헤더 정의
  - AP(올팩)과 동일 구조, C(2)에 `주문번호(지정)` 열 추가된 점만 다름
  - A(0):송장번호, B(1):이슈, C(2):주문번호(지정), D(3):보내는사람(지정),
    E(4):전화번호1(지정), F(5):전화번호2(지정), G(6):우편번호(지정), H(7):주소(지정),
    I(8):받는사람, J(9):전화번호1, K(10):전화번호2, L(11):우편번호,
    M(12):주소1, N(13):상품명1, O(14):수량(A단위), P(15):배송메시지,
    Q(16):(빈열), R(17):유입구분, S(18):유입
- `_PEP_VENDOR_DIRECT_MAP_`에 JT 소스→전용양식 열 매핑 정의
  - 보내는분(Q→D), 보내는전화(R→E), 보내는주소(S→H)
  - 수취인명(M→I), 전화번호(I→J), 주소(J→M), 품목명(E→N), 수량(G→O), 배송메시지(K→P)

---

## 2. 자동 트리거 스케줄 정비
**파일:** `_partnerWebApp.gs`

- **02시 `runDailyEcountBatch` 완전 제거** (기존에 주석 처리만 됐고 GAS 트리거는 살아있던 상태)
- **6시/12시 `runDailyEcountBatch`는 유지** (이카운트 전체동기화)
- 트리거 중복(2개씩) 문제 → 메뉴에서 "통합 자동 트리거 제거 후 재설치"로 해결

### 최종 스케줄
| 시각 | 내용 |
|------|------|
| 06:00 | 이카운트 전체동기화 1 |
| 07:00 | 대리판매 마감 |
| 07:20 | 대리공급 마감 |
| 08:00 | 발주 수집 + 판매현황 갱신 (1회전) |
| 09:20 | 대리공급 Push + 우편번호 (1회전) |
| 12:00 | 이카운트 전체동기화 2 |
| 12:30 | 통합 DB 동기화 [Supabase] |
| 12:40 | 통합허브 상태/재고 업데이트 |
| 14:05 | 발주 수집 + 판매현황 갱신 (2회전) |
| 14:20 | 대리공급 Push + 우편번호 (2회전) |
| 15:35 | 냅킨코리아 Gmail 송장수집 |
| 15:40 | 송장 수집 |
| 16:20 | 송장 배포 |
| 17:00 | 발주허브-DB + 협력업체-DB 동기화 [Supabase] |
| 23:30 | 통합 일일마감 |

---

## 3. Google Chat 알림 누락 보완
**파일:** `_partnerOrders.gs`, `_partnerExclusivePush.gs`, `_partnerChatNotify.gs`

- **`partnerCollectOrdersSilent_` (발주 수집 트리거)에 Chat 알림 추가**
  - 성공: "📦 발주 수집 완료" + 판매현황 갱신 여부 + 소요시간
  - 실패: "❌ 발주 수집 에러" + 오류 내용
- **`partnerPushOrdersToExclusiveFormsSilent_` (대리공급 Push 트리거)에 Chat 알림 추가**
  - 성공: "📤 대리공급 Push 완료" + 도서산간·우편번호 처리 + 소요시간
  - 실패: "❌ 대리공급 Push 에러" + 오류 내용
- **`testChatWebhook()` 테스트 함수 추가** — GAS 편집기에서 직접 실행해 웹훅 연결 상태 확인 가능

### 알림 적용 현황 (전체)
| 트리거 | Chat 알림 |
|--------|-----------|
| 이카운트 전체동기화 | ✅ (기존) |
| 통합허브 업데이트 | ✅ (기존) |
| 대리판매/대리공급 마감 | ✅ (기존) |
| 냅킨코리아 Gmail 송장수집 | ✅ (기존) |
| 송장 수집/배포 | ✅ (기존) |
| DB 동기화 [Supabase] | ✅ (기존) |
| 통합 일일마감 | ✅ (기존) |
| **발주 수집** | ✅ **(신규 추가)** |
| **대리공급 Push** | ✅ **(신규 추가)** |
