# 변경이력 — 2026-07-17 (시스템 최적화·오류수정 Phase 1~2 + B열 수집일 전환)

기획서: `20260717_시스템최적화_오류수정_기획서.md` — Phase 1(사고 예방)·Phase 2(속도 개선) 실행분.
clasp push 완료(2회): Phase 1 반영분 + Phase 2 반영분.

---

## A. 발주탭 B열 주문일자 — "입력 시 생성" → "발주수집 시 수집일" 전환

**파일:** `_partnerLibrary.gs`, `_partnerOrders.gs`

- `p2u_partnerOnEdit`: B열 날짜 스탬프/삭제 로직 제거 → **완전 no-op** (라이브러리라 push만으로 전 업체 즉시 반영, 시트 재설치 불필요)
- `partnerCollectOrders` 수집 루프: **수집에 성공한 행만** B열을 수집일(yyyyMMdd)로 **덮어쓰기** (허브 주문일자와 항상 일치)
  - 중복·입력미완·차단(단종/코드오류) 스킵 행은 B열을 건드리지 않음 → 기존 수집일 보존
  - 기존 배치 write-back(날짜 열 setValues 1회) 재사용 — `noWriteBack` 경로에서도 동작 확인
- 사전 채움 폐기: `_po_refreshAutofillBeforeCollect_`의 B열 채움 제거(A/D/L만 유지), 수집 루프의 `_pt_backfillMissingOrderDates` 호출 제거
- 마감 판정 영향 검증: 미수집 행(B열 빈 값)은 `_pms_processOneFile_`에서 `dateStr` 없음 → 잔류 처리로 안전. 허브 날짜맵 우선 로직 그대로.
- 기획서 M11(다중 행 붙여넣기 스탬프 누락)은 onEdit 스탬프 자체가 폐기되어 **소멸**

### 운영 변화
- 업체가 발주탭에 코드를 입력해도 B열은 **비어 있음** → 발주 수집(08:00/14:05 또는 수동) 시 수집일이 기록됨
- 업체가 직접 날짜를 입력해도 수집 시 수집일로 덮어씀

---

## B. Phase 1 — 사고 예방 (기획서 2.1)

| # | 파일 | 내용 |
|---|---|---|
| H1 | `_debug_triggers.gs` | `deleteOrphanTriggers` 하드코딩 화이트리스트(4개) 폐기 → `_ALL_SCHEDULED_TRIGGERS_` 배열에서 자동 생성 + 프로젝트에 정의된 함수(`globalThis[fn]`)는 절대 삭제 안 함. **운영 트리거 15개 삭제 사고 차단** |
| H3 | `_partnerExclusivePush.gs` | `partnerPushOrdersToExclusiveForms`에 ScriptLock 래퍼(`_pep_pushCore_` 분리). 트리거(09:20/14:20)+수동 동시 실행 시 중복 Push 차단 |
| H4 | `_partnerExclusiveArchive.gs` | 마감 시작 시 "허브 송장 있는 행 전부 삭제" 폐기 → 마감에서 **실제 이동된 전용양식 AX열 UID와 매칭된 행만** 완료 시점에 삭제(`_pea_clearHubRowsByUids_`). 대리판매 미마감 건 오삭제 차단. UID는 재개(resume) 상태에 누적 저장 |
| H5 | `_partnerGmailInvoice.gs` | 15분 단독 트리거 설치 폐기 — `partnerSetupGmailInvoiceTrigger_NK`는 안내만 표시(통합 스케줄 15:35 일 1회로 위임). 트리거 20개 한도 압박 해소 |
| H8 | `_partnerLibrary.gs` | `_p2uLib_findViewer_` 폴백 `allTabs[0]` → `null` 반환 (`_pt_findViewerSheet`와 동일 정책). onOpen #REF 복구가 엉뚱한 탭을 clear하는 사고 차단 (호출부 3곳 모두 null 가드 확인) |

## C. Phase 2 — 속도 개선 (기획서 2.2)

| # | 파일 | 내용 |
|---|---|---|
| H9 | `_partnerDeploy.gs` | `partnerRenameViewerTabToDankaJohoe`의 구버전 무한열 수식(`A1:A`/`L1:L`/`D1:D` 전체열) 재주입 폐기 → SSOT `_pt_injectOrderSpillFormulas`(스필 500행/조회 3000행 bounded)로 통일 |
| M1 | `_partnerOrders.gs` | 행 단위 setValue 루프 4곳 배치화: ①허브 단가 보정(L열 setValues 1회) ②판매현황 P열 완료 기록(setValues 1회) ③폐기송장 발주탭 반영(열 단위 setValues + RangeList 색상) ④사방넷 E열 backfill(setValues 1회) |
| M3 | `_partnerCancelReturn.gs` | ①`_cr_updateOrderTab_` 시그니처 변경 — UID 1건당 전체 탭 스캔 → **파일당 UID 맵 1회 배치**(상태열 setValues) ②`partnerPushCancelStatus` 행별 setValue+탭별 flush → 상태열 setValues 1회 |
| M4 | `_partnerExclusivePush.gs`, `_partnerWebApp.gs` | Kakao 우편번호 **주소→우편번호 영구 캐시**(`_pep_getZipCodeCached_`, ScriptProperties, 최대 1,500건). 성공 결과만 저장, 실행 종료 시 1회 save. 호출 3경로(수동 채우기·트리거·Push 내 JM) 모두 적용 → 반복 주소는 API 호출·120ms sleep 생략 |
| M5 | `_partnerOrders.gs` | `_po_onEditHubShipApproval_` 이중 루프(업체명 매칭 → 전체 재탐색) 단일화 — 매칭 파일 우선 정렬 후 첫 동기화에서 종료, 불필요한 flush 제거. 정상 케이스 파일 1개만 오픈 |
| M6 | `_partnerMaintenance.gs`, `_partnerMenu.gs` | **CF 중복 정리 배치** 신설: `partnerDedupConditionalFormatsAll` — 허브+전체 업체 파일의 조건부서식을 규칙 키(범위+조건+서식) 기준 dedupe, 첫 규칙만 유지(교체 set). 메뉴 `📊 상태 확인/진단 → 🎨 조건부서식 중복 정리`. 시간예산 5분, 초과 시 재실행으로 이어짐 |

---

## D. 한국 공휴일 자동 배치 차단 (추가 요청, 2026-07-17 오전)

| 파일 | 내용 |
|---|---|
| `_partnerHelpers.gs` | `_pt_isWeekendBlackout_` 확장 — 기존 주말 규칙(토 08:30↑·일 전체·월 05:30↓)에 **한국 공휴일 전체 차단 + 공휴일 다음날 05:30 이전 차단** 추가. 공휴일표 `_PT_KR_HOLIDAYS_` (2026~2027, 대체공휴일 포함) 하드코딩. 임시공휴일은 Script Properties `_PT_EXTRA_HOLIDAYS_`에 `"20261231,20270102"` 형식으로 등록하면 코드 수정 없이 반영 |
| `_partnerHelpers.gs` | 진단 함수 `partnerShowBlackoutStatus` 신설 — 지금/오늘/내일 차단 여부 + 다가오는 공휴일 5개 + 임시공휴일 등록 안내 표시 |
| `_partnerMenu.gs` | `⚙️ 자동화 설정 → 🗓️ 주말·공휴일 차단 상태 확인` 메뉴 추가 |

- 모든 자동 배치(스케줄 15개: 이카운트 동기화·마감·수집·Push·송장·Supabase·일일마감·Gmail)가 `_pt_isWeekendBlackout_`를 공유하므로 **공휴일 차단이 전 트리거에 일괄 적용**됨. 트리거 자체는 매일 실행되지만 함수 진입 즉시 스킵(로그 `[BLACKOUT] 한국 공휴일(…)`)
- **유지보수**: 2027년 말에 2028년 휴일표를 `_PT_KR_HOLIDAYS_`에 추가할 것
- **2026-07-17 오후 보완**: 제헌절이 2026년부터 공휴일로 재지정(공휴일법 개정, 2/3 국무회의 의결)된 것을 반영 — `20260717 제헌절`, `20270717 제헌절` + `20270719 대체공휴일` 추가
- **2026-07-18 보완**: 토요일 차단 범위 변경 — 기존 "토 08:30 이후"에서 **토요일 전체 차단**으로 확대 (운영자 결정). 이제 토요일 새벽~오전 배치(이카운트 06:00, 마감 07:00/07:20, 수집 08:00)도 실행되지 않음

## E. 발주탭 D1/L1 스필 #REF! 자가치유 (2026-07-20)

| 파일 | 내용 |
|---|---|
| `_partnerOrders.gs` | `partnerCollectOrders` 탭 처리 진입부에 D1/L1 헤더 `#REF!` 감지 시 `_pt_injectOrderSpillFormulas` 재주입 + 데이터 재로드 추가. D/L열에 값이 직접 쓰이면(마감 미완료행 값 복원·구버전 onEdit 등) 스필이 막혀 #REF!가 발생하는데, 수집 시 자동 복구됨. D/L은 파생열이라 값 손실 없음. 정상 파일은 이미 읽은 헤더로 검사하므로 추가 비용 0 |
| `_partnerOrders.gs` | `_po_onEditHubShipApproval_` 업체탭 N열 쓰기 전 N1 수식 가드 추가 — 수식 모드 파일에서 출고승인 동기화가 N열 스필을 #REF!로 깨는 것 방지 (수집 역기록 경로와 동일 정책) |
| `_partnerCancelReturn.gs` | `_cr_updateOrderTab_` 동일 가드 추가 — 취소/반품 상태 기록이 수식 모드 N열을 깨지 않도록 스킵 |

- **#REF! 재발 근본 원인**: 구버전(라이브러리 전환 전) 주입 onEdit이 C열 편집 시 D열(4)·L열(12)에 값을 직접 씀 → 스필 차단. 해당 업체 파일에 **스크립트 재설치**(복구 메뉴)를 실행해 최신 1줄 래퍼로 교체해야 근본 해결 |

## F. 발주탭 D/L열 값 모드 전환 — 수식 모드 폐기 (2026-07-20, 운영자 결정)

스크립트 재설치 후에도 #REF! 재발 반복 → "입력차단·자동복구" 순환 자체를 제거하기로 결정.
D/L열 스필 수식(7/16 수식 모드)을 폐기하고 **값 모드**(6/17 설계)로 회귀.

| 파일 | 내용 |
|---|---|
| `_partnerHelpers.gs` | `_pt_injectOrderSpillFormulas` D/L 블록: 수식 주입 → `_pt_freezeSpillColumnToValues_`(표시값 고정 + 수식 제거 + 유효성검사 정리)로 교체. A(거래처명)/N(상태) 스필은 유지 |
| `_partnerHelpers.gs` | `_pt_healOrderSpillFormulas` D/L 블록: 수식 재주입 → 값 고정으로 교체 (복구 메뉴도 값 모드로 정렬) |
| `_partnerHelpers.gs` | `_pt_freezeSpillColumnToValues_` getDisplayValues → getValues (단가가 "63,300" 문자열로 고정되는 것 방지) |
| `_partnerOrders.gs` | 수집 진입부: D1/L1에 수식·#REF! 잔재 감지 시 값 고정 + 즉시 자동채움 + 데이터 재로드 (파일별 1회 마이그레이션, 이후 비용 0) |
| `_partnerOrders.gs` | `_po_refreshAutofillBeforeCollect_` 가드 개선: "A1 또는 D1에 수식 있으면 전체 스킵" → **열별 수식 검사** (A/D/L 각각 수식 없는 열만 값 채움) |

- **운영 변화**: 업체가 C열 코드 입력 시 품목명·단가가 실시간 표시되지 않음 → **발주 수집 시 자동 채움** (6/17 방식). 대신 D/L #REF! 및 입력차단·자동복구 순환이 원천 제거됨
- 모든 업체 파일은 다음 발주 수집(또는 마감·복구 실행) 때 자동으로 값 모드로 이관됨

## G. 값 모드 철회 → 수식 모드 복원 + 파괴 원인 제거 (2026-07-20 최종, 운영자 결정)

수식 모드의 존재 이유(다품목 붙여넣기 시 품목명·단가 **즉시** 표시 — onEdit 방식은 5~30분 지연)가 확인되어
F 섹션의 값 모드 전환을 철회. 대신 수식을 깨뜨리던 쓰기 경로를 차단.

| 파일 | 내용 |
|---|---|
| `_partnerHelpers.gs` | `_pt_injectOrderSpillFormulas` D/L 블록 수식 주입으로 원상복구. `_pt_healOrderSpillFormulas` D/L 블록도 수식 재주입으로 원상복구 |
| `_partnerMonthlySettle.gs` | **파괴 원인 수정**: 마감 후 미완료행 값 복원 시 헤더에 수식이 있는 열(D/L/N)은 값 복원에서 제외 — 마감 때마다 스필을 깨고 inject로 다시 살리던 순환 제거 |
| `_partnerOrders.gs` | 수집 중 자동복구(수식 재주입/값 이관) 블록 완전 삭제 — 수집은 수식을 건드리지 않음. #REF! 발생 시 복구 메뉴에서 수동 처리 |
| (유지) | `_po_refreshAutofillBeforeCollect_` 열별 수식 가드 유지 — 수식 있는 열엔 값 안 씀. N열 쓰기 가드(출고승인·취소/반품) 유지 |

- **입력차단**: 별도 삭제 대상 없음 — onEdit는 7/17부터 no-op이며, =FALSE 유효성검사도 inject가 정리함. 남은 것은 1행 헤더의 "경고만" 보호(수식 실수 삭제 방지, 입력을 막지 않음)뿐
- **이관 안내**: 오늘 오전 값 모드로 바뀐 파일은 복구 메뉴 "발주탭 복구" 실행(또는 다음 마감) 시 수식 모드로 복원됨

## H. 전용양식 마감 후 재Push 이중 발주 방지 (2026-07-20)

**증상**: 전용양식 마감 실행 → 행이 마감탭으로 정상 이동했는데, 이후 같은 주문이 전용양식에
"송장번호 없이" 다시 나타남 → 공급처가 신규 발주로 오인할 수 있는 이중 발주 위험.

**원인**: 마감이 아니라 **재Push**. Push의 중복 판별(`_pep_loadExclusiveDedupCounts_`)이
전용양식 탭의 AX열(고유ID)만 읽음. 마감으로 행이 빠져나가면 dedup 기록도 사라지고,
소스탭(`대리공급업체 발주`)에 해당 주문 행이 아직 남아 있으면 다음 자동 Push(09:20/14:30)가
"새 발주"로 인식해 전용양식에 다시 채움. 새 Push 행이므로 송장번호는 비어 있음.

| 파일 | 내용 |
|---|---|
| `_partnerExclusivePush.gs` | `_pep_loadArchiveDedupCounts_(ss)` 신규 — 마감탭 "(YYYY년 M월) 전용발주 마감" **당월+전월**의 고유ID 열(헤더 "고유ID" 탐지, 폴백 51열)을 읽어 UID별 건수 집계 |
| `_partnerExclusivePush.gs` | `_pep_initVendorCache_`에서 전용양식 AX dedup에 마감탭 건수를 **합산** — 마감된 주문은 소스탭에 남아 있어도 재Push 스킵. 메인 Push·임시기록 Push 모두 같은 캐시를 쓰므로 둘 다 적용 |

- **동작 변화**: 마감탭(당월·전월)에 기록된 고유ID는 재Push되지 않음. 15일 초과로 송장 없이 강제
  마감된 건도 재Push가 막히므로, 정말 재발주가 필요하면 소스탭에 새 행(새 주문번호)으로 넣어야 함
- **비용**: Push 시 업체 파일당 마감탭 1~2개 열 읽기 추가 (미미)

## I. 구버전 onEdit 중복 스크립트 정리 — 입력차단·자동복구 잔존 원인 (2026-07-20)

**증상**: 스크립트 재설치를 해도 업체 파일에서 "입력 차단"/"자동 복구" 토스트가 계속 뜨고
D/L열 스필이 #REF!로 깨짐.

**원인**: 같은 업체 시트에 바인딩된 Apps Script 프로젝트가 **2개 이상** 존재
(과거 캐시 초기화 → 재생성 반복으로 중복 생성). 재설치(`createViewerNoticeScript_`)는
캐시된 프로젝트 1개만 새 1줄 래퍼로 덮어쓰고, **캐시가 유효하면 중복 정리를 건너뛰었음**.
구글 시트는 편집 시 바인딩된 모든 프로젝트의 onEdit을 전부 실행하므로,
살아남은 구버전 프로젝트(200줄 onEdit: C열 입력 시 D/L 값 쓰기 + 수동입력 차단 + 값 되돌리기)가
계속 동작 → D/L 값 쓰기로 스필 #REF! 유발.

| 파일 | 내용 |
|---|---|
| `priceManager.gs` | `createViewerNoticeScript_`: 캐시가 유효해도 **중복 정리를 항상 실행** (캐시 프로젝트 보존, 나머지 휴지통) |
| `priceManager.gs` | `cleanDuplicateViewerScripts_(id, preferId)`: 보존 대상을 캐시된 프로젝트로 지정 가능. 휴지통 이동 실패 시 no-op 코드 덮어쓰기 폴백(`_neutralizeBoundScriptProject_`) |
| `priceManager.gs` | `_p2uScanAllBoundScripts_`: Drive 검색을 실행당 1회로 캐시 — 전체 재설치 루프에서 업체 수만큼 반복 검색하던 비용 제거 |

- **운영 조치**: 문제 업체(또는 전체)에 대해 복구시스템 → 🔧 스크립트 재설치를 1회 재실행 → 중복 프로젝트가 휴지통으로 이동됨. 이후 발주탭 복구로 D/L 수식 재주입
- 휴지통으로 이동된 구버전 프로젝트는 30일간 복원 가능

## J. 발주탭 D/L 무음 복원 + 헤더 #REF! 마스크 + 자동입력 100행 (2026-07-20, 운영자 확정)

**정책 확정 (13:10)**: 행별 수식 전환·1행 숨김안 모두 기각. **현행 스필 구조 유지** +
① 헤더에 #REF! 비노출 ② 자동입력 100행 축소(조회는 3000행 유지) ③ 복붙 값 무음 자동복원.

**검증 결과**: IFERROR는 "Array result was not expanded" #REF!(스필 막힘)를 잡지 못함(구글 명세).
→ 헤더 고정은 IFERROR 대신 **조건부서식 오류 마스크**로 구현.

| 파일 | 내용 |
|---|---|
| `_partnerHelpers.gs` | `_PT_ORDER_AUTOFILL_ROWS_=100` 신설 — D/L 수식 기본 스필 범위 `C2:C100`. 업체별 수동 조정 행수(2~499)는 inject/heal이 존중, 구 기본 500은 100으로 이관 |
| `_partnerHelpers.gs` | heal: D1/L1 수식이 살아있어도 표시가 #REF!(스필 막힘)이면 복구 대상으로 감지 |
| `_partnerHelpers.gs` | `_pt_repairOrderTabCollectMode_`: 조건부서식 재적용 포함 — 스크립트 재설치·발주탭 복구 모든 경로에서 헤더 마스크 자동 설치 |
| `_partnerDeploy.gs` | `_pt_applyOrderTabDesign`: 헤더행(A1:P1) `ISERROR` 조건부서식 추가 — 오류 시 글자색=배경색(#1f4e78)으로 #REF! 텍스트 비노출 |
| `_partnerLibrary.gs` | `p2u_partnerOnEdit`: **D/L 무음 복원(0계층)** — 편집 범위가 D/L과 겹치고 값이 들어오면 해당 열 값만 조용히 걷어냄(토스트·알림 없음) → 스필 자동 재계산. D1/L1이 수식 모드일 때만 동작 |
| `_partnerLibrary.gs` | `p2u_partnerOnOpen`: 시트 열 때 D/L 스필 막힘 감지 → 막은 값 정리(1계층 백필) |
| `_partnerOrders.gs` | `_po_refreshAutofillBeforeCollect_`: 수집 직전 D/L 스필 막힘 무음 해제(2계층 백필) — 수식 재주입이 아니라 값 걷어내기만 (과거 "자동복구 순환"과 무관) |
| `partnerOnEditSource.gs` | 주입 래퍼 주석 갱신 (호출부 동일 — 라이브러리라 기설치 파일도 push 즉시 새 동작) |

- **동작**: 코드 입력 → 품목명·단가 즉시 표시(수식). 다품목 엑셀 복붙(품목명 포함) → 순간 D/L이 붙여넣은 값으로 보이다가 수초 내 조용히 수식 표시로 원복. 그 사이 헤더는 #REF! 대신 마스크(빈 것처럼 보임)
- **운영 조치**: ① 전체 업체 「발주탭 복구」 1회 실행(새 100행 수식 + 헤더 마스크 이관) ② 업체별 자동입력 범위 조정은 D1/L1 수식의 `C2:C100`을 직접 수정 — 복구가 되돌리지 않음(단 500은 구 기본으로 간주되어 100으로 변경됨)

## K. 발주탭 P열 상태값 잔재 차단 + O열 도서산간 보라색 미적용 수정 (2026-07-21)

**증상 1 — P열에도 상태값**: 상태는 N열("상태(자동)" 스필)인데 P열에 접수완료 등 상태값이 쌓임.
**원인**: 구버전 파일의 P1에 "상태" 헤더 잔재가 남아 있으면 열 매핑(`_pt_buildOrderTabColumnMap`)이
"상태" 헤더를 **마지막 매칭 우선**으로 잡아 `status`가 N이 아닌 P로 매핑 → 수집 역기록("접수완료")·
출고승인 동기화가 P열에 기록됨.

**증상 2 — O열 보라색 안 됨**: 도서산간배송비(O열) 입력 시 보라 배경이 적용 안 됨.
**원인**: 조건부서식은 먼저 매칭된 규칙이 우선인데, 행 전체(A2:O120)를 칠하는
발송완료(초록)/접수완료(파랑) 규칙이 보라 규칙보다 **앞에** 있어 O열까지 덮음.
도서산간 주문은 대부분 접수완료 상태라 보라가 항상 밀렸음.

| 파일 | 내용 |
|---|---|
| `_partnerHelpers.gs` | `_pt_buildOrderTabColumnMap`: "상태" 헤더 **첫 매칭 우선** — N열이 있으면 P열 잔재를 무시 |
| `_partnerHelpers.gs` | inject/heal의 P열 정리 강화: P1 헤더에 "상태" 포함이면 정리 + 헤더가 없어도 P열에 상태값(접수완료·발송완료 등) 잔재가 있으면 P1:P 전체 정리 |
| `_partnerDeploy.gs` | `_pt_applyOrderTabDesign`: 도서산간 보라 규칙을 **규칙 목록 맨 앞**으로 이동 + 범위를 O2:O120(열 전용)으로 축소 — 상태 행배경과 무관하게 O셀은 항상 보라 |

- **운영 조치**: 전체 업체 「발주탭 복구」(또는 조건부서식 재적용 경로) 1회 실행 → P열 잔재 정리 + 새 서식 순서 적용
- clasp push 완료 (2026-07-21 08:40)

## L. 송장배포가 N열 스필 파괴 → P열 상태 재생성하던 근본 원인 수정 (2026-07-22)

**증상**: 발주 수집까지는 N열 "상태(자동)" 헤더 정상 + P열 빈 헤더인데,
**송장 배포를 실행하면** N열 헤더가 사라지고 P열에 상태값이 생김.

**원인 (2단계 연쇄)**:
1. `partnerPushInvoices`(송장 배포)가 "발송완료"/"출고가능" 상태를 **N열에 값으로 직접 기록**
   — N1 스필 수식 모드 가드가 이 함수에만 없었음 → 스필이 값에 막혀 N1=#REF!
   (7/20 추가된 헤더 CF 마스크가 #REF! 글자를 숨겨 "헤더가 사라진 것"처럼 보임)
2. 다음 배포 실행 시 N1이 #REF!라 헤더 매핑이 상태 열을 못 찾음(`cMap.status=-1`)
   → 기존 "상태 열 자동 보수"가 **마지막 열 뒤(P열)에 "상태(자동)" 헤더를 새로 생성**하고 상태값 기록

| 파일 | 내용 |
|---|---|
| `_partnerOrders.gs` | `partnerPushInvoices`: N1 수식 모드면 상태 값 쓰기 금지 — K열 송장이 채워지면 스필이 "발송완료"를 자동 계산하므로 쓰기 자체가 불필요 (송장/적요 배포는 그대로) |
| `_partnerOrders.gs` | 상태 열 자동 보수 개선: N1에 수식이 살아있으면(#REF! 포함) P열에 새 열을 만들지 않고 상태 열=N으로 인식 + 막은 값 잔재를 무음으로 걷어내 스필 부활. 진짜 상태 열이 없는 구버전만 N열(14열) 고정 보수 |
| `_partnerHelpers.gs` | heal의 N열 판정에 스필 막힘(#REF! 표시) 추가 — 발주탭 복구 시 N 스필도 복구 |

- **운영 조치**: 증상이 이미 발생한 업체 파일은 「발주탭 복구」 1회 실행 (P열 잔재 정리 + N 스필 복구는 K절 정리 로직이 함께 처리). 이후 송장 배포는 N열을 건드리지 않음
- clasp push 완료 (2026-07-22 17:20)

## M. 병행 택배사 송장 탭 추가 수집 (2026-07-22, 운영자 요청)

**요청**: 택배사 전환 예정으로, 송장취합 시트의 신규 탭(GID 1575029201)에서도 송장을
병행 수집. G열=운송장번호, J열=주문번호(고유ID).

| 파일 | 내용 |
|---|---|
| `_partnerHelpers.gs` | `_PT_SECONDARY_INVOICE_GID = 1575029201` 상수 추가 (같은 송장취합 시트 내 탭) |
| `_partnerOrders.gs` | `partnerFetchInvoices`: 로젠(최우선) 수집 직후 신규 탭 수집 추가 — 고정 열 `{invoice: G(6), uid: J(9)}`, 이름/전화/품목 미사용 |
| `_partnerHelpers.gs` | `_pt_ingestInvoiceSheetTabIntoMap` 버그 수정: 고정 열 모드(fixedColIdx)에서 phone=-1이면 2행을 헤더로 재스캔해 **데이터 첫 행이 수집에서 누락**되던 문제 — 고정 열 모드에서는 재스캔 금지 |

- **동작**: 두 택배사 송장이 동일 invoiceMap에 합산 — 고유ID(주문번호)로 허브와 매칭.
  같은 주문에 같은 송장번호가 양쪽에 있으면 자동 dedup(`parseInvoiceLinesFromMatchedRows_`),
  허브에 이미 기록된 송장은 `globalUsedInvoices`로 재배정 차단 (기존 로직 그대로 적용)
- 로그: 송장 수집 결과의 스캔 로그에 `[병행택배사(신규)]` 라벨로 수집 건수 표시
- clasp push 완료 (2026-07-22 18:50)

## Q2. 그린우드형 행색 들쑥날쑥 추가 복구 (2026-07-28)

**증상(스크린샷)**: 같은 `접수완료`인데 윗행은 흰색·아랫행만 하늘색, O열에 배송비 있는 행은 **행 전체**가 보라.

**추가 원인**
1. 구 CF가 O조건을 행전체(`A2:O…`)에 걸어 둔 채 남아 있음
2. 마감 `clearFormat` 이후 일부 행에만 수동/잔류 배경이 남아 CF와 섞임
3. 메뉴에 「조건부서식 재적용」이 **빠져 있어** 배포 후에도 업체 파일에 새 규칙이 안 깔림

| 파일 | 내용 |
|---|---|
| `_partnerDeploy.gs` | 적용 전 `A2:O` 수동 배경 `setBackground(null)` 제거, 상태조건에 완전일치(`=$N2="접수완료"`) 추가, O열은 O열만 |
| `_partnerMenu.gs` | `📊 상태 확인/진단 → 🎨 발주탭 상태색 재적용 (전체 업체)` 메뉴 추가 |
| `_partnerMaintenance.gs` | 재적용 안내 문구 보강 |

- **지금 할 일**: 허브에서 위 메뉴 1회 실행 → 그린우드 포함 전체 반영

## Q. 발주탭 상태(접수완료/발송완료) 행색 CF 복구 (2026-07-28, 운영자 요청)

**문제**: 상태값에 따라 행 배경색이 바뀌는 기능이 파일/행마다 들쑥날쑥.

**원인**
1. CF 범위가 `A2:O120`으로 고정 → 상태 스필은 500행인데 121행 이후는 색 미적용
2. N열 스필이 #REF!로 막히면 상태 텍스트가 비어 CF 조건 실패 (일부 파일·행만 됨)
3. K열(송장) 규칙이 상태 규칙보다 앞에 있어 우선순위가 꼬임 / P열 상태 잔재만 있는 파일은 N만 보던 CF가 실패

| 파일 | 내용 |
|---|---|
| `_partnerDeploy.gs` | `_pt_applyOrderTabDesign` 재작성: 범위=스필상한(최대1000), N 스필 막힘 무음 해제, 상태 규칙 우선+N/P 폴백, set으로 구규칙 교체 |
| `_partnerHelpers.gs` | inject/heal 끝에 CF 재적용 호출 |
| `_partnerMonthlySettle.gs` | 마감 후 inject 실패 시에도 CF만이라도 재적용 |
| `_partnerMaintenance.gs` | 재적용 메뉴 안내 문구 수정 |

- **운영**: 메뉴 `📋 조건부서식 재적용` 1회 실행하면 전체 업체 파일에 즉시 반영.
  개별 파일은 「발주탭 복구」/스크립트 재설치로도 동일 적용.

## P. 마감 후 빈 칸 셀서식(배경·테두리) 잔재 제거 (2026-07-24, 운영자 요청)

**문제**: 마감 시 `clearContent`만 해서 값은 지워지는데 배경색·테두리·폰트서식이 남아
초기화된 영역이 더 지저분해 보임 (발주탭 / 전용양식 / 발주허브 / 임시기록).

| 파일 | 내용 |
|---|---|
| `_partnerHelpers.gs` | `_pt_clearContentAndFormat_` 헬퍼 추가 — `clearContent` + `clearFormat` |
| `_partnerMonthlySettle.gs` | 발주탭 마감 clear, 발주허브 정리 clear에 적용 |
| `_partnerExclusiveArchive.gs` | 전용양식 마감 clear(배치/단일), 허브 UID 정리 clear에 적용 |
| `_partnerOrders.gs` | 임시기록(`대리공급_임시기록`) 송장/오래된 행 정리 clear에 적용 |
| `_partnerDeploy.gs` | 검색입력 탭 초기화에도 적용 |
| `_partnerOrderCleanup.gs` | 발주탭 정리 메뉴 clear에도 적용 |

- 조건부서식 규칙은 유지 (데이터 입력 시 상태색 등은 다시 적용)
- 헤더(1행)는 그대로 둠

## O. 미매칭 유사 이름 후보 + 수동 체크 매칭 (2026-07-24, 운영자 요청)

**요청**: 자동 매칭 실패 시 비슷한 예상 이름을 보여주고, 체크하면 매칭되게.

| 파일 | 내용 |
|---|---|
| `InvoiceMatch_협력업체용.gs` | `_imBuildNameSuggestions_` 추가 — 남은 수취인 큐에서 토큰/부분일치/유사도 상위 3후보. `parseAndMatchInvoiceTextLocal` 결과에 `suggestions`·`remainingQueue`·`lastMatchedRows` 포함. 협력업체 로컬 사이드바 UI에 후보 드롭다운+체크 매칭 |
| `_partnerWebApp.gs` | `_imParseAndMatch_` 동일하게 미매칭 후보·남은 큐 반환 |
| `invoiceMatchSidebar.html` | 미매칭 행: ⭐유사후보 / 전체 남은 수취인 / 이미 매칭된 이름에 추가 선택 → 체크 시 `_matches`에 수동 매칭 추가(행 큐에서 배정) 후 반영 가능 |

- 사용법: AI 분석 후 ❌ 건에서 후보 선택 →「매칭」체크 →「전용양식에 반영」
- 수동 매칭 건은 결과표에 `✓수동`으로 표시

## N. 송장 이미지 매칭 인식률 개선 + Gemini 3.6 Flash 업그레이드 (2026-07-24, 운영자 요청)

**요청**: 송장번호 매칭 시 이미지가 매칭에 실패하는 경우가 많음 — 인식률 개선 + Gemini 3.6 Flash 전환.

**근본 원인(버그)**: 이미지 AI 분석에 실제 사용되는 `_parseInvoiceFileWithGemini_`에
마크다운 코드블록(```json) 제거 로직이 없었음. Gemini가 응답을 코드블록으로 감싸면
`JSON.parse` 예외 → catch → null 반환 → "이미지에서 송장 정보를 인식하지 못했습니다"로
떨어짐. (텍스트용 `_parseInvoicePairsWithGemini_`에는 제거 로직이 있었음)

| 파일 | 내용 |
|---|---|
| `_partnerExclusivePush.gs` | `_parseInvoiceFileWithGemini_` 재작성: ① 코드블록 제거 버그 수정 ② `responseMimeType: "application/json"`으로 JSON 강제 출력 ③ 프롬프트 상세화(송장번호 10~14자리·하이픈 제거, 수취인=상호·지점명 허용, 행 단위 읽기, 사업자번호/전화번호 제외) ④ 결과 정규화(송장번호 숫자만, '님'·수량 접미 제거, 자릿수 검증) ⑤ `gemini-3.6-flash` + 실패 시 3.5-flash 폴백 ⑥ maxOutputTokens 16384 |
| `_partnerExclusivePush.gs` | `ocrImageToText`(OCR 폴백)·`_parseInvoicePairsWithGemini_`·`_parseInvoiceImageWithGemini_` 모델 3.6-flash 전환. 텍스트 파서 폴백을 gemini-1.5-flash(지원 종료) → 3.5-flash로 교체. OCR maxOutputTokens 2048→8192 (3.x는 thinking 토큰이 출력 한도 소모) |
| `_partnerWebApp.gs` | `_imParseAndMatch_` 매칭 보강: 4.5단계 토큰 매칭("햇살블루 김금자"처럼 상호+이름이면 각 토큰을 시트 이름과 정확 대조, 뒤 토큰 우선) + 4.6단계 역방향(시트가 "상호 이름", 입력이 이름만) + 이름 정규화에 수량 접미("1박스" 등) 제거 |
| `InvoiceMatch_협력업체용.gs` | `parseAndMatchInvoiceTextLocal`에 동일한 4.5/4.6 토큰 매칭 + 수량 접미 제거. 임베디드 사이드바 OCR 문자열도 3.6-flash·8192 토큰으로 갱신 |
| `카카오챗봇_설치파일/3_협력업체_송장매칭_복사용.gs` | 3.6-flash·8192 토큰 (복사용 템플릿 — 기설치 업체는 재복사 필요) |
| `_partnerOrderAudit.gs` / `priceAnalysisChatbot.gs` / `CS_WebApp/Code.gs` | 모델 3.6-flash 전환 (버전 통일) |

- `gemini-3.6-flash`: 2026-07-21 GA 출시 확인 (멀티모달 입력 지원, 3.5-flash 대비 성능↑·비용↓)
- clasp push 완료 (2026-07-24 15:25 — 허브 프로젝트 56파일 + CS_WebApp 6파일)
- **검증**: 이미지 AI 탭에 기존 실패 이미지 재투입 → 파싱방식 `gemini-image` 표시 + 행 매칭 확인.
  실패 시 GAS 로그에서 `[송장매칭-이미지]` 라벨로 원인(HTTP 코드·응답 앞부분) 확인 가능

## 검증 체크리스트 (배포 후 확인)

1. **B열 전환 테스트**: 업체 1곳 발주탭 C열에 코드 입력 → B열 빈 상태 확인 → `📦 1️⃣ 대리판매 발주수집` 실행 → B열=오늘(수집일), 허브 주문일자와 일치 확인
2. `⚙️ 운영/진단 → 🧪 처리 후 오류 점검` / 발주탭 수식 진단 실행
3. GAS 편집기에서 `showAllScheduledTriggerStatus`로 트리거 15개 정상 확인 (H1 수정 후 `deleteOrphanTriggers` 실행해도 안전)
4. 내일 아침 자동 배치 Chat 알림 3종(마감 07:00/07:20 · 수집 08:00 · Push 09:20) 정상 확인
5. 대리공급 마감 후 허브에 **대리판매 건(송장 있음)이 남아있는지** 확인 (H4 — 기존엔 삭제됐음)
6. `🎨 조건부서식 중복 정리` 1회 실행 (M6 — 스크롤·편집 렌더 개선)
7. **재Push 방지(H)**: 전용양식 마감 실행 후 `3️⃣ 대리공급업체로 발주 Push` 수동 실행 → 마감된 주문이 전용양식에 다시 생기지 않는지 확인 (실행 로그에 "마감탭 포함 N종" 표시)
8. **무음 복원(J)**: 파일럿 업체 1곳 「발주탭 복구」 실행 → D1 수식이 `C2:C100`인지 확인 → C열 코드 입력 시 품목명 즉시 표시 → 품목명 열 포함 다품목 복붙 → 수초 내 D/L이 수식 표시로 원복되고 토스트가 뜨지 않는지 확인 → 원복 전 헤더에 #REF! 텍스트가 보이지 않는지 확인 → 발주 수집 정상 확인

## 주의 (동작 변화)

- **허브 정리 방식 변경(H4)**: 대리공급 마감이 더 이상 "송장 있는 허브 행 전체"를 지우지 않음. 대리판매 건의 허브 정리는 별도(월별DB 이동 메뉴) 경로로 관리
- **Gmail 트리거(H5)**: `partnerSetupGmailInvoiceTrigger_NK` 실행해도 15분 트리거가 설치되지 않음 (통합 트리거 메뉴 사용)
- **B열(A)**: 수집 전 행은 B열이 비어 있는 것이 정상
