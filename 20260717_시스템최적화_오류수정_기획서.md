# Pack2U 협력업체 시스템 — 전체 최적화·오류수정 기획서

- 작성일: 2026-07-17
- 범위: **협력업체 시스템 전체** (`_partner*.gs`, 헬퍼/메뉴/라이브러리, 연동·트리거)
- 제외: 독립배포 시스템(`orderSyncManager.gs`, `salesUploadFromIntegratedOrders.gs`, `vendorCustCodeManager.gs`), `backup_*` 폴더
- 방법: 전 파일 정적 감사(3개 영역 병렬) + 최근 운영 장애 이력(마감 0건·30분 지연, 코드오류, #REF!) 반영

---

## 1. 시스템 현황 요약

### 1.1 활성 코드 규모

| 파일 | 줄수 | 책임 |
|---|---|---|
| `_partnerExclusivePush.gs` | 6,757 | 대리공급 Push·전용양식·OCR·우편번호·일일마감 |
| `_partnerOrders.gs` | 5,006 | 발주 수집·송장 수집/배포·판매현황·폐기송장 |
| `priceManager.gs` | 5,058 | 허브 단가·배포 시트 생성 |
| `_partnerDeploy.gs` | 2,334 | 배포 시트 생성/복구·뷰어 수식 |
| `_partnerHelpers.gs` | 2,363 | 공통 헬퍼·발주탭 수식 inject/heal |
| `_partnerWebApp.gs` | 1,591 | 통합 트리거 허브·일괄마감·웹앱 |
| `InvoiceMatch_협력업체용.gs` | 1,505 | 송장매칭 사이드바(주입용) |
| `_partnerMonthlySettle.gs` | 1,060 | 대리판매 월별 마감 |
| `_partnerSupabase.gs` | 1,040 | Supabase DB 동기화 |
| 기타 25+개 | ~7,000 | 메뉴/취소반품/도서산간/Gmail/감사 등 |

### 1.2 최근 해결된 이슈 (2026-07-16~17)

| 이슈 | 원인 | 조치 |
|---|---|---|
| 마감 "이동 0건" | B열 주문일자가 `TODAY()` 수식 → 매일 오늘로 변경 → `날짜<오늘` 조건 전멸 | B열 고정값 전환 + 허브 날짜 복구 |
| 마감 30분+ | 파일마다 허브 재오픈·B열 전체 재쓰기·CF 누적·배치 대기 60초 | 허브 날짜맵 배치 1회·조건부 freeze·재개 5초 |
| 품목명 `🚨코드오류` | 단가조회 VLOOKUP 범위 `$G$2000` < 실제 제품 2,900행 | `_PT_VIEWER_LOOKUP_END_=3000` + 복구 시 자동 갱신 |
| 도서산간 미적용 | 허브 Q열(17) 고정 참조 ≠ 실제 P열(16) | 헤더명 동적 탐지 |

---

## 2. 잔여 오류·위험 (심각도별)

### 2.1 🔴 높음 — 기능 오동작/데이터 위험

| # | 위치 | 문제 | 수정 방향 |
|---|---|---|---|
| H1 | `_debug_triggers.gs` `deleteOrphanTriggers` (44–61) | 화이트리스트가 4개 함수뿐 → 실행 시 **운영 트리거 15개 중 대부분 삭제** | `_ALL_SCHEDULED_TRIGGERS_` 배열에서 화이트리스트 자동 생성 |
| H2 | `_partnerExclusiveArchive.gs` (247–285 vs 585–590 vs 858–859) | 수동마감(`_pea_core_`)만 "송장없음+15일 초과 강제이동", 일괄마감(`_pea_processOneFile_`)·미리보기는 무조건 잔류 → **경로별 마감 결과가 다름** | 판정 함수 1개로 통일, 3곳 모두 호출 |
| H3 | `_partnerExclusivePush.gs` `partnerPushOrdersToExclusiveForms` | **ScriptLock 없음** — 트리거(09:20/14:20)와 수동 동시 실행 시 중복 Push | 진입 시 tryLock + 실행중 플래그 |
| H4 | `_partnerExclusiveArchive.gs` `_pea_core_` (159–185) | 마감 시작 시 허브에서 **송장 있는 행 전부 삭제** — 대리판매 미마감 건 포함 위험 | 전용양식 UID와 매칭된 행만 삭제 |
| H5 | `_partnerGmailInvoice.gs` (831–834) | Gmail 트리거 **15분마다** 별도 설치 가능 ↔ 통합 스케줄(15:35 일1회)과 중복, 트리거 20개 한도 압박 | 단독 설치 메뉴 제거 또는 통합 배열로 위임 |
| H6 | `_partnerWebAppAPI.gs` (75, 177, 270) | 웹앱 `doGet`에서 `getActiveSpreadsheet()` — 웹앱 컨텍스트에서 null 가능 | `openById(_PT.INFO_SS_ID)` 고정 |
| H7 | `_partnerWebAppAPI.gs` (404–411) | run 키 소스 하드코딩 | `PropertiesService`로 이관 |
| H8 | `_partnerLibrary.gs` `_p2uLib_findViewer_` (98) | 뷰어 탐색 실패 시 `allTabs[0]` 반환 → onOpen #REF 복구가 **엉뚱한 탭을 clear** 가능 | `null` 반환으로 통일 (`_pt_findViewerSheet`와 동일) |
| H9 | `_partnerDeploy.gs` `partnerRenameViewerTabToDankaJohoe` (2155–2172) | 뷰어명 변경 시 **구버전 무한열 수식** 재주입 → 성능 회귀 | bounded 빌더(`_pt_buildOrder*ArrayFormula_`)로 교체 |
| H10 | `_partnerWebApp.gs` 일괄마감 경로 (570) | `_pms_processOneFile_`에 `hubDateByUid` 미전달 → B열 오염 시 판정 약화 | 허브 날짜맵 전달 통일 |

### 2.2 🟡 중간 — 성능·유지보수

| # | 위치 | 문제 |
|---|---|---|
| M1 | `_partnerOrders.gs` (1172–1176, 3260–3263, 3863–3880, 4491–4499) | 행 단위 `setValue` 루프 4곳 → 열 단위 `setValues` 배치화 |
| M2 | `_partnerSupabase.gs` (860–880, 1030–1039, 1223~) | 행마다 UrlFetch PATCH/POST → upsert 배치(500건) 전환 |
| M3 | `_partnerCancelReturn.gs` (201, 350–354) | UID마다 `setValue` + 탭마다 `flush` |
| M4 | `_partnerExclusivePush.gs` (5763–5787) + `_partnerWebApp.gs` (1392–1406) | Kakao 우편번호 API 행 루프(+sleep 120ms) — 대량 시 6분 한도. 주소→우편번호 캐시(Properties/시트) 필요 |
| M5 | `_partnerOrders.gs` `_po_onEditHubShipApproval_` (5334–5426) | onEdit마다 전 업체 파일 나열+오픈 — 지연 체감 직결 |
| M6 | `_partnerMonthlySettle.gs` `_pms_layoutArchiveTab_` (981–1019) | `getMaxRows()` 전행 서식 + CF 누적 (신규 탭만 적용하도록 이번에 부분 수정됨 — CF 중복 정리 배치 필요) |
| M7 | `_partnerExclusiveArchive.gs` `_pea_preview_` (839–862) | 확인창 전에 전 업체 열기 — UI 블로킹. 마감 대상 업체만 스캔 |
| M8 | `_partnerHelpers.gs` inject vs heal (468–558 vs 1087–1258) | 동일 책임 이중 구현 — 단일화 필요 (드리프트 발생 이력 있음) |
| M9 | `_partnerMaintenance.gs` `partnerMigrateToDataTab` (855–919) | UI는 "로컬 참조 전환" 약속, 실제는 Legacy IMPORTRANGE 적용 — 기능 불일치 |
| M10 | `_partnerMenu.gs` (96 vs 139) | "발주탭 수식 복구" 메뉴 2곳 중복(다른 함수) — 하나로 통합 |
| M11 | ~~`_partnerLibrary.gs` `p2u_partnerOnEdit`~~ | ~~다중 행 붙여넣기 시 첫 행만 B열 날짜 스탬프~~ → **소멸** (2026-07-17: onEdit 스탬프 폐기, B열은 발주 수집 시 수집일 기록) |
| M12 | 전역 | 빈 `catch {}` 수십 곳 — 최소 `Logger.log` 추가해 실패 가시화 |
| M13 | `_partnerOrderCleanup.gs` (65, 148–153) | 열 14개 고정 clear/rewrite → O열(도서산간) 이후 데이터 유실 위험 |
| M14 | 트리거 15:35 Gmail ↔ 15:40 송장수집 | ScriptLock 경합으로 한쪽 스킵 가능 — 간격 10분+ 권장 |

### 2.3 🟢 낮음 — 정리 대상 (데드 코드)

- `_partnerHelpers.gs`: `_pt_onEditSpillGuard_` 스텁 + 이를 설치하는 `partnerInstallSpillGuards`, `_pt_lightenOrderTabSpeed_`(미호출), 레거시 무한열 빌더(325–453)
- `_partnerWebApp.gs`: `_trigger_dailyArchiveAll_`(스케줄 미등록), `_trigger_NK_Gmail_`(중복 구현), `_trigger_collectPriceMap_`(no-op)
- `_partnerDeploy.gs`: `_pt_applyRow3FormulasLocal_`·`_pt_ensureDataTab`(롤백된 `_data` 경로)
- `_partnerExclusivePush.gs`: `srcUidWrites`(1257) 등
- `InvoiceMatch_협력업체용.gs`: HTML 이중 정의(37~ / 1278~)

---

## 3. 구글시트 속도 개선 방안

### 3.1 수식·계산 (업체 시트 체감 속도)

| 방안 | 내용 | 기대효과 |
|---|---|---|
| **전체열 참조 제거** | `C2:C`, `C:G` → 유한범위(스필 500행 / 조회 3000행). inject는 완료, **rename·레거시 빌더 경로 잔존(H9)** | 편집 시 재계산 3–5초 → 1초 미만 |
| **IMPORTRANGE 최소화** | 단가조회 Row3가 열마다 IMPORTRANGE×XLOOKUP(최대 16회) — 그룹 단가표 1회 IMPORTRANGE 후 로컬 참조로 전환 검토(과거 A:Z 롤백 이력 있으므로 **필요 열만** 가져오는 방식으로 재설계) | 파일 열기·재계산 대폭 단축 |
| **휘발성 함수 금지** | `TODAY()`/`NOW()` ARRAYFORMULA는 시트 전체 재계산 유발 + 데이터 오염(마감 0건 사건) — 값 스탬프 방식 유지 | 재계산 억제·데이터 정합 |
| **CF 규칙 정리** | 마감탭·허브에 누적된 조건부서식 일괄 제거 배치(같은 규칙 1개만 유지) | 스크롤·편집 렌더 개선 |
| **시트 다이어트** | 사용하지 않는 행/열 삭제(`getMaxRows` 5000+ → 실데이터+여유 500), 과거 마감탭 서식 범위 축소 | 파일 크기·로드 시간 감소 |

### 3.2 Apps Script (배치·트리거 속도)

| 방안 | 내용 |
|---|---|
| **읽기/쓰기 배치화** | 행 단위 `setValue`/`getValue` 루프 → `getValues`/`setValues` 1회 (M1~M3) |
| **openById 캐시** | 허브·업체 파일 핸들을 실행당 1회만 오픈(마감은 적용됨 — 취소반품·감사·Supabase 경로 확대) |
| **flush 최소화** | 검증 직전 1회만. 파일마다 flush 제거 |
| **실데이터 행 계산** | `getLastRow()` 대신 C열 기준 마지막 데이터 행(`_island_findLastDataRow_` 패턴)을 공용 헬퍼로 승격, 전 모듈 적용 |
| **외부 API 배치** | Supabase upsert 배치(M2), Kakao 우편번호 주소 캐시(M4) |
| **트리거 정돈** | 중복 설치 경로 제거(H5), 15:35↔15:40 간격 확보(M14), `deleteOrphanTriggers` 화이트리스트 자동화(H1) |

### 3.3 구조 개선 (중기)

1. **판정 로직 SSOT**: 마감 이동 조건·뷰어 탐색·컬럼맵을 각각 함수 1개로 통일 (H2, H8, M8)
2. **거대 파일 분할**: `_partnerExclusivePush.gs` 6,757줄 → Push/전용양식/OCR/일일마감 4개 파일
3. **오류 가시화**: 빈 catch에 Logger + 일일 오류 요약 Chat 카드 1회
4. **DB(Supabase) 우선 조회**: 대시보드·감사성 조회는 시트 스캔 대신 DB 조회로 전환(시트 부하 격리)

---

## 4. 실행 계획

### Phase 1 — 즉시 (사고 예방, ~1일) ✅ 완료 (2026-07-17)
- [x] H1 `deleteOrphanTriggers` 화이트리스트 자동화
- [x] H3 대리공급 Push ScriptLock
- [x] H4 허브 삭제 범위 제한
- [x] H5 Gmail 15분 트리거 설치 경로 차단
- [x] H8 뷰어 폴백 `null` 통일

### Phase 2 — 속도 체감 (~2일) ✅ 완료 (2026-07-17)
- [x] H9 rename 경로 bounded 수식 교체
- [x] M1/M3 행 단위 쓰기 배치화 (Orders·CancelReturn)
- [x] M6 CF 중복 정리 배치 (1회 실행 메뉴)
- [x] M4 우편번호 주소 캐시
- [x] M5 onEdit 허브 승인 경로 경량화

### 추가 완료 (2026-07-17) — 발주탭 B열 수집일 전환
- [x] onEdit B열 스탬프 제거 (`p2u_partnerOnEdit` no-op화)
- [x] 발주 수집 성공 행만 B열=수집일 덮어쓰기 (허브 주문일자와 통일)
- [x] M11(다중 행 날짜 스탬프)은 onEdit 스탬프 폐기로 소멸

### Phase 3 — 정합성 (~2일)
- [ ] H2 대리공급 마감 판정 통일 (미리보기=일괄=수동)
- [ ] H10 일괄마감 hubDateByUid 전달
- [ ] M8 inject/heal 단일화
- [ ] M13 cleanup 열 고정 제거
- [x] ~~M11 다중 행 날짜 스탬프~~ (소멸 — B열 수집일 전환으로 onEdit 스탬프 폐기)

### Phase 4 — 정리 (~1일)
- [ ] H6/H7 웹앱 API 안정화·키 이관
- [ ] M2 Supabase 배치화
- [ ] M9/M10 메뉴·마이그레이션 문구 정합
- [ ] 데드 코드 제거(2.3 목록)

### 검증 체크리스트 (각 Phase 공통)
1. `clasp push` 후 **진단 메뉴 먼저**: 🧪 처리 후 오류 점검 / 발주탭 수식 진단
2. 트리거 목록 확인 (`debugTriggers` — H1 수정 후에만)
3. 1개 업체로 축소 테스트 → 전체 반영
4. 다음날 아침 자동 배치 Chat 알림 3종(마감·수집·Push) 정상 확인

---

## 5. 운영 수칙 (재발 방지)

1. **발주탭 B열에 휘발성 수식 금지** — 날짜는 항상 값 스탬프
2. **수식 조회 범위 상수화** — 제품 증가 시 `_PT_VIEWER_LOOKUP_END_`만 조정 (현재 3000, 제품 2,900행)
3. **조건부서식은 교체(set)만, 누적(push) 금지**
4. **트리거는 `_ALL_SCHEDULED_TRIGGERS_` 배열로만 설치** — 개별 설치 메뉴 금지
5. **마감·Push 등 다중 파일 배치는 허브 데이터를 1회만 로드**
6. 새 기능은 반드시 진단 경로(사전점검/오류로그)와 함께 배포
