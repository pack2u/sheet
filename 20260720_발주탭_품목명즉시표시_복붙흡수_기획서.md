# 발주탭 품목명 즉시표시 + 다품목 복붙 흡수 기획서

작성일: 2026-07-20 · 상태: **최종 확정 (2026-07-20 13:10 운영자 재결정 — 아래 최종 결정 참조)**

> ## ★ 최종 결정 (2026-07-20 13:10) — 행별 수식 전환 취소, 스필 유지 + IFERROR 헤더 고정
>
> 운영자 재검토 결과 4안(행별 수식) 대신 **현행 스필 구조 유지 + 표시·복원 보강**으로 최종 확정:
>
> 1. **행 구조 무변경** — 헤더 1행, 입력 2행부터 그대로. (1행 숨김+3행 입력안은 수집·마감·송장 등 발주탭을 읽는 전 모듈 수정 + 전 업체 데이터 행 이동이 필요해 기각)
> 2. **헤더 #REF! 비노출** — 구현 중 검증 결과 IFERROR는 "Array result was not expanded" #REF!(스필 막힘)를 잡지 못함(구글 명세).
>    → 헤더행(A1:P1)에 `ISERROR` 조건부서식(오류 시 글자색=배경색)으로 #REF! 텍스트를 마스크. 스필이 살아나면 헤더가 즉시 정상 표시됨.
> 3. **자동입력 범위 100행으로 축소** (C2:C100, 기존 500행) — 단가조회 lookup은 $C$3:$G$3000 유지. 업체별 범위 미세조정은 운영자가 직접 하며, 복구(heal)가 이를 되돌리지 않도록 판정 완화.
> 4. **onEdit 무음 자동복원 유지** — D/L에 값이 붙으면 메시지 없이 해당 셀만 걷어내 스필 자동 재계산 (2~4초).
>
> 아래 본문(4안 행별 수식 설계)은 검토 기록으로 보존.

> **이전 확정 정책 (12:59 운영자 선택): D/L열은 항상 수식 — "무음 자동복원"**
> - D/L 표시는 행별 수식(즉시 0~1초)이 기본.
> - 복붙 등으로 D/L에 값이 들어오면 **메시지·토스트 없이 2~4초 내 수식으로 조용히 원복**.
>   D/L은 언제나 코드 기준(SSOT) 품목명·단가를 표시 = 사실상 메시지 없는 입력차단.
> - 코드가 틀리면 `🚨코드오류` 표시로 즉시 검증. 붙여넣은 품목명 텍스트는 유지되지 않음(운영자 승인).
> - 시트 범위 보호(강제)는 탈락: 거부창 억제 불가 + D/L 겹치는 복붙 전체가 거부됨.
> - 3-3의 "수집 시 품목명 불일치 경고"는 불필요해짐 (D/L에 사용자 값이 남지 않으므로) — 미구현.

---

## 1. 문제 정의

| # | 증상 | 결과 |
|---|---|---|
| 1 | 코드 입력 후 품목명 표시가 느리거나(수식 깨짐) 안 뜸 | 주문자가 품목명을 직접 타이핑하는 습관 발생 |
| 2 | 엑셀 다품목 복붙 시 품목명 열이 함께 붙여넣기됨 | D열 스필 수식 파괴 → **열 전체 #REF!** |
| 3 | #REF! 후 복구(재주입) ↔ 값 쓰기 반복 | "입력차단·자동복구" 순환, 운영 피로 |

### 근본 원인 — 전부 아니면 전무(all-or-nothing) 스필 구조

현재 D열(품목명)·L열(정산금액)은 **D1/L1 한 셀의 ARRAYFORMULA가 2~500행을 스필**하는 구조.

```
D1: ={"품목명(자동)"; ARRAYFORMULA(IF(C2:C500="","",IFERROR(VLOOKUP(…,'단가조회'!$C$3:$G$3000,2,FALSE),"🚨코드오류")))}
```

- 장점: 코드 입력 즉시 품목명 표시 (수식이므로 지연 0)
- 치명적 단점: **D2:D500 어디든 값이 1개라도 들어오면 스필이 막혀 열 전체가 #REF!**
- 다품목 복붙은 거의 항상 품목명 열을 포함 → 구조적으로 매번 깨질 수밖에 없음

---

## 2. 대안 비교

| 안 | 방식 | 즉시표시 | 복붙 내성 | 탈락/채택 사유 |
|---|---|---|---|---|
| A. 열 재배치 | 품목명(자동)을 복붙 영역 밖(우측 끝)으로 이동 | O | O | ❌ 열 구조 변경 금지 원칙 + 수집·마감·아카이브 전부 열 위치 의존, 전체 업체 마이그레이션 부담 |
| B. onEdit 실시간 **값** 채움 | C열 편집 감지 → D/L에 값 기록 | △(1~3초) | O | ❌ 과거 5~30분 지연 이력(구버전 주입 onEdit: 셀별 토스트·되돌리기·차단 등 무거운 로직이 원인). 값 모드라 수식의 "지연 0" 미달 |
| C. **스필 유지** + 자동 정리 | D/L에 값 감지 시 지우고 스필 재주입 | O | △ | ❌ 스필 구조라 복원 완료까지 **열 전체 #REF! 깜빡임**, 이벤트 유실 시 #REF! 잔존 + 셀별 토스트 — 7/20 폐기된 "입력차단·자동복구" 순환 그 자체 |
| **D. 행별 수식 + 무음 복원 (확정)** | D2:D500, L2:L500에 **행마다 개별 수식** 선주입 + onEdit이 덮인 셀만 조용히 원복 | **O (지연 0)** | **O** | ✅ 값이 덮여도 해당 행만 영향, **#REF! 원천 소멸**. 복원은 무음·배치 1회 (토스트·차단창 없음) |

---

## 3. 권장안 D — 행별 수식 모드 상세 설계

### 3-1. 수식 구조 변경

- D1/L1: 스필 수식 제거 → **일반 텍스트 헤더** ("품목명(자동)", "정산금액(자동)")
- D2:D500 각 셀:

```
=IF($C2="","",IFERROR(VLOOKUP(TRIM(CLEAN(SUBSTITUTE($C2,CHAR(160),""))),'단가조회'!$C$3:$G$3000,2,FALSE),"🚨코드오류"))
```

- L2:L500 각 셀: 동일 패턴, VLOOKUP 5열(최종단가), 오류 시 `""`
- A열(거래처명)·N열(상태) 스필은 복붙 영역 밖이므로 **현행 유지**
- 성능: 행별 VLOOKUP 500×2 = 1,000건 — 기존 스필 계산량과 동급, 체감 차이 없음

### 3-2. 시나리오별 동작

| 시나리오 | 동작 (확정: 무음 자동복원) |
|---|---|
| C열에 코드 1개 입력 | 해당 행 수식이 **즉시(0~1초)** 품목명·단가 표시 (현행과 동일 체감) |
| 엑셀 다품목 복붙 (품목명 포함) | 붙여넣은 행의 D/L만 잠시 텍스트로 덮임 → **2~4초 내 onEdit이 조용히 수식으로 원복** (메시지·토스트 없음). 열 전체 #REF! 없음(행별 수식이라 나머지 행 무영향). 원복 후 코드 기준 품목명 표시 — 코드 틀린 행은 `🚨코드오류`로 즉시 검증 |
| **수식이 지워진 행에 새 코드 입력** | onEdit 0계층(3-4)이 D/L 수식을 **즉시 재주입** → 2~4초 내 품목명 표시. "빈 행으로 하루 종일 방치 → 확인 불가" 공백 없음 |
| 잘못된 코드 입력 | 해당 행만 `🚨코드오류` 표시 (현행 유지) |
| D/L에 직접 타이핑 | 2~4초 뒤 수식으로 조용히 원복 (품목명·단가는 수동 입력 불가 열 — 코드 기준 자동 표시) |

### 3-3. 수집 시 SSOT 확정

발주탭 D/L 표시가 무엇이든, **수집은 C열 코드 기준 허브 단가표(SSOT)로 품목명·단가를 확정** 기록.

- 무음 자동복원 정책으로 D/L에 사용자 값이 남지 않으므로, 당초 검토했던 "수집 시 품목명 불일치 경고"는 **불필요 — 미구현**
- 수집 직후 D/L 수식 백필(②계층)로 정합 유지

### 3-4. 수식 유지 4계층 — 무음 자동복원 (확정 정책)

**"수식이 지워지는 문제"에 대한 답**: 시트 보호로 막는 것은 불가능/부적절 (아래 3-4-1).
대신 D/L에 무엇이 들어오든 **메시지 없이 수식으로 조용히 원복**하는 자가회복 구조로 설계한다.
D/L은 "코드 기준 자동 표시 전용 열"로 확정 — 사용자 값이 머물지 않는다(운영자 승인).

| 계층 | 시점 | 대상 | 비고 |
|---|---|---|---|
| **⓪ onEdit 무음 복원** | **발주탭 편집/붙여넣기 순간** | **편집 범위와 겹치는 D/L 셀 전체** (값이 덮였든 지워졌든 수식으로 원복) | **핵심 계층.** 근거: B열 날짜 스탬프가 같은 경로(`p2u_partnerOnEdit`)로 7/17까지 정상 작동. 구버전 onEdit과 달리 **토스트·알림 없음**, 편집 범위만 배치 1회 처리 → 지연 2~4초 |
| ① onOpen 백필 | 업체가 시트를 열 때 | D/L 전 행 중 수식 아닌 셀 전체 원복 | 기존 onOpen 래퍼(`p2u_partnerOnOpen`, 공지·메뉴 등록)에 한 단계 추가 — 재설치 불필요 |
| ② 수집 직후 백필 | 발주 수집 완료 시 | D/L 전 행 원복 | 허브 권한 실행 — 가장 확실한 계층 |
| ③ 마감·복구 백필 | 마감 잔류행 복원 후 / "발주탭 복구" | 전체 D/L 재주입 | 전량 리셋 안전망 |

#### ⓪계층 지연 예상

| 상황 | 품목명 표시까지 |
|---|---|
| 행에 수식이 살아있음 (평상시) | **0~1초** (수식 재계산, 스크립트 무관) |
| ⓪ 무음 복원 발동 — 보통 | **2~4초** (트리거 발화 1~2초 + 읽기1·쓰기1회 0.5~2초 + 재계산 <1초). 다품목 붙여넣기도 배치 1회 처리라 동일 |
| ⓪ 무음 복원 — 콜드스타트/서버 부하 | **5~15초** (해당 파일에서 스크립트 첫 발동 시. 두 번째 입력부터 2~4초권) |
| 트리거 이벤트 유실 (드묾) | 해당 행만 미표시 → 코드 재입력 시 재발동, 또는 ①오픈/②수집 때 회복 |

- **입력이 일어나는 순간엔 반드시 품목명이 표시됨(⓪)** — "빈 행으로 하루 종일 방치 → 마감 때야 채워져 확인 불가" 시나리오 원천 차단
- ⓪이 (쿼터·일시 오류로) 못 돌아도 ①~③이 순차 회복. 그 사이에도 데이터는 안전: D/L이 값 상태여도 수집은 코드 기준(SSOT)으로 확정
- D열 전체 삭제 같은 극단 케이스도 다음 입력(⓪)/오픈(①)/수집(②)에서 회복
- 기존 "자동복구"와의 차이: 예전엔 스필 구조라 값 하나에 **열 전체 #REF!** + 셀별 토스트·되돌리기로 시트가 버벅였지만, 이번엔 행별 수식이라 #REF! 자체가 없고, 복원은 **무음·편집 범위만 배치 1회**. D/L은 파생열이라 원복해도 데이터 손실 없음(원본 주문 데이터인 C·수량·수취인 등은 절대 건드리지 않음)

#### 3-4-1. "수식 덮어쓰기 차단" 옵션 검토 (탈락)

| 수단 | 동작 | 탈락 사유 |
|---|---|---|
| 범위 보호(경고만) | 편집 시 확인창 표시 | 복붙마다 확인창 — 성가시고, 확인 누르면 어차피 덮어써짐 (효과 없음) |
| 범위 보호(강제) | 소유자 외 D/L 편집 불가 | 거부창(구글 내장)은 스크립트로 억제 불가 + 붙여넣기 범위에 D열 포함 시 **복붙 전체 거부** → 주문자가 엑셀을 못 붙여넣음 |

→ 결론: "메시지 없는 입력차단"은 시트 보호로는 불가능하며, **⓪ onEdit 무음 복원이 그 역할을 대신**한다 (운영자 확정).

### 3-5. 구현 시 수정 지점 (참고용 — 구현 단계에서 확정)

| 파일 | 수정 |
|---|---|
| `_partnerHelpers.gs` | `_pt_injectOrderSpillFormulas` / `_pt_healOrderSpillFormulas` D/L 블록: 스필 → 행별 수식 주입으로 교체. 행별 수식 백필 헬퍼 신설 |
| `_partnerOrders.gs` | `_po_refreshAutofillBeforeCollect_`의 "D1/L1 헤더 수식 감지" 가드 → **D2/L2 수식 감지** 기준으로 변경 (행별 모드에선 헤더에 수식 없음). 수집 직후 ② 백필 호출 |
| `_partnerMonthlySettle.gs` | 잔류행 복원 시 D/L 제외 로직 → 행별 모드 기준으로 정비 + 복원 후 백필 |
| `_partnerLibrary.gs` | `p2u_partnerOnEdit`에 ⓪ 무음 복원 추가 (발주탭 편집 범위와 겹치는 D/L 셀을 수식으로 원복 — 토스트·알림 없음, 편집 범위만 배치 1회). `p2u_partnerOnOpen`에 ① onOpen 백필 추가 |
| 진단 | "발주탭 수식 진단"에 행별 모드 지원: 수식 아닌 D/L 셀 수(복붙 흔적) 리포트 |

### 3-6. 마이그레이션

1. 허브 배포: 수식 주입/복구/수집 로직 교체 (clasp push 1회)
2. 전체 업체: 복구 메뉴 "발주탭 복구" 일괄 실행 → 스필 제거 + 행별 수식 주입 (파일당 1회, 데이터 손실 없음 — D/L은 파생열)
3. 검증: 업체 1곳에서 ① 코드 단건 입력 즉시표시 ② 품목명 포함 다품목 복붙 → #REF! 없음 + 2~4초 내 D/L 무음 원복 ③ 수집 후 백필 확인

### 3-7. 리스크

| 리스크 | 평가 |
|---|---|
| 붙여넣은 품목명이 몇 초 뒤 코드 기준 품목명으로 바뀜 | **의도된 동작(운영자 승인)**. 코드가 틀리면 `🚨코드오류`로 표시되어 즉시 검증 가능. 원본 주문 데이터(C·수량·수취인 등)는 불변 |
| 코드 없이 품목명만 붙여넣는 주문자 | D열이 몇 초 뒤 빈칸으로 바뀌어 "지워졌다"고 느낄 수 있음 → 코드 입력 필수임이 오히려 즉시 드러남 (수집 시 코드 누락 오류로 뒤늦게 발견되던 것보다 빠른 피드백) |
| 행별 수식이 행 삭제/삽입으로 어긋남 | 상대참조($C2)라 행 단위 이동에 안전. 백필이 주기적으로 정합 회복 |
| 500행 초과 발주 | 현행 스필도 500행 상한 — 동일. 백필 시 상한 확장 가능 |

---

## 4. 과제 2 — 스크립트 재설치에 발주탭 수식 복구 통합

**현황**: 복구시스템에서 "🔧 스크립트 재설치"와 "📝 발주탭 수식 복구"가 **별도 메뉴**.
재설치만 하면 바인딩 스크립트는 교체되지만 깨진 D/L 수식은 그대로 남음 → 운영자가 두 번 실행해야 하고, 하나만 실행해서 "고쳤는데 그대로"라는 혼선 발생 (7/20 실사례).

**기획**:
1. `_pd_installVendorAutofillScript_`(재설치 코어)가 스크립트 교체 후 **같은 파일에 발주탭 수식 복구를 자동 실행** — 재설치 1회 = 완전 복구
2. 복구 경로 일원화: 현재 `_pt_repairOrderTabCollectMode_`(7/16 수집모드)와 `_pt_healOrderSpillFormulas`(수식복구) 두 갈래가 존재 → **최신 수식 정책(행별 수식) 하나로 통일**. 복구를 어디서 실행하든 같은 결과가 나오게
3. 결과 리포트에 "스크립트 ✅ / 수식 ✅" 분리 표기 (진단 경로)

---

## 5. 실행 순서 제안

| 단계 | 내용 | 규모 |
|---|---|---|
| 1 | 행별 수식 모드 전환 + ⓪ 무음 복원 + 백필 계층 (3-1 ~ 3-4) | 중 — 허브 배포 + 업체 일괄 복구 1회 |
| 2 | 재설치·수식복구 통합 (과제 2) | 소 |

승인 시 1~3단계를 한 번에 구현·배포하고, 업체 1곳 파일럿 검증 후 전체 적용을 권장.
