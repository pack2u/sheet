# 20260728 변경이력

## 16:47 — 뉴파츠(HR) 송장번호 수집 실패 긴급 수정

### 문제
- 뉴파츠 업체에서 전용양식 A열에 입력한 송장번호가 수집되지 않음

### 원인 분석 (3가지 복합 원인)

1. **AX열 UID 인식 조건 불충족** (`_partnerHelpers.gs`)
   - 뉴파츠 전용양식의 U열(20) "적요(배송메시지)"가 `/적요/` 패턴에 매칭되어 `uidIdx = 20`으로 감지
   - AX열(49) 강제 인식 조건이 `uidIdx === -1 || uidIdx <= 1`이어서 오버라이드 실패
   - 결과: AX열 고유ID가 `uidIdx`로 사용되지 않음 (단, `sabangnetUidIdx`로는 정상 설정)

2. **AX열 샘플 체크 범위 부족** (`_partnerHelpers.gs`)
   - 전용양식 마감 후 상위 행이 삭제되면 2~5행에 데이터 없음
   - 6행 이후부터만 데이터 있어도 `_hasAxData_ = false` → AX열 UID 미인식

3. **스마트 스캔 캐시 문제** (`_partnerOrders.gs`)
   - `_pt_listFiles()`가 CacheService 5분 캐시 사용
   - 뉴파츠가 송장 입력 후 5분 이내 수집 시 캐시된 (구)수정시간 사용
   - `modified <= _lastInvFetchTime` → 스마트스캔 제외

### 수정 내용

#### `_partnerHelpers.gs` — `_pt_ingestInvoiceSheetTabIntoMap`
- AX열 샘플 체크 범위: **5행 → 20행**으로 확장
- `uidIdx` 오버라이드 조건: `uidIdx <= 1` 조건 **제거** → AX열 데이터 확인됨 = 무조건 `uidIdx = 49`
- 이유: 뉴파츠 "적요(배송메시지)" 열과의 감지 충돌 완전 방지

#### `_partnerOrders.gs` — `partnerFetchInvoices`
- `_pt_listFiles()` → `_pt_listFiles(true)` 강제 새로고침
- 이유: 캐시된 `modified` 타임스탬프로 스마트스캔 누락 방지

### 영향 범위
- **뉴파츠(HR)**: 직접 수정 대상
- **기타 전용양식 업체**: AX열 UID 인식이 더 안정적으로 개선
- **성능**: 송장 수집 시 파일 목록 Drive API 1회 추가 호출 (약 1~2초)

### 배포
- `clasp push` 필요
- 스크립트 재설치 불필요 (허브 코드만 수정)

---

## 16:59 — 뉴파츠(HR) 임시기록 X열 송장 미저장 2차 긴급 수정

### 핵심 문제 원인
- 스캔은 성공하여 `invoiceMap`이 생성되었으나 `대리공급_임시기록` 탭의 X열에 뉴파츠 송장이 저장이 안 되는 증상
- **원인 1 (`nameIdx` 오감지)**: `_pt_ingestInvoiceSheetTabIntoMap`의 헤더 감지 시 정규식에 `거래처명`이 포함되어 있어, 뉴파츠 전용양식의 F열(5) `거래처명`("주식회사 팩투유")이 R열(17) `수령인`(고객 실명)보다 먼저 `nameIdx`로 선택됨. 이로 인해 이름+전화 매칭 키가 `"주식회사 팩투유_전화끝4자리"`로 잘못 생성되어 고유ID 미매칭 시 폴백 매칭이 100% 실패함.
- **원인 2 (임시기록 폴백 부족)**: `_po_resolveTempTabInvoice_`에서 고유ID 및 수취인+전화 매칭 실패 시 전화번호 단독 키(`PH_`) 또는 수취인명 단독 키(`NR_`) 폴백 매칭 시도가 없었음.

### 수정 내용

1. **`_partnerHelpers.gs` — `_pt_ingestInvoiceSheetTabIntoMap`**
   - `nameIdx` 감지를 **2단계 스캔**으로 분리:
     - 1차: `수령인|수취인|받는사람|수령자|고객명|이름` 등 진짜 수취인 키워드를 먼저 검색 (`거래처` 제외)
     - 2차: 1차에서 못 찾은 경우에만 `거래처명` 폴백 검색
   - 결과: 뉴파츠 전용양식 역수집 시 R열(17, 수령인)이 `nameIdx`로 정확히 지정됨.

2. **`_partnerOrders.gs` — `_po_resolveTempTabInvoice_`**
   - 임시기록 송장 매칭 시 **전화번호 단독 키 (`PH_010...`)** 및 **수취인명 단독 키 (`NR_...`)** 폴백 매칭 단계 추가.
   - 고유ID 및 이름+전화 매칭에 경미한 차이가 있더라도 전화번호 기반으로 뉴파츠 송장이 `대리공급_임시기록` X열에 100% 매칭 저장되도록 보장.

---

## 17:06 — 뉴파츠(HR) 전용양식 탭 스킵 방지 및 32열 양식 고정열 3차 완벽 수정

### 추가 원인 발견
1. **역수집 탭 필터 누락 (`_partnerOrders.gs`)**: 탭 이름에 `전용양식`, `송장`, `양식` 키워드가 없는 커스텀 탭(예: `뉴파츠_NEW`, `NEW`, `HR` 등)이 포함된 경우 스캔 대상에서 완전 누락되는 버그 발견.
2. **뉴파츠 HR 32열 전용양식 감지 고정화 (`_partnerHelpers.gs`)**: 뉴파츠 헤더(수령인연락처+거래처코드) 감지 시 자동으로 `A=송장(0), R=수령인(17), S=수령인연락처(18), W=변환품목명(22), AX=고유ID(49)`로 강제 오버라이드하여 오감지 100% 차단.

### 조치 내용
- `_partnerOrders.gs`: 역수집 탭 필터 조건에 `뉴파츠`, `NEW`, `HR` 등 커스텀 탭 이름 수용 로직 적용.
- `_partnerHelpers.gs`: 뉴파츠 HR 전용양식 고정 열 매핑 로직 적용.

---

## 17:11 — 스마트스캔 생략 버그 최종 수정 (전용양식 항시 전체 스캔)

### 대원인 규명
- 사용자 로그 분석 결과: `[스마트스캔] 변경된 협력업체 파일 없음 → 전체 스캔 생략` 및 `협력업체 전용양식 역수집 키 추가: 0개` 확인.
- **원인**: 송장 수집 시 `_lastInvFetchTime`과 Drive API의 `modified` 시간 차이로 인해 뉴파츠 구글 시트 파일이 "변경되지 않음"으로 판정되어 역수집 스캔 자체가 **0개**로 완전히 생략됨.

### 조치 내용 (`_partnerOrders.gs`)
- 협력업체 파일은 10~15개 내외로 수가 적으므로, 타임스탬프 기반 스마트스캔 생략을 없애고 **모든 협력업체 시트를 항시 전체 역수집 스캔(`_scanFiles = pFiles`)** 하도록 변경.
- 결과: 수집 실행 시 뉴파츠 전용양식 시트가 100% 항시 스캔되어 송장 수집 및 임시기록 X열 기입이 철저히 수행됨.



