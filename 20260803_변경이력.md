# 2026년 8월 3일 시스템 변경 이력

## 📅 변경 일시: 2026-08-03 (KST)

### 📦 변경 항목: 마감 트리거 시각 변경 + 당일 건 포함 + 새벽 02시 이카운트 삭제

#### 1. 마감 트리거 시각 (`_partnerWebApp.gs` `_ALL_SCHEDULED_TRIGGERS_`)
| 작업 | 이전 | 변경 |
|---|---|---|
| 통합 일일마감 | 23:30 | **22:00** |
| 대리판매 마감 | 07:00 | **23:00** |
| 대리공급 마감 | 07:20 | **23:30** |

#### 2. 당일 건 마감 포함
- `_partnerMonthlySettle.gs`: `dNum < todayNum` → `dNum <= todayNum` (오늘 날짜도 이동)
- `_partnerExclusiveArchive.gs`: `rowDateNum >= todayNum` 잔류 → `rowDateNum > todayNum` (오늘 포함, 미래만 잔류)
- 진단/미리보기 문구도 동일 기준으로 맞춤

#### 3. 새벽 02시 이카운트 연동
- 코드상 `atHour(2)` 설치는 이미 비활성(주석) 상태였음
- `setupAllScheduledTriggers` 재설치 시 잔여 CLOCK 트리거가 모두 제거되므로 구 02시 트리거도 정리됨
- 이카운트 전체동기화는 통합 트리거 **06:00 / 12:00** 만 유지
- `ecount.gs` 상태/안내 문구에서 02시 스케줄 표기 정리

#### 운영 필요 조치
1. clasp push 후 허브 메뉴 **⏰ 통합 자동 트리거 설치 (전체)** 실행
2. 설치 확인 팝업에서 22:00 / 23:00 / 23:30 스케줄 확인

---

### 📦 변경 항목: 월마감 ↔ 이카운트 판매현황 비교(검증)

#### 신규 파일
- `_partnerSettleReconcile.gs` — 업체·월별 독립 비교시트 / 월마감 불러오기 / 비교 실행

#### 독립 스프레드시트 (허브 탭이 아님)
- 폴더: 협력업체 폴더 아래 `월마감-이카운트_검증`
- 파일명: `[검증] 월마감-이카운트_{업체명}_{YYYY-MM}`
- 탭: `비교_월마감` / `비교_이카운트` / `비교_결과`

#### 메뉴 (`_partnerMenu.gs`)
`💼 협력업체 관리 → 📑 월마감↔이카운트 비교(검증)`
- `📂 독립시트 검증` — 업체별 `[검증]…` 파일에 불러오기/비교
- `📋 상품정보시트 검증` — 허브 탭(`비교_월마감` 등)에 불러오기/비교

#### 매칭 규칙
- 1차: 고유ID (이카운트 `주문자명(사방넷)`의 `/` 뒤)
- 보조: 품목코드 + 수량 + 전화끝4
- 금액: 월마감 정산금액 vs 이카운트 합계 (±1원)
- 상태: 양쪽일치 / 금액불일치 / 수량불일치 / 월마감만 / 이카운트만 / 월마감_취소반품

#### 비교_결과 표시 정리
- `개별단가_이카` = 이카운트 합계÷수량 (배송비 포함 개별단가)
- `단가차이` = 마감 개별단가(금액÷수량) − 이카 개별단가
- 상단 요약: 월마감 합계 / 이카운트 합계 / 합계차이

#### 수량×단가 마감 오류 / 단가차이 보정 (2026-08-03)
- **원인**: 발주탭 L열 헤더가 `단가`일 때 `_pt_buildOrderTabColumnMap`이 `unitPrice`를 못 잡아, 마감 이동 시 `단가×수량` 곱셈이 스킵됨 → 마감 금액이 1개분만 저장
- **수정**: `단가`/`단가(자동)` 헤더를 unitPrice로 매핑 + 마감 이동 시 헤더 재스캔 폴백
- **비교검증**: 합계차이가 이카 개별단가의 정수 배수이면 `단가차이` 비움 + 비고에 `1개단가마감`/`단가미기입` 표시

#### 월마감 정산금액(수량×단가) 강화 (2026-08-03)
- `_pms_resolveArchiveLineAmount_`: 단가조회 대조로 이미합계/1개분/미기입 판정 후 줄합계 산출
- 마감탭 금액 헤더를 `정산금액`(줄합계)으로 통일
- 메뉴 `📋 마감탭 정리 → 🔧 마감 정산금액 보정 (단가×수량)` — 기존 마감탭 일괄 보정

#### 이카운트 기준 월마감 수정 (2026-08-03)
- 비교 메뉴 `④ 이카운트 기준 월마감 수정`
- 금액불일치·수량불일치: 고유ID로 업체 발주 마감의 수량·정산금액을 이카 합계 기준으로 갱신
- 이카운트만: 해당 월 마감 탭 하단에 행 추가 (`이카운트보정추가`)
- 수정 후 비교_월마감 갱신 + 비교 재실행

#### 이카운트만 일부만 추가되던 문제 수정 (2026-08-03)
- 원인1: 동일 고유ID면 추가 스킵 → 배송비·다품목 누락
- 원인2: 이카 거래처명으로 업체 그룹이 갈라져 일부 행이 미상/다른 파일로 빠짐
- 수정: 단업체 비교시트는 전부 해당 업체로 모음 + 중복판정을 `UID+품목+수량+금액+일자`로 변경
