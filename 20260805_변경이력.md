# 2026년 8월 5일 시스템 변경 이력

## 📅 변경 일시: 2026-08-05 (KST)

### 📦 변경 항목: 허브 품절임박 출고확인 연동

협력업체 허브에서 `품절임박` 건을 직원이 확인한 뒤 `✅출고가능` / `🚨품절`로 확정하면 업체 발주탭에 즉시 반영.

#### 파일
- `_partnerOrders.gs` — placeholder/DV/수집·매칭·배포 가드 + onEdit 동기화 확장
- `_partnerMenu.gs` — 트리거 설치/제거 메뉴
- `menu.gs` — 주석 갱신

#### 백업
- `backup_20260805_pre_hub_stock_approve/`

#### 동작 요약
1. **수집**: `품절임박`은 허브에 수집됨. N열에 `재고확인후 판단` 안내(실제 송장 아님). O열에만 드롭다운(`🟡품절임박` / `✅출고가능` / `🚨품절`)
2. **송장 매칭·배포**: placeholder는 송장 없음으로 취급
3. **허브 O열 편집**: `✅출고가능` 또는 `🚨품절` 선택 시 업체 발주탭 상태 동기화 + N열 안내 삭제 + O열 DV 제거 (다중셀·Lock 지원)
4. **이카운트**: 기존과 같이 품절/품절임박은 `✅출고가능` 전까지 제외

#### 운영
`💼 협력업체 관리 → ⚙️ 자동화 설정 → ✅ 허브 상태동기화 트리거 설치 (출고가능/품절)`  
(기존 트리거가 있으면 한 번 재설치 권장)

`💼 협력업체 관리 → 🟡 품절임박 UI 소급적용 (N안내+O드롭다운)`  
푸시 전·누락 수집분에 N열 안내 + **O열(상태)** 드롭다운을 다시 붙임  
(드롭다운은 N열이 아님 — N=안내문구, O=상태 선택. O열 클릭 시 ▼ 표시)  
★ **M열(적요)에 `품절임박`만 있고 O가 접수완료인 경우도 대상** — O를 `🟡품절임박`으로 정규화 후 드롭다운 부착 (발송완료 등은 제외)
