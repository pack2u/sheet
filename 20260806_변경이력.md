# 2026년 8월 6일 시스템 변경 이력

## 📅 변경 일시: 2026-08-06 (KST)

### 📦 변경 항목: 품절 = 품절임박과 동일 재검토 수집

상품정보·발주탭·적요의 **품절**도 **품절임박**과 같이 허브에 수집하고, 직원이 `✅출고가능` / `🚨품절`로 확정하는 흐름으로 통일.

#### 파일
- `_partnerOrders.gs`

#### 동작
1. **수집**: 주문상태/적요/배송메시지/상품정보 상태가 `품절`(품절+7 제외)이면 → 허브 `🟡품절임박` + N열「재고확인후 판단」+ O열 드롭다운
2. **소급적용**: 기존 허브 `🚨품절` 행도 재검토 대상으로 포함 → `🟡품절임박` 정규화 + DV
3. **확정**: 직원이 O열에서 `✅출고가능` 또는 `🚨품절` 선택 시 기존과 같이 업체 발주탭 동기화
4. **이카운트**: `✅출고가능` 전 품절·품절임박 제외 (기존 유지)

#### 운영
- 신규 수집분은 자동 적용
- 이미 허브에 `🚨품절`로만 있는 건: `🟡 품절임박 UI 소급적용` 1회 실행 권장

---

### 📦 변경 항목: 준테크(JT) 전용양식 → 대한통운 양식

폴더 `대한통운 양식.xlsx` 열 구조로 준테크 전용양식 헤더·Push 매핑 변경.

#### 파일
- `_partnerExclusivePush.gs` — JT 헤더 23열 + sourceToTarget
- `InvoiceMatch_협력업체용.gs` — 송장 반영 시「운송장번호」열도 동기

#### 운영
1. clasp push 후
2. `전용양식 헤더 일괄 업데이트`
3. 이후 대리발주 Push 시 새 양식 적용

---

### 📦 변경 항목: 임시기록 W열 JT → 준테크 전용양식 Push

재고없음·대리공급 미분류 건을 `대리공급_임시기록` 하단에 추가하고 **W열에 JT(또는 준테크)** 기입 후  
`임시기록 → 전용양식 Push` 하면 준테크 전용양식으로 들어감.

#### 보강
- W열 정규화: `JT` / `준테크` / `[협력업체] 준테크` → `JT`
- 임시기록 탭을 상품정보 시트(`INFO_SS_ID`)에서 조회
- 보내는분(Q/R/S) 비어 있으면 팩투유 기본값
- Y열 `전용양식Push완료` 건 스킵, 생성 UID P열 되쓰기
