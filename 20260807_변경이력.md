# 변경이력 — 2026-08-07

## 발주탭 수식 복구 시 탭 양식 꼬임 수정

### 증상 (용기창고 사례)
- 발주탭에서 **주문일자·수량·고유ID** 열이 빠진 채 열이 왼쪽으로 밀림
  - B=이카운트코드, C=품목명, D=수취인 … / 상태 헤더 중복 / L열 `#REF!`
- 「발주탭 수식 복구」가 **고정 열(D/L/N)** 에만 ARRAYFORMULA를 넣어
  - 품목·단가·상태가 엉뚱한 열에 생기거나 스필이 막혀 양식이 붕괴

### 원인
- 수식 주입(`_pt_injectOrderSpillFormulas`)은 C=코드, D=품목명, L=단가, N=상태 전제
- 열 누락 상태에서는 헤더 텍스트만 덮어쓰거나 수식만 재주입하면 데이터·라벨이 어긋남

### 수정
| 파일 | 내용 |
|------|------|
| `_partnerHelpers.gs` | `_PT_ORDER_TAB_HEADERS_`(15열) + `_pt_normalizeOrderTabStructure_` 추가 — 누락 열 삽입·중복 상태/#REF!/잘못 위치 스필 정리 후 표준 헤더 |
| `_partnerHelpers.gs` | `_pt_repairOrderTabCollectMode_` — 수식 주입 **전** 열 정상화 선실행 |
| `_partnerMenu.gs` | 양식·디자인 초기화(`_repairBatch_header_`) — 맹목 `setValues` 제거, 정상화+수식복구로 통일 |
| `_partnerOrders.gs` | `partnerRepairOrderHeaders` — 동일 |
| `_partnerDeploy.gs` | 발주탭 생성 헤더를 표준 15열과 일치 (`도서산간배송비` 포함) |
| `repairDialog.html` | 증상 안내·소요 설명 보강 |

### 운영
1. clasp push 후 해당 업체에 **발주탭 수식 복구**(또는 양식·디자인 초기화) 재실행
2. 복구 후 확인: B=주문일자, C=이카운트코드, D=품목명, E=수량, L=정산금액, M=고유ID, N=상태, O=도서산간
3. 주문일자·수량은 삽입된 빈 열 — 필요 시 수집/수동 보완

---

## 통합 일일마감 송장 소스: 로젠 → 롯데

### 변경
| 파일 | 내용 |
|------|------|
| `_partnerExclusivePush.gs` | `_pep_archiveUnifiedDaily_`: 주 송장맵을 **롯데**(GID 1575029201, J=주문번호/사방넷ID → G=운송장)로 전환. 로젠은 **폴백**(롯데에 없는 키만) |
| `_partnerHelpers.gs` | `_PT_LOTTE_FIXED_COL` 상수화, 주석을 롯데 주소스에 맞게 정리 |
| `_partnerOrders.gs` | 송장 수집 로그 라벨 `병행택배사` → `롯데택배` (수집은 기존부터 롯데 병행) |
| `_partnerWebApp.gs` | 수동/스케줄 알림·존재여부 체크를 롯데 우선으로 표시 |

### 사방넷 연동 확인
- 스냅샷 매칭키 = 판매현황의 사방넷주문번호(고유ID)
- 롯데 탭 J열 = 동일 주문번호 → **사방넷 번호로 롯데 송장 매칭됨**
- 허브 송장 수집도 이미 롯데 탭을 invoiceMap에 합산 (고유ID 매칭)
- `_PT_SABANGNET_GID` 상수만 있고 **별도 사방넷 택배 탭 ingest는 없음** (사방넷은 주문번호 키, 송장은 택배사 탭에서 옴)
- `사방넷_송장매칭` 탭은 미매칭/비협력 출력용이며 일일마감 소스에서는 제외(대리공급_임시기록과 중복)

## 품절 = 품절임박 수집·UI 자동 적용 (수집 연동)

### 변경
| 파일 | 내용 |
|------|------|
| `_partnerOrders.gs` | `_po_applyStockWarnUiOnHubCore_` 분리 — 품절·품절임박 허브 UI 소급 공통 코어 |
| `_partnerOrders.gs` | `partnerCollectOrders` 신규 수집 후 **허브 소급 UI 자동 실행** (기존 🚨품절 잔여 포함) |
| `_partnerOrders.gs` | 상품정보 상태 판정도 `_po_isStockWarnReviewStatus_`로 통일 (품절·품절임박) |
| `_partnerMenu.gs` | 메뉴명 「품절·품절임박 UI 소급적용」 |

### 동작
1. **수집**: 주문/적요/상품정보 `품절` → 허브 `🟡품절임박` + N「재고확인후 판단」+ O 드롭다운 (기존 유지)
2. **수집 직후**: 허브 전체 소급 UI 자동 — 수동 「소급적용」과 동일 코어
3. **메뉴**: 필요 시 수동 재실행 가능
