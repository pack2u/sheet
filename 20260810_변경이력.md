# 변경이력 — 2026-08-10

## 부원(BW) 전용양식 → 「부원택배 양식 (1).xlsx」

### 변경
| 파일 | 내용 |
|------|------|
| `_partnerExclusivePush.gs` | `_PEP_EXCLUSIVE_FORM_HEADERS_.BW` 18열 (송장번호·이슈 + 엑셀 16열) |
| `_partnerExclusivePush.gs` | `_PEP_VENDOR_DIRECT_MAP_.BW` sourceToTarget·totalCols·상품명G/수량H 갱신 |
| `_partnerDeploy.gs` | 부원 headerCsv 동기화 |

### 운영 탭 헤더 (수동 반영)
`송장번호 | 이슈 | 받는사람 | 전화번호 | 주소 | (빈) | 상품명 | 수량 | B2750 | C3200 | D5500 | E6500 | 배송메세지 | 운임구분 | 운임 | 보내는사람 | 주소 | 전화`

※ 미마감 행은 열 어긋남 — 가능하면 마감 후 헤더 교체. clasp push 후 Push부터 새 매핑 적용.

---

## 송장 스케줄 변경 + 허브 상태/재고 중복 점검

### 변경 (`_partnerWebApp.gs` `_ALL_SCHEDULED_TRIGGERS_`)
| 작업 | 이전 | 변경 |
|------|------|------|
| 냅킨코리아 Gmail 송장수집 | 15:35 | **16:00** |
| 송장 수집 | 15:40 | **16:05** |
| 송장 배포 | 16:20 | 유지 |

### 통합허브 상태/재고 (`runDailyHubBatch`) 중복 확인
| 경로 | 시각 | 비고 |
|------|------|------|
| 통합 자동 트리거 | **12:40** | 정식 SSOT |
| 레거시 `setupDailyTrigger` (ecount) | 12:30 | 통합 설치 시 차단됨. 구 트리거 잔존 시 **12:30+12:40 이중 실행** 가능 |
| 16:00대 | 없음 | 송장(16:00/16:05/16:20)과 **겹치지 않음** |

→ 오후 4시 송장 구간과 허브 업데이트가 겹치지는 않음.  
→ `runDailyHubBatch`가 **2개 이상**이면 메뉴「자동 트리거 상태 확인」또는「통합 자동 트리거 설치」로 정리.

### 운영
코드 push 후 **⏰ 통합 자동 트리거 설치 (전체)** 1회 실행해야 실제 Apps Script 트리거 시각이 바뀜.
