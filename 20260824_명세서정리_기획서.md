# 협력업체 시트 「명세서 정리」 탭 기획서

작성일: 2026-08-24 · 갱신: 2026-08-24  
상태: **기획 v2 — 운영자 결정 반영**

---

## ★ 운영자 확정 사항 (2026-08-24)

| 항목 | 결정 |
|------|------|
| 헤더 | **1행 헤더 자동 인식** (프로필 `headerHints` + `_cs_guessHeaderRow_` 유사 로직) |
| VAT | **VAT 포함 기준**으로 단가·금액 비교 (명세가 공급가+부가세 분리면 합산 후 비교) |
| 비교 원천 | **전용양식 + 전용발주 마감 둘 다** (동시 로드, 상태별 표시) |
| 최종 용도 | **이카운트 구매입력** — 업로드용 양식 탭 자동 생성 |
| 변환·대사 | 받은 명세 → **팩투유 품목명·코드로 변환** → **동일 구조의 명세서 재생성** → **단가 차이** 표시 |
| 입력 채널 | 메일 / 카톡 / 엑셀 / 이미지 등 **형식 제각각** → 송장매칭(InvoiceMatch) 패턴 + **개선 파이프라인** (§9) |
| 파일럿 샘플 | **부원KH(BW)** 거래명세서 (08/19, 롯데택배, 용기+택배 혼합) |

---

## 1. 배경 · 문제

공급사(부원, 뉴파츠, 냅킨코리아, 준테크 등)에서 **거래명세서·출고명세·정산 엑셀**이 메일·카톡·엑셀·이미지 등 **다양한 형태**로 온다.

현재 수동 작업:

1. 우리 **전용양식 / 전용발주 마감**과 품목·수량·수취인 대사
2. **단가조회(뷰어)**·공급가 탭과 **VAT 포함 단가** 대사
3. 공급사 품목명 → **팩투유 품목명·이카운트코드** 변환
4. **이카운트 구매입력** 양식으로 다시 작성

### 기존 기능과의 관계

| 기존 | 역할 | 명세서 정리 |
|------|------|-------------|
| `InvoiceMatch` | 송장·수취인 추출 → 전용양식 A열 | **품목·단가·합계**까지 확장 |
| `partnerArchiveExclusiveForm` | 전용양식 → 마감 | 마감을 **비교 SSOT** 중 하나로 사용 |
| `partnerRunSettleReconcile` | 발주마감 ↔ 이카운트 **판매** | 구매 명세 대사는 별도 |
| HR 전용양식 32열 | 이카운트 **구매발주** 업로드형 | 출력 탭 **템플릿** |

---

## 2. 목표

| # | 목표 | 성공 기준 |
|---|------|-----------|
| G1 | 다채널 입력 → 표준 행 | 엑셀/카톡/이미지 3경로, 헤더 자동 인식 |
| G2 | **전용양식 + 마감** 동시 대사 | 송장/UID/품목 기준, 원천별 불일치 표시 |
| G3 | **VAT 포함** 단가·금액 검증 | 견적(단가조회) vs 명세, 허용치 1원 |
| G4 | **팩투유 명세서** 재생성 | 원 명세와 동일 레이아웃, 품목명만 우리 기준 |
| G5 | **이카운트 구매업로드** 탭 출력 | HR형 30열 또는 prefix별 구매양식 |
| G6 | 합계 **자동 검증** | 행합계 vs 명세 요약란 (부원 사례) |
| G7 | 진단·Dry-run | 사전점검 메뉴, 오류 로그 |

---

## 3. 전체 처리 흐름

```
[입력] 메일·카톡·엑셀·이미지
    ↓ ingest (§9)
[명세서_원본]  ← 원문 보존
    ↓ parse + header detect + VAT normalize
[표준 중간모델]  ← Canonical Row (§6)
    ↓ mapItem (누적품목매핑·공급가탭·단가조회)
    ├→ [명세서_정리]      비교·상태·단가차
    ├→ [명세서_팩투유명세]  원 양식 미러 + 우리 품목명
    └→ [명세서_이카구매]    이카운트 구매입력 붙여넣기용
         ↓
    이카운트 PC 구매입력 (수동 붙여넣기)
```

---

## 4. 탭 구조 (업체 파일)

기존 `발주 및 송장조회`·`전용양식`·`단가조회` **열 변경 없음**.

| 탭 | 역할 |
|----|------|
| `명세서_설정` | 대상월, prefix, VAT모드, 허용오차 |
| `명세서_원본` | 공급사 원문 (붙여넣기 / 파서 결과) |
| `명세서_정리` | **비교·상태·단가차** (운영 메인) |
| `명세서_팩투유명세` | **팩투유 품목명**으로 재구성한 거래명세서 |
| `명세서_이카구매` | **이카운트 구매발주 업로드** 붙여넣기용 |
| `명세서_비교결과` | 요약·합계·미매핑 |
| `명세서_오류로그` | 실행 이력 |

### 4-1. `명세서_설정`

| 셀 | 내용 | 기본값 |
|----|------|--------|
| B2 | 대상월 `yyyy-MM` | 당월 |
| B3 | 공급사 prefix | `BW`, `HR` … |
| B4 | VAT 기준 | **`포함`** (고정 기본) |
| B5 | 단가 허용 오차(원) | `1` |
| B6 | 수량 허용 오차 | `0` |
| B7 | 비교 원천 | **`전용양식+마감`** (고정) |
| B8 | 프로필 ID | `BW_거래명세서_v1` |
| B9 | 입력 출처 | `excel` / `kakao` / `image` / `gmail` |

### 4-2. `명세서_정리` (비교 메인)

| 열 | 헤더 | 설명 |
|----|------|------|
| A | 상태 | ✅ / ⚠️ / ❌ |
| B | 매칭키 | 송장 / UID / Fuzzy |
| C | 송장번호 | |
| D | 고유ID | AX |
| E | 이카운트코드 | **우리** |
| F | **우리품목명** | |
| G | 공급사품목명 | 원문 |
| H | 수량_명세 | |
| I | 수량_전용양식 | |
| J | 수량_마감 | |
| K | **단가_명세( VAT포함)** | 공급가+부가세 환산 |
| L | **단가_견적( VAT포함)** | 단가조회 G |
| M | **단가차** | L − K |
| N | 금액_명세( VAT포함) | |
| O | 금액_견적 | |
| P | 금액차 | |
| Q | 수취인_명세 | |
| R | 수취인_발주 | |
| S | 원천일치 | `양식✓ 마감✓` / `양식만` / `마감만` / `없음` |
| T | 비고 | `PRICE_DIFF`, `UNMAPPED` … |
| U | 원본행 | |

### 4-3. `명세서_팩투유명세` (미러 명세서)

**원 명세와 동일한 열 구조**를 프로필로 유지. 값만 변환:

| 원 열 (부원 예) | 변환 후 |
|-----------------|---------|
| 품목명 | **우리 품목명** (매핑 실패 시 `⚠️원문`) |
| 단가 | **견적 단가(VAT포함)** |
| 공급가액·부가세 | 견적 기준 재계산 (수량×단가, VAT 분리) |
| (추가 열) | `단가차`, `매핑코드` |

**용도:** 공급사 명세와 **나란히** 보며 단가·품목 차이 확인.  
하단 **합계행**: 명세 합계 vs 견적 합계 vs 차이 (부원 601,095원 검증 패턴).

### 4-4. `명세서_이카구매` (구매입력용)

HR 전용양식 C~AF **30열** = 이카운트 구매발주 업로드 양식 (`_PEP_EXCLUSIVE_FORM_HEADERS_.HR`):

```
일자 | 순번 | 거래처코드 | 거래처명 | … | 품목코드 | 품목명 | 규격 | 수량 | 단가 | 금액1 | 공급가액 | 부가세 | …
```

- **거래처코드/명**: `설정!B6/B5` (SSOT)
- **품목코드/명**: 매핑 결과
- **단가**: VAT 포함 → 이카운트 규칙에 맞게 **공급가·부가세 분리** 출력  
  (`공급가액=round(금액1/1.1)`, `부가세=금액1−공급가액`)
- **일자**: 명세 일자 또는 대상월 말일
- prefix별 **다른 구매양식**은 2차 (BW는 용기+택배 혼합 → 품목코드 매핑 후 HR형 출력)

> API `sendPurchaseToEcount`는 허브용. 협력업체는 **PC 구매입력 엑셀 붙여넣기** 우선 (판매현황과 동일 운영).

---

## 5. VAT 포함 규칙 (확정)

### 명세 파싱

| 명세 형태 | 처리 |
|-----------|------|
| 단가 + 공급가액 + 부가세 (부원형) | `단가_포함 = 단가 × 1.1` 또는 `(공급가액+부가세)/수량` |
| VAT 포함 단가만 | 그대로 |
| VAT 제외 단가만 | `× 1.1` 후 비교 |

### 견적 쪽

- **단가조회 G열(최종단가)** = VAT **포함**으로 간주 (협력업체 뷰어 기본)
- 공급가 탭(뉴파츠공급가 등)도 프로필에 `vatMode` 명시

### 비교

```
단가차 = 단가_견적(포함) − 단가_명세(포함)
금액차 = 수량 × 단가_견적 − 수량 × 단가_명세(명세 행합계)
```

---

## 6. 표준 중간모델 (Canonical Row)

모든 입력 채널이 먼저 이 구조로 통일:

```javascript
{
  lineId: "stmt|0824|3",
  date: "2026-08-19",
  supplierItemName: "사각반죽용기 200s 투명",
  supplierItemCode: "",
  spec: "500/CT",
  qty: 500,
  unit: "EA",
  unitPriceExVat: 50,
  supplyAmt: 25000,
  vatAmt: 2500,
  unitPriceIncVat: 55,        // ★ 비교용
  amountIncVat: 27500,
  invoice: "259931147055",    // 품목명内 송장 추출
  recipient: "안국현",
  invoiceFromName: true,      // 택배행 여부
  rawRow: 3,
  sourceChannel: "image"
}
```

**부원KH 패턴 (파일럿):**

- **용기행**: 규격·EA 수량·낮은 단가
- **택배행**: 품목명内 `12자리 송장`, 수량 1, 2,750~6,500원
- **합계 검증**: 표 행합 ≠ 요약란 → `⚠️ PARTIAL_CAPTURE` (캡처/붙여넣기 누락)

---

## 7. 이중 비교: 전용양식 + 마감

### 로드

```javascript
linesForm  = loadExclusiveForm(ss)           // 진행 중
linesArch  = loadExclusiveArchive(ss, month) // (YYYY년 M월) 전용발주 마감
linesOrder = mergeForCompare(linesForm, linesArch)
```

### 병합·표시 규칙

| 상황 | `원천일치` | 설명 |
|------|------------|------|
| 양쪽 모두 존재·일치 | `양식✓ 마감✓` | 정상 |
| 양식만 | `양식만` | 아직 마감 전 |
| 마감만 | `마감만` | 양식에서 이미 삭제(마감 이동됨) |
| 수량/품목 불일치 | `❌ 불일치` | 상세 코드 |

**매칭 키 우선순위:** 송장 → AX(UID) → 수취인+송장(품목명内) → Fuzzy

**택배비 행:** 송장번호로 전용양식 A열·마감 A열과 1:1 연결 (부원 B2750~E6500 열과 교차 검증 2차)

---

## 8. 품목명 변환 (팩투유)

| 순위 | 소스 | 예 |
|------|------|-----|
| 1 | 품목명内 이카운트코드 / 송장만 | — |
| 2 | 공급사 품목명 → `누적품목매핑` 역조회 | `사각반죽용기 200s` → 코드 |
| 3 | 공급사 공급가 탭 | HR `뉴파츠공급가` |
| 4 | Fuzzy (단가조회 C열 품목명) | 후보 3개 제안 |
| 5 | 택배행 | `B2750`/`C3200` 등 **운임등급** → 전용양식 I~L열과 매칭 |

실패: `⚠️ UNMAPPED` — `명세서_비교결과`에 수동 매핑 UI (InvoiceMatch 수동매칭 패턴)

---

## 9. 입력 채널 · 파싱 (InvoiceMatch 참고 + 개선)

### 9-1. 현재 InvoiceMatch 강점

- 카톡 **텍스트** 붙여넣기 + regex
- **이미지** Gemini Vision OCR
- **엑셀** 첨부 탭
- 결과 **미리보기 → 확인 → 반영**

### 9-2. 명세서 정리용 개선안 (권장)

**「통합 ingest 파이프라인」** — 송장매칭과 **사이드바 UI 공유**, 파서만 분기:

```
┌─────────────────────────────────────────┐
│  📋 명세서 정리 사이드바                    │
├─────────┬─────────┬─────────┬───────────┤
│ 엑셀/표  │ 카톡/텍스트│  이미지  │  Gmail   │
│ 붙여넣기 │ (regex+AI)│ (Vision)│ (2차)    │
└────┬────┴────┬────┴────┬────┴─────┬─────┘
     └──────────┴─────────┴──────────┘
                    ↓
         detectProfile(prefix, headers)
                    ↓
         parseToCanonicalRows()
                    ↓
         validateTotals()  ← ★ 행합 vs 요약 (부원 필수)
                    ↓
         [미리보기 테이블] ← 사용자 Confirm
                    ↓
         write 명세서_원본 + runCompare()
```

| 개선점 | 이유 |
|--------|------|
| **Canonical Row 먼저** | 송장매칭은 A열만; 명세는 품목·단가·합계 필요 |
| **Confirm 게이트** | OCR/AI 오인식 방지 (이미지 명세) |
| **합계 검증 게이트** | 행합 ≠ 요약란 → 경고·진행 중단 옵션 |
| **프로필 auto-detect** | 헤더 `일자·품목명·공급가액·부가세` → `BW_거래명세서_v1` |
| **엑셀 1순위** | 가장 정확 — 메일 첨부 `.xlsx` → 시트 import 또는 붙여넣기 |
| **카톡 2순위** | 기존 Gemini 프롬프트 확장 (품목·단가·부가세 JSON) |
| **이미지 3순위** | 부원 스캔 PDF/사진 — Vision → JSON → Confirm |
| **Gmail 4순위** | `_partnerGmailInvoice.gs` 패턴 확장, 첨부 → ingest |

### 9-3. 채널별 구현

| 채널 | 1차 | 2차 |
|------|-----|-----|
| **엑셀** | `명세서_원본` 직접 붙여넣기 + 헤더 detect | Drive 첨부 자동 import |
| **카톡** | InvoiceMatch 텍스트 파서 + **품목 테이블 JSON** 프롬프트 | — |
| **이미지** | `ocrImageToText` → 구조화 JSON (품목·단가·합계) | PDF multi-page |
| **메일** | 수동: 첨부 엑셀 다운 → 붙여넣기 | Gmail 라벨 트리거 |

### 9-4. Gemini 프롬프트 (명세용 — 송장매칭 확장)

```
거래명세서/출고명세에서 JSON 배열 추출:
[{ date, itemName, spec, qty, unit, unitPrice, supplyAmt, vatAmt,
   invoiceInName, recipient, rowType: "product"|"shipping" }]
+ summary: { supplyTotal, vatTotal, grandTotal }
행합계와 summary 일치 여부 report.
```

---

## 10. 파일럿: 부원KH(BW) 프로필

### 원문 구조 (확인됨)

```
일자 | 품목명 | 규격 | BOX | 수량 | 단가 | 공급가액 | 부가세
08/19 | 사각반죽용기 200s 투명 | 500/CT | 1 | 500 EA | 50 | 25,000 | 2,500
08/19 | 대가… 안국현 259931147055 | | 1 | 1 | 2,750 | 2,750 | 275
…
요약: 공급가액 546,450 | 부가세 54,645 | 합계 601,095
```

### 프로필 `BW_거래명세서_v1`

```json
{
  "id": "BW_거래명세서_v1",
  "prefix": "BW",
  "headerRowDetect": true,
  "headerHints": ["일자", "품목명", "규격", "공급가액", "부가세"],
  "columns": {
    "date": ["일자"],
    "itemName": ["품목명"],
    "spec": ["규격"],
    "box": ["BOX"],
    "qty": ["수량", "수량(단위포함)"],
    "unitPriceExVat": ["단가"],
    "supplyAmt": ["공급가액"],
    "vatAmt": ["부가세"]
  },
  "summaryRowHints": ["전잔액", "공급가액", "부가세", "총합계"],
  "vatMode": "excluded_columns", 
  "compareIncVat": true,
  "invoiceInItemName": "\\d{12}",
  "rowTypeRules": {
    "shipping": "invoiceInItemName && qty==1",
    "product": "spec || qty>1"
  },
  "shippingPriceCols": ["B2750", "C3200", "D5500", "E6500"],
  "mirrorLayout": "same_as_source",
  "ecountOutput": "HR_구매발주_30col"
}
```

### 파일럿 검증 시나리오

1. 이미지 10행 OCR → 행합 231,880 vs 요약 601,095 → **`PARTIAL_CAPTURE` 경고**
2. 전체 엑셀 수신 시 → 행합 = 요약 일치 확인
3. 용기 3종 + 택배 5건 → 전용양식·마감 송장 매칭
4. `명세서_팩투유명세` 단가 = 단가조회 G, `단가차` = 0
5. `명세서_이카구매` 붙여넣기 → 이카운트 PC에서 행 수 일치

---

## 11. UI · 메뉴

### 허브 `_partnerMenu.gs`

```
📋 명세서 / 공급 정산
  1) 명세서 탭 생성 (현재 파일)
  2) 📋 명세서 정리 사이드바 열기     ← InvoiceMatch 확장
  3) 비교 실행 (원본 탭 기준)
  4) 사전 점검 / Dry-run
  5) 팩투유명세 · 이카구매 탭 갱신
  ──
  6) 전체 업체 탭 일괄 생성
  7) 프로필 진단 (prefix)
```

### 사이드바 `statementReconcileSidebar.html`

InvoiceMatch v12 구조 계승:

- 탭: **표 붙여넣기 | 카톡/텍스트 | 이미지 | (2차 Gmail)**
- 하단: **미리보기 → 합계검증 → [명세서 반영] → [비교 실행]**
- 결과: 일치/단가차/미매핑 건수

---

## 12. 모듈 설계

| 파일 | 책임 |
|------|------|
| `_partnerStatementReconcile.gs` | ingest·compare·write |
| `_partnerStatementProfiles.gs` | BW/HR/NK 프로필, 헤더 detect |
| `_partnerStatementDeploy.gs` | 탭군 생성 |
| `_partnerStatementEcount.gs` | 이카구매 30열 변환 |
| `statementReconcileSidebar.html` | UI |

### 주요 함수

- `openStatementReconcileSidebar()` — UI 진입
- `ingestStatement_(channel, raw, opts)` — 통합 ingest
- `parseStatementToCanonical_(rows, profile)` — 중간모델
- `validateStatementTotals_(rows, summary)` — 합계 검증
- `mapToPack2UItems_(canonical, prefix, ss)` — 품목 변환
- `loadExclusiveFormAndArchive_(ss, month)` — **이중 원천**
- `compareStatement_(canonical, orders, priceMap)` — 비교
- `buildPack2UMirrorStatement_(canonical, mapped)` — 팩투유명세
- `buildEcountPurchasePaste_(mapped, settings)` — 이카구매
- `partnerRunStatementReconcile(opts)` — 일괄 실행
- `partnerDiagnoseStatementReconcile()` — 사전점검

---

## 13. 구현 단계 (개정)

### Phase 0 — 파일럿 확정 (1주)

- [x] 운영자 결정: VAT포함, 이중비교, 이카구매, 팩투유명세
- [ ] 부원 **전체 엑셀** 명세 1건 (546,450원 전체 행)
- [ ] `BW_거래명세서_v1` 프로필 동결

### Phase 1 — BW 엑셀 경로 (2주)

- [ ] 탭군 생성
- [ ] 엑셀 붙여넣기 → Canonical → 합계검증
- [ ] 팩투유 품목 매핑 + `명세서_팩투유명세`
- [ ] `partnerDiagnoseStatementReconcile()`

### Phase 2 — 이중 비교 (2주)

- [ ] 전용양식 + 마감 동시 로드
- [ ] 송장·택배행 매칭
- [ ] `명세서_정리` 상태·단가차

### Phase 3 — 이카구매 출력 (1주)

- [ ] `명세서_이카구매` HR 30열
- [ ] VAT 분리 출력 (구매입력 규칙)
- [ ] 운영자 이카 PC 붙여넣기 검증

### Phase 4 — 멀티채널 (2주)

- [ ] 사이드바: 카톡·이미지 (InvoiceMatch 공유 파서)
- [ ] Confirm + 합계 게이트
- [ ] HR/NK 프로필 추가

### Phase 5 (선택)

- [ ] Gmail 첨부 자동 ingest
- [ ] Supabase 감사 로그

---

## 14. 리스크

| 리스크 | 대응 |
|--------|------|
| 이미지/OCR 누락 (부원 10행 vs 601,095) | `validateTotals` + PARTIAL 경고 |
| VAT 혼동 | **포함 고정**, 프로필 `compareIncVat: true` |
| 용기+택배 혼합 | `rowType` + 송장 추출 |
| 양식 vs 마감 시차 | `원천일치` 열로 구분 |
| 이카 구매양식 업체별 상이 | 1차 HR 30열, prefix별 2차 |

---

## 15. 참고 코드

| 주제 | 파일 |
|------|------|
| 송장·명세 OCR | `InvoiceMatch_협력업체용.gs`, `_partnerExclusivePush.gs` |
| 전용양식·HR 32열 | `_partnerExclusivePush.gs` `_PEP_EXCLUSIVE_FORM_HEADERS_` |
| 부원 BW 18열 | 동일 `BW:` |
| 마감 | `_partnerExclusiveArchive.gs` |
| 대사 UX | `_partnerSettleReconcile.gs` |
| Gmail | `_partnerGmailInvoice.gs` |
| 단가 | `_partnerDeploy.gs`, `priceManager.gs` |

---

*다음: 부원 **전체 엑셀** 명세 1건 → Phase 1 BW 파일럿 착수.*
