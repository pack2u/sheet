# 변경이력 — 2026-09-03

## 사방넷 송장대량등록 ← 세트분리(뉴) 연동

### 배경
세트분리 V2(세트분리(뉴) 시트, `1JuwZjorb…`)의 「🔁 송장 전파」가
롯데 실적 + 대리공급_임시기록을 합쳐 「사방넷등록」 탭(주문번호·운송장번호·택배사)을 만든다.
이 결과를 기존 메뉴 「5️⃣ 허브로 송장 수집 → 📊 사방넷 송장대량등록 엑셀 저장」이
그대로 포함하도록 원천을 하나 늘렸다. 팀 워크플로 변화 없음.

### 변경
| 파일 | 내용 |
|------|------|
| `_partnerOrders.gs` | `_po_rebuildSabangnetBulkUpload_()` 에 5번째 원천 추가 — 세트분리(뉴) 「사방넷등록」 탭 (기존: 임시기록·허브·롯데탭·구세트분리 송장매칭) |

### 동작
- 주문번호|송장 `seen` 중복 제거라 구 세트분리와 병행 운영 중 겹쳐도 안전
- V2 전화주문 ID(`^\d{6}-PH-`)는 사방넷 주문번호가 아니므로 제외
- 택배사가 롯데가 아니면 `_pep_sabangCodeForCarrier_` 로 사방넷 코드 변환, 미지정은 `skipNoCode`
- V2 시트를 못 읽으면 건너뛰고 로그만 남김 (기존 4개 원천은 그대로 동작)
- 결과 로그: `[사방넷대량등록] 세트분리(뉴) 보강 N건`

### 관련 (세트분리V2 프로젝트 쪽, 별도 배포)
- 「🔁 송장 전파」가 대리공급_임시기록(상품정보 P/V/X열)도 읽어 롯데 실적과 병합
- 원장(주문라인원장)에 운송장번호·송장매칭(롯데 직접/대리공급/합포장 전파) 기록
- 상세: `세트분리V2/README.md`

---

## 세트분리(뉴)에 사방넷 송장대량등록 이식 (같은 날 후속)

허브 메뉴를 쓰지 않고 세트분리(뉴)에서 완결되도록 `_po_rebuildSabangnetBulkUpload_` 를 포팅했다.
`세트분리V2/gasBulk.js` 신설, `gasExcel.js` 폐기.

### 원천 넷 — 회차가 아니라 표를 직접 훑는다
| # | 원천 | 열 | 비고 |
|---|------|-----|------|
| 1 | 대리공급_임시기록 (상품정보) | P=주문번호 X=송장 W=업체 | |
| 2 | 협력업체_발주허브 (상품정보) | C=주문번호 N=송장 B=업체 | |
| 3 | 롯데 자사출고 (거래관리시스템송장) | J=주문번호 G=송장 | 코드 002 |
| 4 | 주문라인원장 (오늘 전체 회차) | 고유ID·운송장번호 | **합포장 동봉 전용** |

### 이식 중 실제로 문제였던 것
- **회차 범위로 좁혔던 것** — 허브 원본은 회차 개념이 없다. 좁히니 94행밖에 안 나왔다.
- **합포장 동봉 142건 누락** — 동봉 주문은 자기 번호로 롯데에 올라간 적이 없어 롯데탭에 없다.
  원장에서 `송장매칭 = 합포장 전파` 인 행을 대표 송장으로 채택해야 한다. 이게 원장을 원천에 넣은 이유.
- **날짜 필터 없음** — 원천 표는 여러 날치가 쌓인다. `대량등록_대상일수`(기본 1=오늘) 신설.

### 결과
사방넷 업로드 실패 4건까지 감소 (이식 초기 411행 중 161행 미매칭 → 해소).

### 남은 것
- 롯데 송장탭 날짜 열이 501행 모두 빈칸 → 그 원천만 날짜 필터가 걸리지 않는다.
  탭이 매일 새로 쓰이면 무해하나, 누적되는 구조라면 열을 다시 잡아야 한다.
  진단 메뉴가 열 목록을 찍어 준다.
