# 변경이력 — 2026-09-03

## 사방넷 송장대량등록 ← 세트분리(뉴) 연동

### 배경
세트분리 V2(세트분리(뉴) 시트, `1JuwZjorb…`)의 「🔁 송장 전파」가
롯데 실적 + 대리공급_임시기록을 합쳐 「사방넷등록」 탭(주문번호·운송장번호·택배사)을 만든다.
이 결과를 기존 메뉴 「5️⃣ 허브로 송장 수집 → 📊 사방넷 송장대량등록 엑셀 저장」이
그대로 포함하도록 원천을 하나 늘렸다. 팀 워크플로 변화 없음.

### 변경
| 파일 | 내용 |
|------|------|
| `_partnerOrders.gs` | `_po_rebuildSabangnetBulkUpload_()` 에 5번째 원천 추가 — 세트분리(뉴) 「사방넷등록」 탭 (기존: 임시기록·허브·롯데탭·구세트분리 송장매칭) |

### 동작
- 주문번호|송장 `seen` 중복 제거라 구 세트분리와 병행 운영 중 겹쳐도 안전
- V2 전화주문 ID(`^\d{6}-PH-`)는 사방넷 주문번호가 아니므로 제외
- 택배사가 롯데가 아니면 `_pep_sabangCodeForCarrier_` 로 사방넷 코드 변환, 미지정은 `skipNoCode`
- V2 시트를 못 읽으면 건너뛰고 로그만 남김 (기존 4개 원천은 그대로 동작)
- 결과 로그: `[사방넷대량등록] 세트분리(뉴) 보강 N건`

### 관련 (세트분리V2 프로젝트 쪽, 별도 배포)
- 「🔁 송장 전파」가 대리공급_임시기록(상품정보 P/V/X열)도 읽어 롯데 실적과 병합
- 원장(주문라인원장)에 운송장번호·송장매칭(롯데 직접/대리공급/합포장 전파) 기록
- 상세: `세트분리V2/README.md`
