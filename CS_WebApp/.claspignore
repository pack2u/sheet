**/*.png
**/node_modules/**
**/_favicon_b64.txt

# 로컬 검증용 Node 테스트 (GAS 에 올리지 않는다)
*_test.js

# 브라우저로 눈으로 확인하려고 만든 임시 미리보기 (GAS 에 올리지 않는다)
_preview_*.html

# ★ 비밀키는 push 로 왕복시키지 않는다 (2026-09-08) ★
#   종전에는 _secrets.gs 가 push 대상이라, 로컬 파일이 잘못되면 그대로 서버를 덮었다.
#   2026-09-02 에 실제로 로컬 _secrets.gs 가 빈 파일이 된 적이 있다. 그때 push 가
#   나갔으면 운영 CS웹앱의 Gemini·허브 연동이 통째로 죽었다.
#   서버 값은 Apps Script 편집기에서 직접 관리한다. 키를 바꿀 때도 편집기에서 바꾼다.
_secrets.gs
