// ═══════════════════════════════════════════════════════════════════
// [단일 소스] 배포 시트 onEdit 자동완성 코드
// ───────────────────────────────────────────────────────────────────
// ★ 2026-07-03: 라이브러리 패턴 전환
//   기존: 200줄+ 로직을 업체 시트에 직접 주입 → 수정 시 전체 재설치
//   전환: 1줄 wrapper만 주입 → 허브 라이브러리 함수 호출
//   결과: clasp push만 하면 전체 업체에 즉시 반영!
// ═══════════════════════════════════════════════════════════════════
function getPartnerOnEditCode_() {
  return `
// ── [Pack2U] 발주탭 onEdit — D/L 무음 복원 (2026-07-20) ──
// ★ 복붙으로 D(품목명)/L(정산금액)에 값이 들어오면 메시지 없이 걷어내 스필 수식 원복
// ★ 품목명·단가 표시는 D1/L1 스필 수식이 담당 (즉시 표시)
function onEdit(e) {
  Pack2U.p2u_partnerOnEdit(e);
}
`;
}
